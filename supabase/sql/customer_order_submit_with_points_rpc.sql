-- ============================================================================
-- 🗑 폐기됨 (2026-07-05 본문 제거 완료) — 실행해도 아무 일 안 일어남
-- ============================================================================
-- 이 파일에는 원래 submit_customer_order_with_points 를 "재고차감 없이" 정의하는
-- 구버전 함수 본문이 있었습니다. 실수로 재실행하면 라이브 DB의 재고 자동차감이
-- 조용히 사라지는(오버셀 사고) 위험이 있어 2026-07-05에 본문을 완전히 제거했습니다.
--
-- ▶ 주문 제출 RPC의 단일 기준 = supabase/sql/inventory_auto_deduct_rpc.sql
--   (재고차감 + 포인트 차감 + 이력 + 잔액 갱신 통합 버전, 2026-07-05 점검에서
--    라이브 DB 배포본이 재고차감 버전(stock_variants 포함)임을 확인함)
--
-- ▶ 구버전 본문이 필요하면 git 히스토리에서 확인:
--   git log -- supabase/sql/customer_order_submit_with_points_rpc.sql
-- ============================================================================
