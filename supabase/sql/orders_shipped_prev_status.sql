-- 목적: 라이브 "출고완료 처리" 시 직전 주문상태(입금확인/자동입금확인/수동입금확인/카드결제완료 등)를 보관.
--   → 출고완료 후에도 입금 배지를 원래대로 표시하고, "출고완료 해제" 시 직전 상태로 정확히 복원하기 위함.
-- 안전: ADD COLUMN only. 기존 데이터/돈/입금/정산/배송 로직에 영향 없음(새 컬럼은 NULL 기본).
--   기존 데이터는 모두 NULL이라 동작 변화 없음. 새로 출고완료 처리하는 주문부터 직전 상태가 채워짐.

alter table orders add column if not exists shipped_prev_status text;
