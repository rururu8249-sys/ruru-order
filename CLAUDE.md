@AGENTS.md

# CLAUDE.md — 루루동이 프로젝트 지침

## ★ 세션 시작 시 필수
이 파일을 먼저 읽고 "## 진행상황"과 "## 남은 작업"을 확인할 것.
세션6까지 완료된 줄 알지 말고, 아래 진행상황의 최신 날짜·항목을 기준으로 이어갈 것.

## ★ 작업 완료 시 필수 (과거에 갇히지 않게)
git push로 작업을 배포할 때마다, 반드시 이 파일의 "## 진행상황" 맨 위에
한 줄을 추가하고(날짜+완료내용), 끝난 항목은 "## 남은 작업"에서 제거할 것.
이 갱신을 빼먹으면 다음 세션이 과거 상태로 시작되므로 절대 빠뜨리지 말 것.

## ★ 작업 중 끼어든 요청 처리 (삼천포 방지)
작업 도중 사장님이 다른 질문·요청을 하면:
1. 먼저 "지금 진행 중이던 작업"을 기억에 붙들 것 (절대 잊지 말 것).
2. 끼어든 요청이 급하면(버그·운영지장·즉답필요) → 그것부터 해결하고, 끝나면 "원래 OO 작업으로 돌아갑니다" 하고 복귀.
3. 끼어든 요청이 안 급하면(나중에 해도 됨) → "## 남은 작업"에 한 줄 추가만 하고, 현재 작업 계속 진행.
4. 단순 질문(코드수정 아님)이면 → 답만 하고 바로 원래 작업으로 복귀.
5. 매 응답 끝에 진행 중 작업이 있으면 "현재 작업: OO" 한 줄로 상기시킬 것.

## ★ 현재 진행 중 작업 (있으면 여기 기록, 끝나면 지움)
(없음)

## 진행상황 (최신이 맨 위 · push할 때마다 갱신)
- 2026-07-17 **[돈·정책변경] 합배송 기준을 "배송비 낸 주문"→"범위 내 같은 주소 유효 주문 전부"로**(app/order/page.tsx 1파일 5+/3-, 사장님 정책 확정): 스마일(01071051816) 사례 — 합배송 시간창 안에 이전 주문 2건(id 2612/2647, 알로나시 무료배송이라 shipping_fee 0)이 있는데 새 주문(2675)에 4,000원 부과. 원인=`checkAlreadyPaidShippingGroups`의 `hasPaidShippingFee`(배송비>0) 게이트 — 무료배송 상품만 먼저 산 고객은 기준 주문이 안 됨(읽기 전용 SQL로 데이터 확정). 사장님 결정 "0원이었건 만원이었건 범위 안이면 배송비 0" → 게이트 1줄 제거(주소 동일·취소 제외·시간창/방송 조건 전부 유지, `hasPaidShippingFee`는 이 1곳만 사용이라 영향 격리·import 정리). ⚠️부작용 명시: 무료배송 상품 먼저 산 고객은 그 범위에서 배송비를 아예 안 냄(사장님 인지 후 확정). 검수 tsc 0에러 + **판정 시뮬 8/8 PASS**(스마일 0원기준 합배·취소 제외·다른주소 부과·첫주문 부과·업체/일반 그룹 분리 유지·기존 배송비지불 케이스 회귀 0·괄호 정규화). 기존 주문 소급 재계산 없음(2675는 필요 시 관리자에서 배송비 수동 조정). DB변경 없음.
- 2026-07-17 **담김 현황: 담은 시각 표시 추가**(LiveCartHoldsModal 1파일 20+/2-, 표시 전용): 그룹 헤더에 `🕒 M. D. 오후 h:mm 담음`(KST, Intl) — 남은 시간 옆. 값은 예약 created_at 최신값 = **마지막 장바구니 갱신 시각**(재접속 시 sync로 갱신되는 한계 주석 명시). 검수 tsc 0에러. DB변경 없음.
- 2026-07-17 **담김 현황 v2: 시간 기준 → "현재 방송 진열 상품" 기준으로 교체**(cart-holds route+모달 문구, 읽기 전용): 직전 5c4a314의 `created_at >= started_at` 필터가 **실패** — 고객 장바구니에 남은 옛 상품도 재접속 시 예약 전체교체(sync)로 created_at이 새로 찍혀 시간으론 못 거름(사장님 실화면 확인). → 활성 방송의 `broadcast_products`(is_visible!==false) product_id 집합으로 필터. 안내문구 "진열된 상품 담김만 표시 중". scope=all 토글·방송 OFF 전체 표시는 유지. 쓰기는 여전히 clear의 cart_reservations.delete뿐. 검수 tsc 0에러.
- 2026-07-17 **담김 현황: 기본을 "현재 방송 시작 이후"로 제한 + 전체 보기 토글**(cart-holds route+LiveCartHoldsModal 2파일, 읽기 전용·선점해제/재고/돈 무접촉): 선점 유지시간을 설정으로 늘린 뒤(수 시간~) 지난 방송·쇼핑몰 모드 때 담긴 것까지 섞여 나와 "등록 안 한 상품이 왜 나오냐" 혼란. → GET에 활성 방송 탐지(대시보드/미션과 동일 패턴: status 대소문자 무시+삭제 제외) 후 `created_at >= started_at` 필터, `?scope=all`이면 기존대로 전부. 응답에 scope/broadcastTitle 실어 팝업 안내줄("📺 현재 방송(제목) 시작 이후 담김만 표시 중")+[지난 것까지 보기]/[현재 방송만 보기] 토글. 방송 OFF면 기존대로 전체 표시(쇼핑몰 모드). 검수 tsc 0에러 + 2파일(59+/9-). DB변경 없음.
- 2026-07-17 **유튜브 SEO 생성기: 설명문에 주문 사이트 링크 추가**(youtube-seo/page.tsx 1줄 교체): 설명문 템플릿 "주문 방법" 섹션에 `🛒 주문은 여기서 → https://ruru-order.vercel.app/order` 삽입("방송 중 안내" 문구 자리 교체). 검수 tsc 0에러. DB변경 없음.
- 2026-07-17 **유튜브 SEO 생성기: 연관검색어 다중 브랜드 조회 수정**(youtube-seo/page.tsx 1파일 28+/6-): "휴먼메이드,꼼데,apc" 입력 시 통짜 문자열 1회 조회라 맨 앞 브랜드 연관어만 나오던 문제 → 쉼표 분리 후 **브랜드별 각각 조회(Promise.all, 최대 6개)** 하고 라운드로빈으로 섞어 상위 18개 표시(쏠림 방지). 직접 입력 키워드는 기존대로 단일 조회. API route 무변경. 검수 tsc 0에러. DB변경 없음.
- 2026-07-17 **유튜브 SEO 생성기 v3 — 썸네일 "디자이너 퀄리티" 재작업**(youtube-seo/page.tsx 1파일만·API/DB 무접촉, v2와 같은 커밋에 포함 가능): 사장님 "미리캔버스처럼" 요청. ①**전용 폰트** — Black Han Sans(유튜브 썸네일 표준 검은고딕)+Jua 구글폰트 동적 로드(`document.fonts.load` 후 재렌더, 실패해도 시스템폰트 폴백). ②**디자인 템플릿 3종 선택 UI** — [포토 임팩트](사진 풀블리드+하단 그라데이션+3D 오프셋·검정외곽선 헤드라인+노랑 하이라이트바 브랜드줄+빨강 스타버스트 특가 스티커(문구 8자 초과 시 노란 박스 폴백)+반투명 채널 칩) / [반반 스플릿](좌 컬러패널 대각선 컷+흰 스트라이프 경계+도트 패턴+우 사진 풀블리드) / [심플](중앙정렬, 사진은 브랜드색 82% 톤 오버레이 배경, 브랜드 테두리 칩). ③사진에 `ctx.filter saturate(1.12) contrast(1.06)` 보정. 신규 캔버스 헬퍼: sparkle/starburstPath/drawStarBadge/highlightBar/measureBar/hexToRgba. 검수 tsc 0에러. DB변경 없음.
- 2026-07-17 **유튜브 SEO 생성기 v2 — 사장님 피드백 3건 반영**(youtube-seo/page.tsx 1파일만·API/DB 무접촉): ①해시태그 "전체 복사" 제거(설명문에 이미 포함) → **"태그(유튜브 태그 입력칸용)" 쉼표 나열 복사** 신설(스튜디오 태그칸에 통째 붙여넣기). ②연관검색어 칩 클릭 = 복사 → **선택 토글(최대 5개)로 변경, 선택 시 제목 후보·태그·해시태그·설명문에 즉시 반영**(선택 키워드로 시작하는 제목 후보 추가). 연관검색어 섹션을 제목 위로 이동(먼저 고르고 제목 생성하는 순서). ③**썸네일 전면 재작업** — 기존 "우측 액자 배치"(카드뉴스식) 폐기 → 유튜브 썸네일 표준 문법: **사진 1280×720 풀블리드(cover) + 하단 어두운 그라데이션 + 흰 글씨/두꺼운 검정 외곽선(strokeText) + 브랜드 줄 노랑 + 특가 배지 우상단 노란 박스**. 사진 없으면 그라데이션 배경 템플릿(장식 원 2개). 썸네일 문구 자동 채움도 브랜드/메인 분리(쉼표→` · `). 검수 tsc 0에러. DB변경 없음.
- 2026-07-17 **사이드바에 "유튜브 SEO" 링크 버튼 추가**(AdminLiveSidebar 1파일 17줄 순수 추가·기존 줄 변경 0): 메뉴 목록 아래 `<a href="/admin-live/youtube-seo" target="_blank">` 링크(🎬 유튜브 SEO·제목·썸네일 생성기, 새 탭 — 방송 중 컨트롤타워 안 끊김). ADMIN_LIVE_MENUS state·activeMenu 체계 미사용(단순 링크)이라 메뉴 클릭/배지/모달 로직 무변경. 검수 tsc 0에러 + git diff 17+/0-. DB변경 없음.
- 2026-07-17 **유튜브 SEO·썸네일 생성기 신규**(커밋 f6fdafb. 신규 파일 2개만, 기존 파일 무변경·DB 무접촉): `app/admin-live/youtube-seo/page.tsx`(독립 툴 페이지, youtube-test 패턴 미러링 — 템플릿 제목/설명/해시태그 생성 + 유튜브 자동완성 연관검색어(`suggestqueries` 프록시) + 캔버스 1280×720 썸네일 PNG 다운로드) + `app/api/admin-live/youtube-seo/route.ts`(GET=자동완성 프록시, POST=Claude AI 보강 — `ANTHROPIC_API_KEY` 미설정 시 ok:false 응답, 페이지는 템플릿 폴백. 키는 선택사항). 접근: `/admin-live/youtube-seo` (미들웨어 자동 보호 확인 — matcher `/api/admin-live/:path*` 포함, 사이드바 메뉴 미추가·youtube-test와 동일하게 URL 직접 접근). 검수: 맥 로컬 npm run build Compiled OK + BANKDA 가드 2개 통과 + tsc --noEmit 통과 + CC 교차검증(돈/Supabase 접근 0·모델 ID 정상·라우트 인증 보호 확인). DB변경 없음.
- 2026-07-17 **주문서: 유튜브 닉네임 미입력이면 진입 차단(닉네임 모달 강제)**(커밋 90115d9. **렌더 게이트 1곳만·로그인/제출/돈/재고 로직 무변경**): 기존엔 닉네임 입력 모달을 `isKakaoLoginReturn`(카톡 복귀)일 때만 띄워서, 재로그인으로 이름/주소만 복원돼 `hasSavedInfo=true`가 된 케이스(닉네임만 빈)에서 관문을 건너뛰고 담기가 됐음(중복계정·닉네임 미입력 사고의 A 원인). → `mustEnterYoutubeNickname = hasSavedInfo && !youtubeNickname.trim()` 신설, 조건을 `if ((isKakaoLoginReturn || mustEnterYoutubeNickname) && !isAutoLoggedIn)`로. 정상 고객(닉네임 있음)은 `isAutoLoggedIn=true`라 안 걸림. **초기 return(렌더 게이트) 조건 1곳만 변경** — 담기/제출/포인트/배송비/RPC 전부 무변경. 검수 BANKDA 가드 2개 + Compiled OK + 1파일(7+/1-). DB변경 없음.
- 2026-07-16 **물건챙기기 엑셀: "챙김" 컬럼 추가**(커밋 79c51b9. 표시/출력 전용·돈/재고/상태 write 없음): 피킹 엑셀에 6번째 컬럼 `챙김`(팝업에서 체크=`챙김`/미체크=`안챙김`). 판정은 팝업 화면과 **동일한 `pickedIds` 집합**(`orders.picked_at` 기반)을 export에 전달 — 화면·엑셀 일치. `exportLiveOrdersForPicking(orders, meta, pickedIds?)` 3번째 인자 옵셔널이라 `pickedIds` 없으면 컬럼 생략(하위호환). 상품행은 `item.id`, 상품행 없으면 `order.id`로 매칭(팝업 `togglePick`과 동일 키). 검수 BANKDA 가드 2개 + Compiled OK + 2파일(18+/8-). DB변경 없음.
- 2026-07-16 **담김 현황: customers(카톡 가입정보) 2차 폴백**(커밋 5c9cd7d. 읽기 전용·차감/주문/돈 무접촉): 예약 저장값(1차)·주문이력(폴백)에 이어 `customers` 테이블을 **2차 폴백**으로 추가 — 주문 이력조차 없는 신규 고객도 가입/닉네임 입력값으로 표시. `customer_phone`이 하이픈 유무 섞여 저장돼 있어 **두 형식 모두로 조회**(주문페이지와 동일 패턴), 매칭 키는 숫자만으로 정규화(예약 phone도 숫자만이라 일치). `customers`는 `select`만(youtube_nickname·customer_name), 쓰기 없음. 우선순위: 예약 저장 닉네임 > 주문이력 > customers. 검수 BANKDA 가드 2개 + Compiled OK + 1파일(21+). DB변경 없음.
- 2026-07-16 **담김 현황: 담는 시점 닉네임/이름 저장·표시(첫 구매 고객 포함)**(커밋 812a492. 표시 전용·주문 제출/차감/돈 로직 무접촉): 직전 c1d1960은 주문 이력으로 닉네임을 매칭해서 **첫 구매(주문 이력 없는) 고객은 이름이 안 떴음**. 담는 시점에 이미 카톡 로그인+닉네임을 거치므로 그 값을 예약에 직접 저장. ①`app/order/page.tsx` 예약 sync POST body에 `nickname`/`customerName` 추가(각 40자 컷, **try/catch 안 — 실패해도 주문 흐름 무영향**, 돈/제출 로직 무변경). ②`cart-reservations` route가 그 값을 `cart_reservations.nickname`/`customer_name`에 저장(insert에만 추가, TTL·차감 무관). ③`cart-holds` route는 **저장된 닉네임 우선**, 없으면(컬럼 추가 전 옛 예약) 주문이력 폴백. ⚠️`cart_reservations`에 `nickname`/`customer_name` 컬럼 **이미 존재 확인**(service_role로 조회 성공) → 배포 안전(컬럼 없으면 insert 실패로 선점표시 멈출 수 있었음). 검수 BANKDA 가드 2개 + Compiled OK + 3파일(20+/3-). DB변경: 없음(컬럼 이미 있음).
- 2026-07-16 **담김 현황: 상품명 미표시 버그수정 + 닉네임(이름) 표시**(커밋 c1d1960. 읽기 전용·차감/주문/돈 로직 무접촉): ①**버그수정** — cart-holds 라우트가 `products.select("id, product_name, name")`로 조회했는데 **`name` 컬럼이 products에 없어** select가 통째로 에러 → 모든 상품이 "상품"으로 나오던 문제. 실제 존재하는 `product_name`만 조회로 수정, 에러도 삼키지 않고 응답에 실어 보냄. ②**닉네임/이름 표시** — 예약(`cart_reservations`)엔 전화번호만 있어, 같은 `customer_phone`의 `orders` 이력에서 `youtube_nickname`/`customer_name`을 찾아 붙임(읽기 전용 `select`, limit 500, 이력 없으면 빈 값). 모든 접근은 읽기(cart_reservations·products·orders) + `cart_reservations.delete`(clear)뿐 — products.stock·orders 쓰기·RPC 없음. 검수 BANKDA 가드 2개 + Compiled OK + 2파일(60+/17-). DB변경 없음.
- 2026-07-16 **장바구니 선점: 유지시간 설정화 + "담김 현황" 팝업/관리자 API**(커밋 37c7611. **진짜 재고 차감/복구·주문·돈 로직 무접촉** — cart_reservations(표시용 예약)만 읽고 지움): ①**유지시간 설정화** — `cart-reservations` route의 `HOLD_MINUTES=15` 하드코딩을 `settings.cart_hold_minutes`에서 읽게(`getHoldMinutes`, **10분~30일 클램프**, 읽기 실패/미설정 시 기본 15분). 예약 `expires_at` 계산에만 사용, insert 로직·차감 RPC 무변경. 설정 UI(`AdminLiveSettingsPanel`)에 분/시간/일 단위 선택(`decomposeHoldMinutes`, 저장은 분으로 환산·API와 동일 클램프). ②**담김 현황 팝업** — 신규 `LiveCartHoldsModal` + `LiveOrderTable`에 `🛒 담김 현황` 버튼(최소 추가: import+state+조건렌더+버튼 1개, 필터/정렬/페이지/onSelectOrder 무변경 — 제한 파일 규칙 준수). "담기만 하고 아직 주문서 제출 안 한 고객" 목록 표시. ③**신규 관리자 API** `app/api/admin-live/cart-holds/route.ts`(`verifyAdminSessionFromRequest` 인증) — GET: `cart_reservations` 읽기 + `products` 이름만 조회(select), POST `{action:"clear", sessionKey}`: 그 세션 예약 강제 해제(`cart_reservations.delete`). **products.stock·orders·RPC 절대 안 건드림**(주석·grep 확인). 검수 BANKDA 가드 2개 + Compiled OK + 5파일(347+/3-). ※`cart_reservations` 예약은 원래 표시용 선점(진짜 재고 차감은 주문제출 RPC 단일 소유) — 이 기능은 그 예약을 조회/해제만 함. DB변경 없음(settings 키 1개 사용).
- 2026-07-16 **물건챙기기: 상품금액 표시(팝업+엑셀) + 엑셀 5컬럼 축소**(커밋 51d216c. 표시/출력 전용·돈 재계산·주문/재고 로직 무관): ①**피킹 팝업**(`LiveOrderPickingModal`) — `PickItem`에 `amount` 추가(주문에 저장된 값 그대로, 상품행이면 `item.amount`·없으면 `order.productAmount`). 상품 줄에 `N원`(딥로즈) 표시, 0원이면 숨김. **재계산 안 함**(표시 전용). ②**엑셀**(`exportLiveOrdersForPicking`) — 물건챙기기 내보내기를 11컬럼(방송명·주문시간·닉네임·이름·전화·상품명·옵션·수량·결제상태·주문번호·배송메모) → **5컬럼(닉네임·상품명·옵션·수량·상품금액)**으로 축소, 금액은 저장값 그대로. 다른 엑셀(로젠 송장 등)은 무변경. 검수 BANKDA 가드 2개 + Compiled OK + 2파일(13+/30-). DB변경 없음. `types.ts`는 무변경(`item.amount`/`productAmount` 기존 필드).
- 2026-07-12 **위젯: 이미지 로드 실패 자동 재시도 + 고정/토글/진열 실시간 반영**(커밋 2cee327. 표시 전용·돈/주문/DB 무변경): ①**이미지 재시도**(`ProductWidgetClient`) — 불안정 회선(중국 현장 등)에서 저장소 이미지가 한 번 실패하면 재시도 없이 빈 카드로 남던 문제. `<img onError>`에서 1.5초 간격 **최대 6회** 자동 재시도, 재시도마다 캐시 우회 쿼리(`?wr=n`)로 강제 재요청. 상품이 바뀌면 `imgRetry` 리셋(신규 useState/useEffect, 무조건 호출이라 rules-of-hooks OK). ②**즉각 반영** — 기존 20초 폴링에 realtime 구독 3개 추가(각 공용 0.4초 디바운스 `scheduleStockReload` 호출): `products` UPDATE(고정/재고/이미지), `broadcast_products` `*`(담기/빼기/순서), `broadcasts` UPDATE(위젯 ON/OFF 토글·시작/종료). 실시간이 안 와도 20초 폴링이 백업. 구독은 방송 스코프가 아니라 해당 테이블 변경 시 리로드(디바운스라 무해). 검수 BANKDA 가드 2개 + Compiled OK + 1파일(25+/1-). DB변경 없음.
- 2026-07-12 **방송별 "위젯 상품카드 ON/OFF" 토글 신설 + 내장 배너 제거(PRISM 소스로 대체)**(커밋 4d88ab9. 표시/연출 전용·방송상태/정산/주문/돈 로직 무관): 방송 중 위젯의 상품카드만 껐다 켜고 싶다는 요청. ①**신규 컬럼** `broadcasts.widget_card_enabled boolean NOT NULL DEFAULT true`(`supabase/sql/broadcasts_widget_card_enabled.sql`, **ADD COLUMN only**, NULL/미설정=ON 취급→기존 방송 자동 ON). ②`liveBroadcastController`에 `setBroadcastWidgetCard(id, enabled)`(그 컬럼만 update) + 타입 필드. ③`AdminLiveDashboard` — `widgetCardOn = widget_card_enabled !== false`, 낙관적 토글+실패 롤백(쇼핑몰 토글과 동일 패턴). ④`LiveHeader` — `🖼 위젯 상품 ON/OFF` 버튼(방송 중에만 활성). ⑤`ProductWidgetClient` — `active.widget_card_enabled === false`면 카드 숨김(`setPinned(null)`/`setRotation([])` 후 return), undefined면 표시(폴링 20초 반영). **+배너 정책 변경**: 직전(f94e85a) 위젯 내장 배너(`<img src="/order-banner.png">`)와 배너-아래 카드 위치를 **되돌림** — 주문방법 배너는 **PRISM에 PNG 소스로 직접** 올려 운영(두 겹 표시 방지), 카드 기본 위치 좌하단 24px 복원. `order-banner.png`는 PRISM용으로 **유지**(코드 참조는 없어졌지만 파일 보존). 검수 BANKDA 가드 2개 + Compiled OK + 5파일(84+/20-). ⚠️**Supabase SQL Editor에서 `broadcasts_widget_card_enabled.sql` 실행 필요** — 미실행 시 위젯은 정상 표시(undefined→ON)이나 관리자 토글은 저장 실패(컬럼 없음). ⚠️맥 build+push 완료.
- 2026-07-12 **고정(is_pinned) 전역 플래그를 "현재 방송"에만 반영**(커밋 b5f56d4. 표시/연출 전용·고정 저장·`sort_order`·돈 로직 무변경): `is_pinned`가 상품 전역 플래그라 지난 방송에서 고정해둔 상품이 새 방송 위젯에 뜨거나, 그 상품 때문에 현재 방송 순서변경(드래그)까지 잠기던 문제. ①**위젯**(`ProductWidgetClient`) — 고정 상품도 **활성 방송 `broadcast_products`에 있을 때만** 표시(`pinnedInActive = p && ids.includes(pid) ? p : null`). 활성 방송 없거나 진열 0개면 카드 없음(배너는 별개로 항상 표시). ②**위젯 기본 위치** — 좌하단 24px → **배너 아래 우측**(`top: calc(16px + min(94vw,1400px)*0.35 + 16px)`, `right:24px`). 드래그 저장 위치(`pos`)가 있으면 그게 우선(PRISM에서 드래그 어려워 기본값만으로 배열되게). ③**상품관리**(`AdminLiveProductManagePopup`) — 방송상품 탭 드래그잠금을 `!pinnedId`(전역) → **`!pinnedInBcList`**(고정상품이 현재 보는 방송목록에 있을 때만)로. 목록에 없으면 `bcProductsView===bcProducts`라 인덱스 일치 → 드래그 풀어도 순서 안 엉킴. 검수 BANKDA 가드 2개 + Compiled OK + 2파일(26+/7-). DB변경 없음. ※배너 PNG가 2000×700(0.35) 아니면 위젯 카드 세로 간격 어긋남 → `0.35` 조정.
- 2026-07-12 **방송 위젯에 "주문방법 배너" 추가**(커밋 f94e85a. 표시 전용·돈/주문/드래그 로직 무관): `public/order-banner.png` 추가 + `ProductWidgetClient` 최상단에 `<img src="/order-banner.png">`(absolute, 상단 16px·가운데, `width: min(94vw, 1400px)`, `pointerEvents: none`). OBS 브라우저 소스 하나로 배너+상품카드를 함께 띄우기 위함. 상품카드·드래그 앵커·토스트·폭죽·실시간 구독은 전부 무변경(배너는 별도 absolute 요소). 검수 BANKDA 가드 2개 + Compiled OK + 2파일(14+). ※배너 교체는 `public/order-banner.png`만 덮어쓰면 됨. ※위젯 페이지 전체 `pointerEvents:none`이라 배너도 클릭 통과(OBS 캡처 방해 없음).
- 2026-07-12 **서바이벌 위젯: 껐다 켜도 옛 결과 자동재생 방지 + 돌리기 전 참가자 명단 대기표시**(커밋 ffceec4. 연출/표시 전용·추첨/지급 로직 무관): OBS 위젯(`app/event-survival/live`)이 폴링에서 `status==="result"`면 무조건 연출을 재생해서, **위젯을 새로 켜거나 소스 새로고침하면 지난 판 결과가 다시 돌아가던** 문제. ①`firstLoadRef`(신설) — 위젯 로드 후 **첫 폴링**에 이미 확정돼 있던 결과는 `lastEventKeyRef`만 기록하고 재생 안 함(명단만 대기표시). 켜져 있는 동안 **새로** 확정된 결과(`result_at|updated_at` 키가 바뀔 때)에만 연출. ②`showRosterReady`(신설 `useCallback`) — `idle/spinning` 상태이거나 아직 안 돌렸을 때 참가자 로스터만 배치(막대인간)해서 대기 화면 표시(`phase="ready"`). ③`rosterKeyRef` — 참가자 명단이 그대로면 매 폴링(2.5초) 재배치 안 함(깜빡임 방지), 참가자 0명이면 `setHasEvent(false)`로 완전 투명. 서버 확정 결과·`startFromServer`·지급은 전부 무변경(위젯은 표시만). 검수 BANKDA 가드 2개 + Compiled OK + 1파일(53+/8-). DB변경 없음. ※preview 모드(`?preview=1`)는 기존대로 샘플 동작.
- 2026-07-11 **상품 고정/해제 즉시반영(optimistic) + 백그라운드 무음 새로고침**(커밋 ca3289b. **DB 쓰기·위젯 로직 무변경**): 방송 상품 탭 `📌 고정`/`고정중` 토글이 느린 회선에서 DB 왕복+전체 `select("*")` 새로고침을 기다려 반응이 늦던 것 개선. ①`pinSingle`/`unpinSingle`이 DB 쓰기 **전에** `setProducts`(로컬 is_pinned 갱신)+`setPinnedId`로 화면부터 즉시 반영, 실패 시 `ruru-live-product-updated` 이벤트로 서버 상태 재동기화(자동 롤백). ②`loadProducts(silent)` 인자 추가 — 이벤트발 백그라운드 리로드는 로딩스피너 없이(무음), 최초 로드·재고 저장 등 명시 호출은 기존대로 스피너 표시. 고정은 여전히 "1개만"(전체 해제→그 상품 true) 2단 쓰기 그대로, `adminCatalogWrite`·`broadcast_products`·위젯(`ProductWidgetClient` is_pinned) 로직 전부 무변경. 검수 BANKDA 가드 2개 + Compiled OK + 1파일(12+/6-). DB변경 없음. ※카탈로그가 수백 개로 커지면 토글마다 전체 products 재조회가 부담될 수 있음 — 그때는 `onUpdated`를 대상 행만 패치하도록 좁히면 됨(현재 ≤80개라 무해).
- 2026-07-11 **상품관리 팝업: 검색어 유지(수정/등록 후 재오픈 시 리셋 방지)**(커밋 4cc9f79. 표시/상태 전용·돈/재고 무관): 기존 탭 유지(`initialTab`/`onTabChange`)와 **동일 패턴**으로 검색어도 부모(Dashboard)에 보존. `AdminLiveProductManagePopup` Props에 `initialSearch`/`onSearchChange` 추가, `search` state를 `useState(initialSearch ?? "")`로, `[search]` 변경 시 `onSearchChange?.(search)` 보고. Dashboard에 `lastProductSearch` state 추가해 `initialSearch`/`onSearchChange`로 연결. 상품 수정→팝업 닫힘(unmount)→재오픈 시 방송/쇼핑몰/전체 3탭 공용 검색어가 유지됨(수정하려고 검색한 게 매번 풀리던 불편 해소). 검수 tsc 0에러 + BANKDA 가드 2개 + Compiled OK + 2파일(14+/2-). DB변경 없음.
- 2026-07-11 **이벤트 "입금완료한 사람만": 화면 필터 무시하고 방송 전체 결제완료(무통장+카드) 대상**(커밋 688358d. 참가자 대상 계산만·추첨/지급 로직 무변경): `AdminLiveEventRoulettePanel.loadParticipants`가 paid 모드일 때 `orderGroupIds`를 `undefined`로 보내 서버가 화면 필터(기간/방송/상태) 대신 **활성 방송 전체**를 기준으로 잡게 함(auto/manual 모드는 기존대로 화면 필터). 서버 `isPaidOrderRowForRoulette`는 이미 `PAID_STATUS_VALUES`(입금확인·카드결제완료·출고 등 10개)라 무통장·카드 모두 포함. 안내문구도 "제출자 전체=화면 필터 / 입금완료=방송 전체 결제완료(무통장·카드)"로 수정. 검수 Compiled OK + 1파일(2+/2-). DB변경 없음.
- 2026-07-11 **서바이벌 생존자 수 입력 상한 추가**(커밋 414c223. 표시/입력 가드만·추첨/지급 무관): `+` 버튼·숫자 입력을 `Math.min(…, Math.max(1, finalParticipants.length - 1))`로 클램프 → 참가자−1 초과 입력 불가(기존 실행 시 가드 1070·서버 클램프 `participants.length-1`과 일치시킴). `−` 버튼은 기존 `Math.max(1, …)` 유지. 검수 Compiled OK + 1파일(2+/2-). DB변경 없음.
- 2026-07-11 **고객 주문페이지 버튼 문구 변경**(커밋 81a2261. 표시 전용·주문/돈 로직 무관): 담기 `담기`→`장바구니 담기`(4877), 제출 `… 주문서 제출 →`→`… 주문서 제출 및 결제 →`(5087), 하단바 `🛒 담은 상품 N개 · 확인하기 →`→`🛒 장바구니 담은상품 N개 확인하기 →`(5544). 검수 Compiled OK + 1파일(3+/3-). DB변경 없음.
- 2026-07-11 **방송 상품위젯에서 주문서 QR 블록 제거**(커밋 9a18ff5. 표시 전용·돈/재고/실시간 로직 diff 0): 2026-07-09 통합했던 위젯 좌측 딥로즈 세로 띠 + `QRCodeSVG`(`ProductWidgetClient`)를 통째로 제거(`qrcode.react` import·`ORDER_URL`·`QR_LABEL_W`/`QR_SIZE`·QR 블록 JSX). 드래그 핸들(`startDragWidget`)은 상품 카드에 그대로 유지. 관련 주석도 정리(QR 히스토리 1줄만 남김). 검수 Compiled OK + 1파일(3+/53-). DB변경 없음.
- 2026-07-10 **미션 앵커 버그 수정 — "미션 먼저 켜고 방송 시작" 순서에서 옛 방송 참조**(커밋 59fe9e2. 돈 로직 diff 0. **시뮬 23/23 PASS**): 사장님 질문 "방송 시작하고 미션 켜면 안 되는 건가?" → 코드 확인 결과 **실제 위험 발견**. 직전 커밋의 `missionBroadcastId = activeBc?.id || missionBroadcastId` 가 **활성 방송이 없으면 옛 방송 id를 그대로 유지** → 방송 시작 전에 미션을 켜면 명단·지급 대상이 **지난 방송 구매자**로 잡힐 수 있었음(돈). **수정 2곳**(mission/route.ts): ①POST 저장 시 꺼짐→켜짐 전이에서 활성 방송이 없으면 앵커를 **빈 문자열로 초기화**(옛 방송 참조 차단). ②GET(진행률, 패널이 6초마다 폴링)에 **앵커 자동 동기화** 추가 — 미션이 켜져 있고 ON 방송이 있는데 저장된 `mission_broadcast_id`가 비었거나 다르면 현재 방송으로 upsert. 미션이 꺼져 있거나 방송이 없으면 **건드리지 않음**(종료 후 명단 조회용 앵커 보존). try/catch로 감싸 동기화 실패가 진행률 조회를 막지 않음. **권장 순서는 여전히 "방송 시작 → 미션 켜기"**(미션 켠 시점부터 카운트 0). 순서를 바꿔도 앵커는 자동 보정됨. 검수 tsc 0에러 + BANKDA 가드 2개 + Compiled OK + **순수로직 시뮬 23/23**(켜는 순서 A/B·수정 전 버그 재현·ON 우선/앵커 폴백/삭제방송 null·종료 후 앵커 보존·편집 저장 시 시작시각 유지·마킹 재시도 3회/서버 폴백/전부 실패해도 세션 잠금 유지). ※배지 마킹의 **실전 동작은 시뮬로 증명 불가** — 방송 중 첫 당첨 때 `지급완료`로 바뀌는지 육안 확인 필요.
- 2026-07-10 **[돈 버그] 이벤트 "지급대기" 배지 → 중복지급 실제 발생. 마킹 서버 폴백+재시도로 차단**(커밋 f0a5048→7a7b88f. **포인트 지급 API(`customer-points`)·`lib/customerPoints` diff 0**): 사장님 — 자동지급은 되는데 배지가 계속 `지급대기`. **추정 금지로 읽기전용 SQL 대조**(`event_roulette_winners` × `customer_point_ledger` 시간창 조인) 결과 **가설 A 확정** — 6/20까지 `is_reward_done=true` 정상, **7/5부터 배지 false인데 실제 지급 1,000P씩 나감**. 게다가 **`쥬쥬엉니` 2건·2,000P 이중지급 실증**(배지가 안 잠겨 재실행 시 재지급). 원인=마킹 직전 당첨자 행을 **브라우저 supabase로 select**하는 구조(RLS/레이스/팝업 닫힘 중 하나만 어긋나도 winnerId 미확보 → mark 스킵 → 잠금 없음). 6/20 이후 이벤트 로직 커밋은 스타일(ac2f172)뿐이라 코드 회귀 아님 = 구조적 취약점. **수정**: ①신규 서버 액션 `mark_reward_done_by_event`(event-roulette route, `getSupabaseAdmin` service_role로 eventId+닉네임으로 당첨행 찾아 `is_reward_done=true`) — 기존 `markRewardDone`(winnerId 기준)은 무변경. ②`grantPointToWinner`의 마킹을 **3회 재시도**로, winnerId 있으면 기존 액션·없으면 서버 폴백 액션 사용(클라 select 제거). ③`grantPointToSurvivors`도 마킹 3회 재시도. 실패해도 세션 잠금 유지 + 경고 토스트("배지 직접 눌러 지급완료로"). **grant 호출·금액·전화번호 3단계 조회·중복가드 키 전부 무변경**. 검수 tsc 0에러 + BANKDA 가드 2개 + Compiled OK + 돈 API diff 0. ⚠️**기존 false 9건은 돈이 이미 나간 상태** → `update event_roulette_winners set is_reward_done=true ... where winner_at >= '2026-07-05'` 로 배지만 잠금(사장님 실행). ⚠️쥬쥬엉니 초과 1,000P 회수 여부는 사장님 판단(코드로 자동회수 안 함).
- 2026-07-10 **미션: 방송 종료 후에도 선물 명단·지급 가능**(신규 settings 키 `mission_broadcast_id`. 커밋 f0a5048): 사장님 — 이벤트 종료하고 깜빡한 채 방송을 끄면 명단이 안 나옴. **원인 확정**: `lib/mission.ts fetchMissionBuyers`가 `fetchActiveBroadcast`(status ON)만 찾고, 없으면 route 86행 `진행 중인 방송이 없습니다` 400. **수정**: ①`fetchMissionBroadcast` 신설 — ON 방송 우선, 없으면 settings `mission_broadcast_id`로 broadcasts 조회(삭제방송 제외). `fetchMissionBuyers`만 이걸 사용. **`computeMissionProgress`·위젯은 기존대로 ON 방송만**(변경 0 → 방송 끄면 위젯 사라지는 동작 유지). ②mission route: 미션이 **꺼짐→켜짐**될 때 그 시점 활성 방송 id를 `mission_broadcast_id`에 저장(종료·편집 저장에선 유지, 지우지 않음). ③앵커조차 없을 때만 400(문구 명확화). **지급 대상 계산(`resolveMissionWindow`·`isPaidRow`·dedup)·`payout_confirm` 가드 전부 무변경** — 조회 기준 방송만 넓힘. **자동지급은 도입 안 함**(사장님 선택: 확인창 유지 — 종료 시 명단이 뜨고, 지급은 모달의 [N P 지급 실행]을 눌러야만 나감). 검수 tsc 0에러 + BANKDA 가드 + Compiled OK. **DB 스키마 변경 없음**(settings 키 1개 추가).
- 2026-07-10 **[UX 복구] 상품관리 "방송 상품" 탭에 [📌 고정] 버튼 신설**(표시/진열 전용·돈/재고/주문 무관. 커밋 e073d08): 사장님 — 위젯이 계속 순환으로 바뀌는데 특정 상품만 고정할 데가 없음. **원인 확정**: `AdminLiveDashboard.tsx` 1563행이 `<LiveBroadcastPanels variant="column" hideProducts .../>` 라 **"🛍 지금 띄운 상품" 패널이 화면에서 빠짐**(그 자리에 라이브현황·고객이슈). 그 패널에만 있던 고정 UX(`goToLiveIdx`→`pinLiveProduct`, `▶ 자동순환`)가 통째로 사라져 **`is_pinned` 코드는 살아있는데 누를 버튼이 없는 상태**였음(전체 상품 탭 카드 `📌 고정 해제`와 위젯설정 일괄 `고정모드`만 남아 방송 중 쓰기 번거로움). **수정**(AdminLiveProductManagePopup 1파일): ①신규 `pinSingle(p)` — 기존 일괄 `pinSelected`와 **동일 방식**(`is_pinned=false` 전체 해제 → 그 상품만 `true`), `unpinSingle`엔 `setPinnedId("")`+토스트 문구 보강. ②신규 `pinnedId` state(products 로드 시 동기화, 고정/해제 즉시 반영). ③**방송 상품 탭 행마다 `📌 고정` / `📌 고정중`(딥로즈) 토글 버튼**을 `수정`·`빼기` 앞에 추가(선택 복사 모드면 숨김, `busyId` 가드, `stopPropagation`으로 행 클릭·드래그와 충돌 방지). 위젯(`ProductWidgetClient` 255행)은 원래 `is_pinned` 상품이 있으면 그것만 띄우고 없으면 순환 → 코드 변경 없이 즉시 동작. **고객 페이지 배지도 이미 존재**(app/order/page.tsx 4773행 "지금 방송에서 소개 중" 강조 카드 + 정렬 최상단, 방송 ON+고정일 때만) → 이 버튼 하나로 위젯·고객화면 동시 반영. **위젯에 "라이브상품" 배지는 미적용**(사장님 기존 지침 "라이브마켓 배지 넣지 말 것" + 상품 덮는 요소 제거 방침과 충돌). 검수 tsc 0에러 + BANKDA/CRON 가드 + Compiled OK + `broadcast_products`/주문/재고/돈 로직 diff 0.
- 2026-07-10 **미션: 이벤트 종료 시 선물 명단 자동 표시 + "포인트가 아닌 선물"을 주문서에 0원 행으로 얹기**(신규 훅 1개·패널 1파일. 커밋 022a6ef→b9fc3e0. **돈/재고/입금/정산 파일 diff 0**): 사장님 — 이벤트 종료하고 선물 주는 걸 깜빡함 + 미션이 무조건 포인트 이벤트인 건 아님(사은품·무료배송 등). ①**종료 즉시 명단**: `endEvent` 성공 후 `openPayout()`(읽기 전용 `payout_preview`) 자동 호출 → 선물 줄 명단 모달이 바로 뜸. `payout_preview`는 원래 reward 0이어도 명단을 반환(400은 `payout_confirm`만) → 포인트 미설정이어도 명단 확인 가능. ②**모달 분기**: reward>0이면 제목 `구매자 전원 포인트 지급 — 최종 확인`+[N P 지급 실행], reward=0이면 `🎁 선물 줘야 할 구매자 명단`+지급버튼 숨김+안내문("사은품이면 직접 챙겨주세요"). **[📋 명단 복사]** 신설(`1. 닉네임 (43,000원)` 클립보드 → 밴드/카톡 공지용). 구매자 0명일 때 "이미 전원 지급됐어요"라고 잘못 뜨던 것 → `totalBuyers`로 분기해 "구매자가 없어요"로 수정. ③**사이드 버튼 라벨**: reward 없으면 `🎁 구매자 전원에게 지급하기` → **`🎁 선물 줄 명단 보기`**("지급"이라 겁나서 못 누르는 문제). 둘 다 openPayout(조회)만 호출, 실제 지급은 모달 안 버튼에서만. ④**신규 `components/admin-live/useLiveOrderGiftAdd.ts`** — 명단 모달에 `선물 이름` 입력+[주문서에 넣기]+`중복 허용` 토글. 대상들의 **가장 최근 결제완료 주문서**(비취소·비테스트, `isPaidRow`=PAID_STATUS_VALUES 동일 기준)에 `🎁 {선물명}` 행 1줄 INSERT. **기존 `useLiveOrderItemAdd.addDirectItem`은 127행 `unitPrice<=0` 가드 때문에 0원을 못 넣음 → 그 가드는 안 건드리고(일반 상품 보호) 선물 전용 함수를 복제 신설**. 선물 행은 `product_id=null`(재고 미차감)·`qty 1`·단가 0·`shipping_fee 0`·`vat_amount 0`(0원×수수료율)·`final_amount 0`·`point_used 0`, 그룹 공유필드(주문자/받는사람/주소/상태/방송/제외플래그/`created_at`)는 기준 행에서 복사 → **주문 총금액(final_amount 합) 불변 = 입금 자동매칭 안 깨짐**. `created_at` 복사라 주문일도 안 튐. ⑤**중복 방지 기준**(사장님 질문) = 포인트 ledger와 무관. **"같은 주문서(order_group_id)에 같은 선물명 행이 이미 있으면 건너뜀"**. 며칠 방송 중 다음날 새 이벤트에 그 사람이 **새 주문서**를 내면 정상 지급되고, 새 주문 없이 또 주려면 `중복 허용` 켜면 됨. 검수 tsc 0에러 + BANKDA 가드 2개 + Compiled OK + **선물 시뮬 14/14 PASS**(최근 주문서 선택·기준행 최소id·취소/테스트 제외·이미있으면 skip·중복허용시 재추가·무통장/카드 vat 0·총금액 불변·배송비 0·재고 미차감·포인트 0) + `useLiveOrderItemAdd/useBulkPointGrant/lib/mission/mission route/customer-points` **변경 0건**. ※선물 행은 송장·물건챙기기·고객 주문조회에 그대로 노출됨(의도).
- 2026-07-10 **미션 게이지 바 v3 — 가독성·문구·달성 연출**(app/event-mission/live 1파일·표시 전용·돈/지급 로직 diff 0, Cowork 세션. 커밋 e8a4e74): 방송 실화면 확인 결과 바가 가로를 다 먹어 글씨가 안 읽힘. ①**크기** — 위젯 폭 560→**470**(`?w=420` 조절 유지), 진행바 `flex:1`(가로 꽉참)→**150px 고정**, 행 높이 32→40. 글씨 장면A 20→**25px**, 장면B 문구 16→**22px**, 바 안 % 14→**18px**, 🎁 21→26px. 박스 밖 삐져나감 방지로 장면B에 `overflow:hidden`+말줄임(포인트 금액 길어도 안전). ②**문구**(업계 검색 근거 — Goal-Gradient: 목표 근접 시 행동 가속 / 행동형 카피: "N 남음"보다 "N 채우면 ○○ 열림"이 전환↑) — 장면A `🎯 다 같이 100% 목표달성 이벤트`, 장면B 평소 `100% 채우면 전원 선물 오픈!`, **90%↑(near) 전용 문구 신설** `조금만 더! 곧 전원 선물 오픈!`. 포인트 미설정(reward 0)이어도 "전원 선물"로 안내(위젯≠포인트 필수). ③**달성 연출** — 사장님 지침 "무한 반복은 산만" → `CELEBRATE_MS=8000` 신설, 100% 되는 순간부터 **8초만** 폭죽 14개 낙하(고정좌표 배열=SSR hydration 안전)+금빛 반짝+🎉 튐, 이후 **정적 고정**(초록 패널+금테두리+`🎉 100% 목표달성! 선물이 펑펑펑`). 늦게 들어온 시청자도 색으로 달성 인지. `near` 펄스는 done이면 미적용. 검수 tsc 0에러 + BANKDA/CRON 가드 통과 + Compiled OK + 무한 애니메이션 grep 3건 전부 celebrate/near 게이트 확인. ※미션이 settings `mission_active=false`면 위젯 완전 투명(체크만 하고 저장 안 누르면 안 뜸).
- 2026-07-09 **방송 상품위젯 v2 + 주문서 QR 통합 + 미션바 재배치**(표시 전용·돈/재고/실시간 로직 diff 0, Cowork 세션. 커밋 9e5c55e→aa02a44): ①**상품카드 리디자인** — 정사각형→**세로 3:4**(옷 사진이 세로라 정사각형이면 글씨가 상품을 덮음). 사장님 지침 "상품 덮는 바·30% 음영·그림자 전부 금지, **글씨 아웃라인만**" → 하단 바/그라데이션/boxShadow **전부 제거**(grep 0건), 상품명 17px·옵션 13px·금액 23px 전부 다방향 `text-shadow` 검정 아웃라인으로 사진 위 직접 노출. 위젯 뒤 그림자(떠있는 느낌)도 제거. ②**옵션 표시 정상화** — `없음/없슴/무/-/none/na`를 지우는 `cleanOptionText`, 사이즈는 **`36(S) 38(M) 40(L)`** 표기(`sizeDisplayLabel`, app/order/page.tsx와 동일 헬퍼). ⚠️**표시 전용 매핑**이라 DB 저장값·재고 variant 키는 `36`/`S` 그대로(기존 주문·재고 무영향). ③**SOLD OUT** — `isSoldOutWidgetProduct`가 고객 주문페이지 `isSoldOutOrderProduct`와 동일 기준(재고관리 ON일 때만, 옵션합/총재고). 30px+빨강 품절 pill. ※letter-spacing이 마지막 글자 뒤 여백을 만들어 가운데정렬이 왼쪽으로 치우치던 것 → 같은 값의 `textIndent`로 보정. ④**즉시 재고반영** — orders INSERT·상태변경 UPDATE 구독에 `scheduleStockReload()`(400ms 디바운스) 추가 → 품절이 방송 중 바로 반영. ⑤**토스트 문구·연출** — "주문서 제출!"→**"🎉 주문성공!"**, `💰 입금완료!`/`💳 카드결제완료!`, 폭죽이 카드 `overflow:hidden`에 잘리던 것 → 앵커로 빼고(zIndex 9) 14→26개. ⑥**주문서 QR을 위젯과 한 세트로** — `qrcode.react` QRCodeSVG, 고정 주소 `https://ruru-order.vercel.app/order`. 처음엔 가로 헤더바(📱 주문서 QR)였으나 **QR이 정사각형이라 헤더를 올리면 QR을 줄여야 함** → 사장님 선택 **B안: 왼쪽 딥로즈 세로 띠**(writing-mode vertical-rl, 30px, 글자 15px)로 바꿔 **같은 높이에 QR 160px**(기존 140px). 카드/QR/토스트/폭죽 전부 한 앵커라 드래그하면 같이 이동(위치 localStorage 저장 기존 유지). ⑦**세로 길이** — 방송화면에서 어깨 아래까지 내려오던 것을 `CARD` 240→**200**으로 축소, 위젯 전체 **443px**(모자 머리끝 위). 더 줄이려면 `const CARD` 한 줄만 낮추면 비율 그대로 축소. ⑧**미션 게이지 바 재배치**(app/event-mission/live) — 화면 가로 전체 배너 → `justifyContent:flex-start` + `width: min(96vw, {barWidth}px)`(기본 560, `?w=480` 조절 가능)로 **주문서 QR 왼쪽**에 배치. 폰트 vw→고정(20/14/16px). 문구 사장님 지침대로 장면A `🎯 100% 목표달성 이벤트`, 달성 시 `🎉 100% 목표달성! 선물이 펑펑펑 — 전원 N P!`. 검수 tsc 0에러 + BANKDA 가드 2개 + Compiled OK + boxShadow/linear-gradient grep 0 + 돈/입금/정산/재고 로직 diff 0. ※OBS는 소스 우클릭→새로고침해야 반영. ※신규 등록한 상품 20개는 재고관리 OFF라 SOLD OUT 미표시(켜야 뜸).
- 2026-07-09 **서바이벌(폭풍 생존게임) 이벤트 신설 — 이벤트 4번째 탭**(Cowork 세션, 목업 storm-survivor-square.jsx 기준): 방송 구매자 50~100명을 막대인간으로 뿌리고 라운드마다 재난(번개/파도/돌풍/우박/운석)으로 탈락 → **최종 생존자 K명**(설정) 확정 → 포인트 자동지급. ①**신규 `app/event-survival/live/page.tsx`**(OBS 투명배경 오버레이) — 탈락 엔진이 "서버가 정한 생존자 K명만 남기고 나머지 탈락" 방식이라 결과가 서버와 100% 일치. 공개 API 2.5초 폴링, 이벤트 없으면 완전 투명(방송에 빈 박스 안 뜸), `?preview=1&total=&winners=`로 단독 미리보기. SSR hydration 방지(mounted 가드 — Math.random 렌더). ②**신규 `app/api/event-survival/overlay/route.ts`** — 인형뽑기 공개 API와 동일 패턴(고정토큰 `survival_luludongi_live` 접두 매칭 → 최신 이벤트 1건), 읽기 전용. ③**서버 `resolveSurvivalEvent`**(event-roulette route에 신규 함수) — 고정 당첨자 우선 확정(원본 명단에서 찾아 중복제외 필터에 안 휘둘림) → 나머지는 **기존 검증된 `pickRouletteWinner` 가중치 추첨**으로 중복 없이 K명 채움 → 이벤트 update + `event_roulette_winners`에 **K행 insert**(이미 있으면 skip=재실행 중복 0). **기존 `spinEvent`(단일당첨) 무변경**. createEvent는 eventKind "survival" 분기만 추가(기존 claw/roulette 동작 보존). ④**관리자 탭**(AdminLiveEventRoulettePanel) — `⛈️ 서바이벌` 탭 추가, 참가자 3버튼(주문서 제출자 전체/입금완료만/수동)·중복당첨금지·가중치 토글·위젯주소·이벤트목록 **전부 기존 공용 재사용**. 신규: **생존자 수 K** −/+ 입력, **당첨 고정 다중선택**(최대 K명, 서바이벌만 배열 `fixedSurvivorNicknames`; 룰렛은 기존 단일 `fixedWinnerNickname` 그대로). ⑤**돈: 신규 `grantPointToSurvivors`** — 기존 `grantPointToWinner`는 dedup 키가 eventId라 "이벤트당 1명" 전제(그대로 쓰면 1명 지급 후 나머지 skip) → **돈 API(`/api/admin-live/customer-points` action:grant)·전화번호 3단계 조회(orders→customers→kakao_nickname)는 100% 동일**하게 쓰고 **dedup 키만 winnerId(당첨자 행) 단위**로 바꾼 별도 함수. 2중 가드(세션 ref + DB `is_reward_done`), 1명 실패해도 나머지 계속·실패자만 잠금해제(재시도 가능), 지급 성공 시 그 행만 mark_reward_done. **호출 게이트는 룰렛과 동일**(운영모드+선물=포인트+금액>0), 돌리기 전 `생존자 N명 × M P = 총 X P 실제 지급` confirm. **기존 grantPointToWinner/룰렛/인형뽑기 지급 로직 삭제·변경 0건**. 검수 tsc 0에러 + **생존자 선정 시뮬 13/13 PASS**(고정당첨 보장·중복0·K클램프(참가자-1)·가중치 97%·중복제외 회피·없는 고정닉 차단) + **지급 시뮬 14/14 PASS**(K명 1인1회·동일세션 재실행 0·새로고침 후 DB잠금 0·부분기지급 신규만·번호없음/실패 격리+재시도·테스트모드 0·금액0/직접입력 0·총액정확) + 돈/입금/정산/배송/Bankda 파일 변경 0건. ⚠️**Supabase에 `supabase/sql/event_survival_columns.sql` 실행 필요**(event_roulette_events에 `survivor_nicknames jsonb`·`winner_count int` ADD COLUMN only — **2026-07-09 사장님 실행 완료**). 미실행 시 서바이벌만 에러, 룰렛/인형뽑기/미션 정상(신규 컬럼은 서바이벌 전용 UPDATE에서만 사용). ⚠️맥 build+push. ※잔여 위험(룰렛과 동일): 지급됐는데 배지 마킹만 실패하면 새로고침 후 재실행 시 재지급 가능 → 배지 직접 클릭으로 지급완료 처리. ※생존자는 서버 확정 직후 지급(연출 종료 대기 안 함 → 연출 끊겨도 지급 누락 없음).
- 2026-07-06 **방송 위젯 연출 2건**(표시 전용·돈/판정 무관, Cowork 세션): ①**상품위젯 축하 오버레이** — 주문 제출/입금확인/카드결제완료 토스트를 문자열 상단바 → **상품 카드를 살짝 덮는 딥로즈 오버레이**(아이콘+제목 "주문서 제출!"/"입금 확인!"+닉네임 크게+상품·금액, 3초 자동소멸·큐 순차 유지)로 교체. 폭죽 위치도 카드 위치(pos) 따라가게(드래그 이동 시 동행). toastQueue 구조화(문자열→객체), realtime 구독/중복가드/금액 컬럼(final_amount 우선) 무변경. ②**미션 위젯 기본멘트 교체**(사장님 지침) — 장면A "다 같이 구매할수록 바가 가득 차요!"→"🔥 지금 내 주문 하나가 전원 선물을 앞당겨요!"(구매욕·선물욕 자극형, 선물 금액 표시는 장면B 유지). tsc 0에러. ⚠️맥 build+push.
- 2026-07-06 **[버그] 방송 ON 중 표가 항상 현재 방송만 보이던 문제 수정 + 컷 조건 확장·안내 배지**(사장님 제보 — 배지 매칭3·매출바 250,000원인데 표엔 2건만, "필터 걸어도 예전 주문 확인 불가"): LiveOrderTable baseOrders의 방송묶음 컷(broadcastStartedAt 이후만)이 필터 무관 무조건 적용이었음 → **컷은 "방송=현재 방송 AND 기간=전체(기본 화면)"일 때만** 적용, 방송/기간 필터 중 하나라도 바꾸면 해제(기간 오늘/7일/직접, 방송 전체보기/과거 방송 전부 과거 주문 표시 가능). 방송 중 기본 화면은 기존과 동일. + 컷 활성 시 헤더에 "이번 방송 주문만 표시 중 · 지난 주문은 기간/방송 필터 변경" 배지(뭘 보고 있는지 명시). 판정 시뮬 6/6 PASS. 표시 전용·돈 로직 무관. tsc 0에러. ⚠️맥 build+push.
- 2026-07-06 **사이드바 예외 배지 클릭 이동 + 툴팁**(표시/필터 전용): 사장님 "매칭1·카드2가 뭔지 모르겠다" → 배지에 hover 툴팁(뜻 설명) + 클릭 시 주문·입금 화면으로 이동하며 기간/범위 전체 + 해당 상태 필터(manual_match_needed/card_unpaid) 자동 적용(과거 방송 건도 보이게 broadcast/date all — 전체 재조회는 기존 needsFullLoad 경로). AdminLiveSidebar 신규 prop onExceptionBadgeClick, Dashboard 핸들러. 배지 라벨에 "›" 붙여 클릭 가능함 표시. tsc 0에러. ⚠️맥 build+push.
- 2026-07-06 **어드민 2차 패스(사장님 "입금내역·정산 엉망" 피드백)**: 입금내역 느림의 근본원인 확정 — /api/admin-v2/deposits가 deposits+orders 전체를 통째 로드(4화면 공용)라 초기 수십 초 → **API 기간 파라미터 추가는 돈 인접 공용이라 위험분석 후 별도 세션(최우선 백로그)**. 이번엔: PaymentMatchPanel 영어 헤더 한국어화+로딩 안내 문구, LiveOrderTable 재고차감완료 배지 정상=무표시(예외만 배지, 판정 로직 무변경), LiveStatsCards 매출에 "(현재 조회범위)" 라벨. tsc 0에러. ⚠️맥 build+push.
- 2026-07-06 **어드민 실화면 UI/UX 분석 + 버그 2건 수정**(Cowork 세션, 실행버튼 안 누르고 열람만·리포트 어드민페이지_UIUX분석_20260706.md): ①**[버그] 정산 팝업 "방송종료 요약 리스트 401"** — app/api/admin-live/broadcast-end-reports/route.ts가 옛 인증(ADMIN_SESSION_TOKEN 원문비교) 쓰는 마지막 잔재였음(6/18 youtube 라우트와 동일 불일치) → verifyAdminSessionFromRequest(서명쿠키)로 교체, GET/POST await. ②**[UI] LiveOrderTable 주문내용 칸** — 여러 상품이 각각 "룰..."로 뭉개지던 것 → renderOrderSummary를 첫 상품 온전히+"외 N개"+hover 툴팁(title=전체목록)으로 최소수정(getVisibleOrderSummaryParts·다른 로직 무변경). 검수 tsc 0에러. ⚠️맥 build+push. ※리포트 백로그: 미매칭 138건 보관처리(위험분석 후), 방송OFF 우측 빈패널 축소, 재고차감 배지 예외만 표시, 매출바 "전체 누적" 라벨, 입금내역 영어 헤더 한국어화, 방송목록 중복이름, 썸네일 누락 표시.
- 2026-07-06 **2차 개선: 고객 송장 표시 + 전화번호 형식 검증 + 죽은 모달 제거**(표시/검증 전용·돈 로직 무접점, Cowork 세션): ①**송장 고객 표시** — orders.tracking_number/tracking_company(관리자 입력, DB에 이미 존재)가 고객에겐 "밴드에서 송장조회" 안내뿐이던 것 → MyOrderResultCard(마이오더)+CustomerOrderLookupBottomSheet(주문내역 시트, page.tsx 그룹 매핑에 trackingNumber/Company 추가)에 송장번호+[배송 조회하기] 버튼(로젠=ilogen.com/web/personal/trace/번호 직링크, 타 택배사=네이버 "택배사 번호" 검색). 밴드 안내 블록은 **송장 미등록 출고완료 주문에만 폴백**(showBandTrackingNotice에 !trackingNumber 조건). 조회 쿼리는 양쪽 다 select("*")라 컬럼 추가 불필요. ②**전화번호 검증 강화** — validate에 `/^01[016789][0-9]{7,8}$/` 추가(기존 10자리 이상만 체크라 지역번호 02도 통과했음 — 잘못된 번호는 입금매칭·알림톡·배송연락 전부 깨지는 근원). 검증 시뮬: 010 11자리/011 10자리 통과, 02·9자리 차단. ③**죽은 코드 제거** — OrderDepositConfirmModal(주석에 "띄우지 않습니다" 명시된 미사용 입금확인 모달) import/state/핸들러/렌더/shouldSkipDepositConfirm 제거(컴포넌트 파일은 잔존). 검수 tsc 0에러. ⚠️맥 build+push 필요. ※후속 후보(리포트 7장): 배송지 받는사람 연락처 검증, 재주문 버튼, 주문완료 배송일정 안내.
- 2026-07-06 **재고 홀드(담는 즉시 예약) 시스템 신설 + 결제수단 기본값 미선택**(Cowork 세션, 사장님 지침 "담는 즉시 차감돼야 함"): ①**결제수단 미선택 기본값**(5717b5a) — paymentMethod useState ""(미선택), validate에 "결제 방법을 선택해주세요" 차단 추가(카드 최소금액 게이트 앞), draft 저장조건 `!== "무통장입금"`→`!== ""`, 제출 후 리셋 ""(draft 복원은 기존 유효값만 복원 유지). setDone/OrderPriceSummaryBox/PaymentGuide prop은 "" 코어스("카드결제" 아니면 "무통장입금"). **제출 payload payment_method는 validate 통과 후라 항상 유효값(무변경)**. ②**재고 홀드** — 담기 즉시 진짜 재고를 깎는 대신(담고 이탈 시 영구증발 사고 구조) **cart_reservations 예약(15분 TTL+45초 하트비트 연장) 방식**: 신규 `supabase/sql/cart_reservations.sql`(신규 테이블·RLS enable+정책없음=anon차단·service_role 전용) + 신규 `app/api/cart-reservations/route.ts`(POST sync=세션 예약 전체교체 멱등/clear, GET ids별 byProduct/byVariant 합계·미만료만·exclude=본인세션, qty 0~99·40줄 클램프) + page.tsx: 세션키 localStorage `ruru_cart_session_key`, items 변경 1.5s 디바운스 sync(빈 주문서=해제, 제출 성공 시 items 리셋으로 자동 해제), 45초 하트비트+타인예약 fetch(탭 visible만). **반영 지점(표시 전용)**: 카드 sold 판정(옵션별 inCart+타인예약>=stock, 총재고 상품도 신규 커버), 임박배지 lowStockRemain/Options(reserved 파라미터 추가), 옵션시트 isSoldOutColorSize(타인예약 차감). **진짜 재고 차감/복구는 기존 제출 RPC·취소복구 RPC 단일 소유 완전 무변경** — 예약 API 죽어도 주문/오버셀 방어(RPC) 정상. 검수 tsc 0에러+**시뮬 8/8 PASS**(타인예약 품절/본인제외/만료무시/옵션분리/없음키/총재고합산/임박3-2=1/멀티세션합산)+돈 로직 diff 0. ⚠️**Supabase에 cart_reservations.sql 실행 필요**(미실행 시 예약 API만 500, 화면·주문 전부 정상=기존과 동일) + ⚠️맥 build+push. ※한계(후속): 옵션시트 수량 스테퍼 최대값은 예약 미반영(제출 RPC가 최종 방어), 어드민 상품관리에 "담김 N" 표시 미구현, 신규 접속자는 예약 반영까지 최대 45초.
- 2026-07-06 **고객 주문페이지 UI/UX 개선 배치 8건**(표시/UI 전용·주문 제출/포인트/배송비/Bankda 로직 무접점, Cowork 세션. 근거: 고객주문페이지_UX개선방안_20260706.md — 실화면 캡처+업계 검색 기반): ①**[신규] 상품 딥링크** — `/order?p=상품ID` 접속 시 그 상품 옵션시트 자동 오픈(담기 버튼과 동일 경로 selectQuickGroupBuyProduct, 1회 실행 가드 deepLinkHandledRef, 품절이면 안내만, hasSavedInfo+목록 로드 후 실행) + **관리자 상품관리 전체상품 탭에 [🔗 링크] 버튼**(주문링크 클립보드 복사, showAdminToast) → 방송 채팅 고정메시지에 붙여 채팅→주문서 연결 압축. ②**사이드 레일 아이콘 교체** — 3D 이모지(🔴🔔📢📦👤💬🔄)→딥로즈 단색 라인 SVG(railIconSvg 헬퍼, 핸들러/기능 무변경, 알림ON은 배경 딥로즈+흰 아이콘). ③**쇼핑몰 모드 빈 박스 축소** — 방송 OFF일 때 방송자리 박스 aspectRatio 9/16 제거→콘텐츠 높이(padding 26px)로, 첫 화면에 상품 바로 보임(방송 ON 영상 영역 무변경). ④**상품카드** — 가격 15→17px. ※"방송 접수 없이 지금 바로 구매 가능" 서브텍스트는 제거했다가 **사장님 지침으로 원상복구**(배지만으론 신규 고객이 뜻을 모름 — 이후 이 문구 임의 제거 금지). ⑤**옵션시트** — 옵션 없는 상품의 색상/사이즈 "없음" 죽은 칸 2개 제거(안내문구만 유지, colorMode/sizeMode none 분기 표시부만). ⑥**담김 확인 차단형 모달→비차단 토스트**(2.4초 자동소멸 useEffect, pointerEvents none, 하단바 개수 표시가 역할 대체 — 방송 중 연속 담기 안 끊김). ⑦**금액 표시** — OrderPriceSummaryBox에 "총 입금금액/총 결제금액" 합계 행 신설(finalAmount prop 표시만)+보유 포인트 단위 원→P, 제출 버튼 라벨에 최종금액(`N원 주문서 제출 →`, won(finalPaymentAmount) 표시만). ⑧**주문완료 시트(CustomerPaymentGuideBottomSheet)에 "다음 방송 놓치지 마세요" 섹션** — 방송알림 신청 버튼(신규 props liveAlertOptin/liveAlertSaving/onLiveAlertRequest→기존 saveLiveAlertOptin(true) 재사용, 알림톡 opt-in 확대)+홈화면 앱 추가 버튼(window.__ruruPwaPrompt 재사용, iOS/안드 수동 안내 폴백, standalone이면 숨김) — 업계 근거(web.dev: 구매 직후=설치·알림 동의 최적점). +PWAInstallBanner 닫기 쿨다운 7일을 안드로이드에도 적용(기존 iOS만)+beforeinstallprompt를 window.__ruruPwaPrompt로 공유. 검수 tsc 0에러+돈 로직 diff 0(grep: submit/final_amount/point_used/shipping_fee/deposit/bankda 접점 0)+5파일(172+/53-). ⚠️맥 build+push 필요(샌드박스 next build 불가). ※딥링크는 카카오 로그인 리다이렉트를 거치면 쿼리 소실 — 기존고객(localStorage 정보 보유) 기준 동작, 신규고객 확장은 후속. ※카드 최소금액 상품목록 단계 사전고지·배송지 시트 탭수 축소·신규 3스텝·검증순서 정리는 미착수(남은 작업). **+후속 커밋(7f5570f)**: ⑨방송 ON+고정상품(is_pinned)이면 고객 목록 카드가 "지금 방송에서 소개 중" 강조 카드(딥로즈 테두리+헤더, 정렬은 기존 pinned 우선 그대로)+**방송상품 45초 읽기 폴링**(탭 visible일 때만, 방송 OFF면 미동작 — 관리자가 띄운 상품 바꾸면 고객 화면 자동 반영). 배경: 유튜브 모바일 앱 채팅은 링크 클릭 불가(스팸 방지 정책, 설정으로 못 풂) → 딥링크 배포는 설명란/카톡채널/밴드용, 폰 시청자는 "사이트 열면 지금 상품이 맨 위" 동선으로 해결.
- 2026-07-05 **[로드맵 STEP8·완주] repo 백업파일 청소**(기능 영향 0, Cowork 세션): git에 커밋돼 있던 `.bak_날짜` 백업 66개 제거(전수 확인 — import 참조 0·오탐 0, 원본은 git 히스토리에 보존되어 `git log --follow`로 복구 가능). 디스크의 untracked `.backup_*` 폴더는 히스토리에 없어 영구소실 위험이라 **의도적으로 미삭제**. .gitignore에 `*.bak/*.bak_*/*.bak.*` 추가(재발 방지). ※이로써 7/5 종합분석 로드맵 전 항목 완료(STEP1 DB점검→2 RPC무해화→3 복제·재고임박→4 예외배지→5 희소성배지(옵션별)→6 인라인재고·정렬→7 반품/교환→8 청소).
- 2026-07-05 **[로드맵 STEP7] 반품/교환 최소형(기록 전용) 신설**(return_* 신규 컬럼만 write·정산/입금/재고/포인트/주문상태 계산 완전 무관, Cowork 세션): 업계 근거(반품 경험이 재구매율 좌우 — 지금까지 환불 명단을 수동 관리하던 것을 시스템화). ①**DB**: supabase/sql/orders_return_columns.sql — orders ADD COLUMN IF NOT EXISTS `return_status/return_reason/return_amount/return_updated_at`(기록 전용 명시). ②**주문상세 드로어**: "반품/교환 기록" 섹션 — 상태 select(기록없음=지우기/반품접수/교환접수/반품완료/교환완료)+환불금액(기록용)+사유 textarea, 저장=`orders.update(return_* 4컬럼만).in(rowIds)`(handleSaveCustomerFields와 동일 패턴), "기록 전용 — 정산·입금·재고에 영향 없음" 안내문 상시 표시. ③**테이블 배지**: LiveOrderTable 닉네임 아래 `↩ 반품접수` 등 warn색 배지(returnBadge, 기존 배지줄에 최소 추가)+hover 시 사유. ④adapter/types에 returnStatus/Reason/Amount 파생 필드(첫 행 기준·읽기 전용). 검수 tsc 0에러+**시뮬 6/6 PASS**(저장 페이로드 3케이스·배지·컬럼 미실행 DB 안전)+드로어 diff에 돈 컬럼 0(grep). ⚠️맥 build+push + ⚠️**Supabase에 orders_return_columns.sql 실행 필요**(미실행 시 반품 저장만 에러, 다른 기능 무영향). ※2단계(정산 자동 차감 연동)는 돈 로직이라 별도 위험분석 후. ※로드맵 잔여: repo .bak 청소.
- 2026-07-05 **[UI기준 변경] 고객 "N개 남음" 배지 — 옵션 상품은 합산(총재고) 표시 금지, 임박 옵션만 옵션별 표시**(사장님 지침·읽기 전용, Cowork 세션): STEP5의 합산 방식이 "어느 옵션이 급한지 모름+넉넉한 옵션 구매자에게 가짜 긴박감" 문제 → app/order/page.tsx `lowStockRemainOrderProduct`는 **옵션 상품이면 null**(단일 상품 전용으로 축소), 신규 `lowStockOptionsOrderProduct`가 재고 1~5개인 임박 옵션만 [{label(색/사이즈, "없음" 제거, 빈값="기본"), stock}] 반환 → 카드 배지 `🔥 블랙/스몰 1개 · 화이트/스몰 3개 남음`(재고 적은 순 최대 2개만. ※"외 N" 축약은 사장님이 헷갈린다고 해서 제거 — ad6de04 직후 수정). 품절(0) 옵션 제외·전 옵션 여유면 미표시. 단일 상품은 기존 "N개 남음" 유지. **이후 희소성 표시는 이 기준(옵션별)이 정식.** 검수 tsc 0에러+돈 diff 0+시뮬 9/9 PASS. ⚠️맥 build+push 필요(STEP6과 같이 커밋 가능). DB변경 없음.
- 2026-07-05 **[로드맵 STEP6] 전체 상품 탭 인라인 재고 수정 + 정렬 드롭다운**(재고 쓰기 1곳 신설·시뮬 검수, Cowork 세션): 업계 근거(Shopify/Lightspeed quick edit 표준). ①**인라인 재고 수정** — 카드에 "재고 N개" 상시 표시 + **총재고(옵션 없는) 상품만** [✎ 수정]→인라인 input(포커스 전체선택·Enter 저장·Esc 취소)→`products.update({stock:n, is_soldout:n<=0})`(폼 저장·트리거와 동일 세트). **옵션 상품은 [옵션별 수정]→수정 폼 유도**(products.stock이 note 파생이라 직접 수정 시 desync — 트리거 ruru_products_inventory_variants_sync_trg가 product_note 변경 시에만 도는 것 SQL로 확인 후 설계). 재고관리 OFF/레거시는 인라인 미노출+saveInlineStock 이중 가드. ②**정렬 select** — 기본순(고정 우선·기존)/최신 등록순(created_at desc)/재고 적은순(재고관리 상품 asc·미관리 뒤로). 표시 순서만. 변경 1파일(115+). 검수 tsc 0에러+**시뮬 9/9 PASS**(옵션·OFF·레거시 차단/0클램프·007→7/is_soldout 연동/정렬 2종). ⚠️맥 build+push 필요. DB변경 없음. ※로드맵 잔여: 반품/교환 최소형→repo .bak 청소.
- 2026-07-05 **[로드맵 STEP5] 고객 상품카드 "N개 남음" 희소성 배지 신설**(읽기 전용·주문/재고/돈 로직 무변경, Cowork 세션): 업계 근거(CommentSold 오버레이 희소성 노출로 구매 유도). app/order/page.tsx 신규 헬퍼 `lowStockRemainOrderProduct`(isSoldOutOrderProduct와 동일 데이터 기준 — `stock_management_enabled===true`일 때만, 옵션상품=variants stock 합(음수 0클램프)/단일상품=products.stock, **남은 1~5개일 때만** 숫자 반환) → 상품카드 배지줄에 `🔥 N개 남음`(HOT 배지와 동일 팔레트 #C0392B/#FBEAE7, sold면 미표시=SOLD OUT 오버레이가 담당). 재고관리 OFF/레거시(note 없음)/0개/6개↑는 미표시. 방송위젯은 기존 stockLabel "남은 N"이 이미 있어 무변경. 변경 1파일 17줄 추가(제출/배송비/포인트 diff 0 — grep 검수). 검수 tsc 0에러+**시뮬 8/8 PASS**(옵션합·경계5/6·품절·관리OFF·레거시·음수·비숫자). ⚠️맥 build+push 필요. DB변경 없음. ※로드맵 잔여: 인라인 재고·정렬→반품/교환 최소형→repo .bak 청소.
- 2026-07-05 **[로드맵 STEP4] 사이드바 예외 배지 신설**(읽기 전용 카운트·돈 로직 무변경, Cowork 세션): 업계 근거(예외 중심 알림 — 처리 필요한 것만 눈에 띄게). AdminLiveDashboard에서 로드된 orders 기준(필터 무관) `excludeFromSettlement!==true`만 집계 — needMatch=`paymentStatus==="manual_match_needed"` / cardUnpaid=`카드결제&&card_unpaid`(취소는 paymentStatus="canceled"라 자연 제외) → AdminLiveSidebar 신규 prop `exceptionBadges`로 전달, "주문·입금" 메뉴 우측에 빨간 pill 배지 [매칭 N][카드 N] 세로 스택(0건이면 숨김). 방송 중 다른 팝업 열어놔도 처리거리 발생 즉시 인지. 변경 2파일(Dashboard 9+/Sidebar 13+), 상태변경/매칭/돈 로직 0. 검수 tsc 0에러+시뮬 3/3(테스트제외·취소제외·0건숨김). ⚠️맥 build+push 필요. DB변경 없음. ※로드맵 잔여: 위젯 희소성→인라인 재고·정렬→반품/교환 최소형→repo .bak 청소.
- 2026-07-05 **[종합분석 로드맵 STEP1~3] DB점검 15/15 통과 + 폐기 RPC 파일 무해화 + 상품 복제·재고임박 신설**(Cowork 세션): ①**STEP1 DB 점검** — 읽기전용 점검 SQL 15항목(컬럼 4·RPC 6·트리거 2·제약 1·테이블 1·주문제출 재고차감 버전 확인) 사장님 Supabase 실행 결과 **전부 ✅적용됨**(재고복구·포인트반영·자동적립 구멍 없음 확정). ②**STEP2 RPC 파일 무해화** — `customer_order_submit_with_points_rpc.sql`(재고차감 없는 구버전, 경고 헤더만 있고 실행 가능한 create function 본문이 살아있었음)의 **본문 408줄 완전 제거** → 폐기 안내문만 남김(재실행해도 무해, 원본은 git 히스토리). 단일 기준=inventory_auto_deduct_rpc.sql 명시. DB 무변경. ③**STEP3 상품관리 보완**(AdminLiveProductManagePopup 1파일, 업계 Shopify/Lightspeed 표준 근거): 전체 상품 탭에 **[복제] 버튼**(기존 ruru-edit-quick-product 이벤트 재사용, id/uuid/created_at 제거+이름 "복사" 접미+고정 미복제 → 폼이 신규모드+프리필로 열림, 저장 전 아무것도 생성 안 됨) + **재고임박 배지**(product_note 있고 재고관리 중일 때만 products.stock 기준, ≤3 "🔥 재고 N"·0 "⛔ 품절", 관리OFF/레거시/파싱실패는 미표시) + **🔥 재고임박 필터 칩**(danger 색, 배지와 동일 조건). lowStockOf 읽기 전용·재고 쓰기 0. 검수 tsc 0에러+**시뮬 12/12 PASS**(복제 id제거/이름/고정미복제/원본불변·lowStock 6케이스·필터). ⚠️맥 build+push 필요. DB변경 없음. ※로드맵 잔여: 사이드바 예외배지→위젯 희소성→인라인 재고·정렬→반품/교환 최소형→repo .bak 청소(승인 후).
- 2026-07-05 **모바일 재고 입력 "0 안 지워짐/40 되는" 버그 수정 + 이벤트 팝업 모바일 잘림 수정**(UI 입력/스타일만·돈/재고 저장 로직 무변경, Cowork 세션): 사장님 — 모바일 상품등록 재고칸이 0 디폴트인데 4 치면 40 되고 0을 따로 지워야 함. 원인=QuickProductFastForm 옵션별 재고(1168) `value={row.stock}`(숫자)+onChange `Math.max(0,Number(v)||0)`라 빈칸 불가·커서가 0 옆에 붙음. 수정: 재고 3칸(옵션별/총재고/1인구매제한)에 **onFocus 전체선택**(requestAnimationFrame+select, 탭하면 기존값 선택→바로 치면 덮어씀·모바일/PC 동일)+inputMode="numeric". updateVariantStock/저장 로직 무변경. +모바일 최적화 전수점검: `type="number"` 전 컴포넌트 grep(설정 적립률·재고편집기는 문자열 state라 정상), 고정폭 팝업 grep → **이벤트(룰렛) 팝업만 width:680px에 maxWidth 없어 폰에서 옆 잘림** 확정 → `maxWidth:"100%"+overflowX:auto` 1줄 추가(다른 팝업은 전부 maxWidth:100% 기적용 확인). 검수 tsc 0에러. ⚠️맥 build+push 필요. DB변경 없음.
- 2026-07-05 **방송 상품 "선택 복사 → 새 방송" 기능 신설**(UI·broadcast_products 연결만·돈/재고/입금 무관, Cowork 세션 작업): 사장님 — 지난 방송에 담았던 상품들을 체크 선택해서 오늘 새 방송 만들 때 그대로 복사. **AdminLiveProductManagePopup.tsx 1파일만**(80+/8-): 방송 상품 탭 진열 상품 헤더에 [☑ 선택 복사] 토글(방송선택+상품1개↑일 때만 노출) → 체크 모드(행 클릭/체크박스 토글·전체선택 체크박스·드래그 순서변경 잠금·선택행 rose 하이라이트) → [선택 N개 → 새 방송으로 복사] → 기존 새 방송 모달 재사용(복사 안내 배지 "선택한 상품 N개가 그대로 담깁니다") → `createDraftBroadcast`(기존 그대로, OFF 껍데기) 후 `newBcCopyIds`를 broadcast_products에 insert(sort_order 0..n-1·is_visible true, 상품 데이터 복사 아니라 진열 연결만) → 새 방송 자동선택+토스트 "방송이 생성됐고 상품 N개를 담았어요". 복사 insert 실패 시 방송은 생성·경고 토스트(수동 담기 가능). 상태 3개(bcCopyMode/bcCopySel/newBcCopyIds), 방송 변경 시 선택 초기화, [+ 새 방송] 일반 버튼은 복사목록 클리어, 수정/빼기 버튼 stopPropagation(행 토글 충돌 방지). **돈/재고/입금/정산/주문 RPC 전부 무변경**(confirmBcPick과 동일 insert 패턴). **+같은 커밋: 상품 담기 피커 "지난 방송에서 불러오기" 필터** — 사장님: 기존 담기 피커는 전체 창고에서만 골라 특정 방송 것만 불러올 수 없었음. "방송에 상품 담기" 피커 상단에 범위 select([📦 전체 창고]기본/[📺 방송제목(날짜) 방송에서 불러오기], 현재 선택방송 제외) 추가 → 선택 시 그 방송 broadcast_products의 product_id를 읽어(읽기 전용) 목록 필터 + [☑ 표시된 N개 모두 선택(담긴 것 제외)] 버튼 + 빈목록 문구 분기. 피커 열 때 필터 초기화. `confirmBcPick`(insert)·기존 검색·이미담김 비활성 로직 무변경 → 이미 만들어둔/켜진 방송에도 지난 방송 상품 일괄 담기 가능(선택 복사는 새 방송용, 이 필터는 기존 방송용으로 상호보완). 검수 tsc 0에러+CC 빌드 통과(BANKDA 가드 2개+Compiled OK+42페이지, 피커 필터는 tsc만—빌드 재실행 필요). DB변경 없음. ※전체 상품 탭 방송별/등록일별 필터는 방송 상품 탭과 기능 중복이라 사장님 합의로 보류.
- 2026-07-04 **합배송 B: 관리자 수동 시간설정 활성 시 방송 무관 시간범위 우선**(돈/배송비 계산 변경·최소수정·시뮬 검수) + 설정 UI 단순화·안내정확화: 사장님 — 관리자 합배송 시간설정을 켜두면 방송 ON이어도 그 시간범위로 합배송(쇼핑몰모드 껐다켜도). 기존엔 방송 ON이면 무조건 broadcast_id 기준(수동설정 무시)이었음. **app/order/page.tsx 3곳**(양쪽 경로 일관): ①`resolveCurrentCombineShippingSettings`(2066) 맨 앞에 관리자-우선 분기 — `isCombineShippingActiveNow(sourceSettings)` true면 방송 window 분기보다 먼저 관리자 `{enabled:true,start,end}` 반환(그 아래 방송ON→broadcast·else→same-day 기존 그대로). ②조회 쿼리(2181) `adminWindowActive=isCombineShippingActiveNow(loadedSettings)` 추가 → `combineByBroadcastId = !adminWindowActive && (기존조건)` → 관리자 window 유효 시 `.gte/.lte(created_at, settings.start/end)` 경로(settings는 ①로 관리자 window라 조회·판정 일관). ③마킹 경로(3384)는 같은 `checkAlreadyPaidShippingGroups`+`resolveCurrentCombineShippingSettings` 사용이라 ①②가 자동 커버(추가변경 없음). **CombineShippingSettingsTab.tsx**: 안내문구 "아래 시간범위 안에 같은 고객이 주문하면 배송비 0원(합배송)…※방송 껐다켜도 이 범위 안이면 합배송"(번호→고객) + 앞선 단순화(오늘방송/지우기 버튼 삭제·마운트 시 만료/빈값이면 오늘18:00~내일05:00 프리필·상태뱃지 토글 2개[🟢켜짐/⚪꺼짐]로 축소)도 이 커밋에 포함. **시뮬 4/4 PASS**(S1 방송ON+관리자active→관리자window·byBc=false / S2 방송ON+관리자inactive→broadcast·byBc=true / S3 방송OFF+관리자active→관리자·false / S4 방송OFF+관리자inactive→same-day·false). 검수 BANKDA 가드 2개+Compiled OK. 배포 d7b784d. DB변경 없음(settings 키 그대로). ※방송 ON 중 관리자설정 inactive면 기존 broadcast_id 기준 유지(회귀0).
- 2026-07-03 **합배송 설정 저장 가드 강화(잘못된 시간범위 배송비 사고 예방)**(UI/검증만·배송비 계산 무관): `CombineShippingSettingsTab.tsx` handleSave 검증 추가(기존 시작≥종료 유지)+UI 보완. ①사용ON인데 **종료가 이미 과거**(만료 범위)면 저장 막고 토스트("종료 시간이 이미 지났습니다…") — 잘못 저장 시 사실상 오늘기준 폴백 사고 예방. ②사용ON인데 **(종료−시작)>7일**이면 `window.confirm("약 N일…같은 번호 주문 전부 배송비 0원…계속?")` 취소면 저장 안 함. ③빈칸 차단(기존 유지). ④빠른설정 버튼 [오늘 방송(18:00~내일05:00)](오늘18시/내일5시 startLocal/endLocal 세팅·저장버튼 눌러야 반영)/[지우기](두 칸 비움). ⑤현재상태 3단계(저장 확정값 시각비교): 🟢사용중(isCombineShippingActiveNow)/🟡예정(사용ON+시작 전)/⚪꺼짐·만료. **app/order·배송비 계산·settings 키이름 0 변경**. 검수 app/order diff 0 / 계산·돈(shipping_fee·deposit·bankda·정산·rpc) diff 0 / 파일 1개(70+/8-) / BANKDA 가드 2개+Compiled OK. 배포 bd72072. DB변경 없음.
- 2026-07-03 **합배송 수동 시간설정 UI 신설(작동하는 저장·계산 로직 무변경)**(설정 저장만·돈/배송 계산 무관): 사장님 — 합배송 시간범위를 관리자에서 수동으로 켜고 설정. 죽은 admin-v2/combine/page.tsx 재사용 안 하고 **신규 `components/admin-live/CombineShippingSettingsTab.tsx`**: 마운트 시 settings 3키(`combine_shipping_enabled/_start_at/_end_at`) 조회→`parseCombineShippingSettings`, UI=[합배송 시간 사용]토글+[시작]/[종료] datetime-local+[저장]+현재상태 뱃지(`isCombineShippingActiveNow` 저장확정값 기준 🟢사용중/⚪꺼짐)+안내문("방송 OFF일 때 이 시간범위 안 같은 전화번호 주문 배송비 0원"). 저장=기존 AdminLiveSettingsPanel과 **동일 방식** `supabase.from("settings").upsert([3키], {onConflict:"key"})`(start/end는 `fromDateTimeLocalValue`로 ISO). 검증=사용ON인데 시작≥종료/빈칸이면 저장 막고 토스트. AdminLiveSettingsPanel: SettingsTab에 "combine" 추가+payment 뒤 탭(🚚 합배송)+렌더 분기+import(GLOBAL_SAVE_TABS 미포함=자체 저장버튼만, 하단 공통저장바 중복 없음). **app/order/page.tsx(배송비 계산) 0줄 변경**—이미 이 설정 읽고 있어 UI 저장만으로 연동. 검수: app/order diff 0 / 계산·돈(shipping_fee·deposit·bankda·정산·rpc) diff 0 / BANKDA 가드 2개+Compiled OK. 배포 bd6b345. DB변경 없음(settings 키 3개 사용).
- 2026-06-27 **[근본원인]합배송이 자정/날짜 넘기면 배송비 재부과되던 버그 수정(방송 기준으로 변경)**(돈/배송 정합·입금/정산 RPC 무관): 사장님 — 방송 며칠 켜뒀는데 수안정님 등 배송비가 또 부과됨. 추정 금지로 전수 검수(읽기전용 SQL): 합배 기능(주소기준)은 2026-05-27 17:11(844b48d)에 추가, 그 후 **자정 넘김/여러날 방송**에서 배송비 2번 부과 확정 — 725ee139방송(05-27 23시→05-28 0시) 6명+수안정(1b48fbe8, 06-26→06-27) = **28,000원/7명 과다청구**(05-27 이전 건은 합배기능 전이라 별개, 출고완료 사이건·40000건 제외). 원인=`checkAlreadyPaidShippingGroups`가 합배 기존주문을 **시간 window(사실상 한국시간 '그날', getKoreaTodayCombineWindow)** 로 조회 → 방송기준 분기(bceff0a)가 제출 때 안 먹고 날짜폴백으로 떨어짐. 수정: 방송 ON이면 그 조회를 **`broadcast_id` 직접 매칭**(같은 방송+같은 전화+같은 주소)으로 변경, 방송 OFF(쇼핑몰)·broadcast 미로드면 기존 window 폴백. 날짜·타임존·started_at 파싱에 안 흔들림. **입금/정산/주문/재고 RPC 전부 무변경**(배송비 계산 입력만). 검수 tsc 0에러+BANKDA 가드 2개+**합배 시뮬 7/7**(같은방송 크로스데이=0 / 다른주소=부과 / 다른방송=부과 / 첫주문=부과 / 취소제외 / 방송OFF 기존동작 유지). ⚠️미배포(맥 build+push). DB변경 없음. ※이미 과다청구 28,000원/7명은 배송비 차감·환불 명단 별도(사장님 처리), 코드는 향후 재발 방지.
- 2026-06-27 **[근본원인]검색 후 담기 누르면 초기화면 튀고 안 담기던 버그 해결**(UI·돈/주문 무관): 사장님 — 검색해서 1개만 나올 때 담기 누르면 메인으로 튀고 안 담김(2개↑면 정상). 추정 금지로 추적: app/order/page.tsx 918~935줄 **전역 pointerdown 리스너**가 `data-ruru-product-search-area`(직접입력 시트=4934줄에만 있음) 밖을 탭하면 `setProductSearchText("")`로 **메인 검색어까지 비움**. 담기 버튼이 그 보호영역 밖이라, 담기 누르는 순간 pointerdown→검색 지워짐→목록이 전체(38개)로 reflow→pointerup 때 담기버튼이 이동해 click이 빗나감="초기화면 튀고 안 담김"(1개일 때 reflow 폭이 커 더 잘 빗나감, 2개↑는 우연히 맞음). 수정: 그 핸들러에서 `setProductSearchText("")` 한 줄 제거(원래 목적인 직접입력 추천 드롭다운 닫기=`setProductSearchOpenIndex(null)`만 유지). 직접입력 시트는 closeDirectInputSheet(3944)·selectBroadcastProduct(2826)에서 따로 검색어 초기화하므로 영향 없음. ※직전 7162a81(담기 후 검색유지)은 보조 수정이었고, 진짜 원인은 이 전역핸들러였음. 검수 tsc 0에러+BANKDA 가드 2개+1파일(4+/1-). ⚠️미배포(맥 build+push). DB변경 없음.
- 2026-06-27 **고객 상품목록 일부만 보이던 버그 수정(무한스크롤→전부표시)**(UI·표시만·돈/주문 무관): 사장님 — 전체 필터인데 상품이 다 안 나옴(38개 중 ~10개만). 추정 금지로 읽기전용 SQL 전수 확인: 현재 ON 방송에 **38개 담김·전부 status 판매중·bp_노출 true** → 38개 다 보여야 정상인데 10개만 보임. 원인 확정=app/order/page.tsx 상품그리드가 `filtered.slice(0, visibleProductCount=10)`로 **처음 10개만** 자르고 나머지는 IntersectionObserver 무한스크롤로 로드하는데 그게 일부 기기/레이아웃(하단 고정 담기바 등)에서 안 먹어 10개에서 멈춤. 수정: `visibleItems = filtered`(전부 렌더, 방송담긴/카탈로그 ≤80이라 가벼움) + 무한스크롤 센티넬 div 제거. 카테고리/검색/품절/돈/주문 로직 무변경. ※products엔 is_visible 컬럼 없음(노출은 status로 판정) 확인. 검수 tsc 0에러+BANKDA 가드 2개+1파일(3+/4-). ⚠️미배포(맥 build+push). DB변경 없음.
- 2026-06-27 **물건챙기기 대폭 보완: 상품별(배치피킹) 뷰 + 같은고객 묶음 배지 + 안 챙긴 것만 + 진행 바**(UI·picked_at만·돈/재고 무관): 사장님 — 인터넷 검색해서 제대로 보완 요청. 검색 근거(warehouse picking: 배치피킹이 20건↑일 때 이동·시간↓·오류↓ / order consolidation: 같은 고객 합치면 중복배송·누락↓ / 디지털 픽리스트: 수량·진행 명확)로 4가지 추가. LiveOrderPickingModal: ①**상품별 합계(배치피킹) 뷰** — 툴바 '주문별↔상품별' 토글, batchRows로 옵션까지 같은 상품을 전 주문에서 합산("알로반팔 흰색/스몰 ×4 (1/4)"), 한 줄 누르면 그 상품 ids 전부 일괄 챙김(toggleIds→writePicked, picked_at만), ㄱㄴㄷ 정렬·검색·안챙긴것만 연동. ②**같은고객 배지** — 패널 phone(숫자만) 카운트>1이면 "📦 같은고객 N건"(합배송 안내, 누락방지). ③`unpickedOnly` 토글(다 챙긴 주문 숨김+남은 상품줄만). ④진행 바(emerald %)+"N개 남음/다 챙김 🎉". 진행률은 scopedPanels 기준=항상 전체 정확. 정렬버튼은 주문별일 때만 노출. 검수 tsc 0에러+BANKDA 가드 2개+**배치집계/같은고객 시뮬 8/8**(3주문 합산 ×4·부분/전체·phone카운트·일괄토글)+1파일만(105+/8-)+orders update는 picked_at뿐. ⚠️미배포(맥 build+push). DB변경 없음.
- 2026-06-26 **상품수정 후 총금액 포인트 미반영 수정 + 포인트 표시 라벨**(돈 정합 수정): 사장님 — 상품수정 됐는데 총금액이 포인트 안 뺀 금액(44,000)으로 떠 헷갈림 + "-2,705"가 뭔지 모름. 확인: 표 상품금액 밑 "-N원"=포인트 사용액(order.pointUsedAmount). **진짜 버그 발견**: admin_update_inventory_linked_order_item이 저장 시 `final_amount = v_new_total`(포인트 미차감)로 써서, 원래 주문제출(final_amount=총액-포인트)과 어긋남 → 총금액·입금매칭 금액(expectedAmount=final_amount)이 포인트만큼 부풀려짐. 수정 2건: ①RPC final_amount=`greatest(0, v_new_total - point_used_amount)`로(제출과 동일·0클램프, point_used_amount 자체는 무변경) ②LiveOrderTable 상품금액 밑 표시를 "포인트 -N원"으로 라벨. 포인트 0 주문은 영향 0(회귀없음). 검수 tsc 0에러+BANKDA 가드 2개+금액 시뮬 5/5(40000+4000-2705=41295 / 포인트0=44000 / 0클램프 / 카드vat). ⚠️**Supabase에 admin_update_inventory_linked_order_item_rpc.sql 재실행 필요**(final_amount 로직 반영) + ⚠️LiveOrderTable은 맥 build+push + ⚠️이미 수정저장된 그 주문은 한 번 더 수정→저장해야 final_amount 재계산됨(기존행 소급 안 됨).
- 2026-06-26 **주문상세 '재고연동 상품수정' 막힘 해결**(배포본 기반 최소수정·돈/재고 로직 무변경): 사장님 — 상품수정 시 "재고차감완료 주문만 가능. 현재상태: deducted" 에러. 원인 확정(읽기전용 조회): 배포된 주문제출 함수가 재고차감 상태를 **`deducted`(총칭)** 로 저장 중(분포: null1618·deducted122·restored49·skipped*73·deducted_total24·deducted_option1)인데, 수정 RPC `admin_update_inventory_linked_order_item` 게이트는 `deducted_total`/`deducted_option`만 허용 → **고객주문 대부분 상품수정 불가**였음. RPC가 repo에 없어 pg_get_functiondef로 배포본 전체 확보 후 **딱 2군데만** 수정: ①게이트 IN목록에 `deducted` 추가 ②`deducted`면 상품 옵션설정(stock_mode/옵션변형) 기준으로 복구경로(옵션/총재고) 확정(제출 차감과 동일 기준)→이후 기존 복구/재차감/금액계산/이력 그대로. 수정 1회 시 그 주문 status가 deducted_option/total로 자동치유. **재고복구·재차감·카드vat·금액·이력 로직 한 글자도 안 건드림**. 신규파일 supabase/sql/admin_update_inventory_linked_order_item_rpc.sql(repo에 없던 함수 동기화). 프론트 무변경(훅이 이미 deducted를 inventoryDeductedAt로 이 RPC에 보냄). 검수 게이트+경로결정 시뮬 9/9 PASS(옵션/총재고 분기·기존값 유지·deducted_at없음/restored/null/skipped 차단). ⚠️**Supabase에 이 SQL 실행해야 적용**(코드빌드 불필요). 실행 후 기존 122건+향후 주문 상품수정 가능. ※근본(제출 함수가 deducted 총칭 저장)은 그대로 두고 수정RPC가 흡수—제출함수 변경은 돈 RPC라 별도 검토.
- 2026-06-25 **카드결제 최소금액 기준을 '상품금액'→'상품금액+택배비'로 변경**(UI/검증만·돈 계산 무변경): 사장님 — 상품 49,000원이라 카드결제(5만 이상)가 막힘, 택배비 포함 5만 넘으면 되게. 원인=app/order/page.tsx 카드결제 게이트가 `productAmount < cardPaymentMinAmount`로 상품금액만 비교(배송비 4,000 제외). 수정: `(productAmount + shippingFee) < cardPaymentMinAmount`로 변경(shippingFee=기존 calculateShippingFeeBreakdown.totalShippingFee, 이미 화면 표시값과 동일). 안내문구 2곳 "택배비 포함 N원 이상"으로 명시. 카드 추가금액(vat)·총액·RPC·입금/정산 계산 전부 무변경(게이트 비교 대상만 확장). 검수 tsc 0에러+BANKDA 가드 2개+판정 시뮬 5/5(49000+4000=53000 가능 / 배송무료49000 불가 / 46000+4000=50000딱 가능 등). ⚠️미배포(맥 build+push). DB변경 없음. ※배송무료(이미 배송비 낸 상태)면 상품 49000<5만이라 여전히 불가=의도.
- 2026-06-25 **검수 중 발견한 재고 위험 2건 해결**(돈/포인트/취소 RPC 무변경): ①**RPC 파일 중복 위험 차단** — `customer_order_submit_with_points_rpc.sql`(재고차감 없는 버전)에 "⚠️실행 금지/폐기예정" 경고 헤더 추가(누가 재실행 시 재고차감 사라지는 사고 방지). 주문제출 RPC 단일기준=`inventory_auto_deduct_rpc.sql`로 명시. 정식 단일화는 배포본 pg_get_functiondef 확인 후. ②**주문서 자체취소 시 재고복구 신설** — 기존 `cancel_order_and_restore_points`는 포인트만 복구하고 재고는 영구소모하던 버그 확정(전수확인: 재고복구는 admin_delete_order_item에만 있었음). **신규 RPC `restore_order_inventory(p_order_ids,p_mode)`**(supabase/sql/order_inventory_restore_on_cancel.sql) — 취소=재고 +복구(restored_*표시)/복구해제=재고 재차감(0클램프=check위반방지), 멱등, 검증된 admin_delete 복구패턴 그대로(ruru_sync_product_stock_note_from_variants 재사용), inventory_ledger 기록. **기존 돈/포인트/취소 RPC는 안 건드리고 별도 함수로 분리**. useLiveOrderCancelRestore 훅이 취소/복구 상태변경 성공 후 이 RPC 호출(실패해도 주문처리 유지=비차단 경고). 검수 tsc 0에러+BANKDA 가드 2개+**재고복구 시뮬 9/9 PASS**(6→7복구/멱등/재차감/0클램프/직접입력·미차감 스킵/품절해제/취소·해제·재취소 사이클). ⚠️미배포 + ⚠️**Supabase에 order_inventory_restore_on_cancel.sql 실행 필요**(미실행 시 취소는 되나 재고복구는 경고만 뜨고 안 됨=현재와 동일). repo SQL이 배포 DB보다 불완전함 확인(ruru_sync_product_stock_note_from_variants·inventory_restore_* 컬럼이 repo에 없고 DB에만 있음→신규 SQL에 ADD COLUMN IF NOT EXISTS로 방어).
- 2026-06-25 **개인당 구매제한 기능 신설 + 재고 차감 검수**(돈/재고 RPC 무변경): 사장님 — ①현재 방송 재고 차감 검수 ②재고0 품절 ③1인당 구매제한. **검수 결과(라이브 근거)**: 주문 제출 RPC가 트랜잭션 안에서 재고차감+inventory_ledger+is_soldout 처리, 디올 7→6 정상("옵션재고 차감완료/주문 제출 자동 차감" 배지 확인), 재고부족 시 예외로 주문차단(오버셀 방지), 고객화면 재고0 SOLD OUT/품절. ⚠️**잠재위험 발견**: 같은 함수 `submit_customer_order_with_points`를 정의하는 SQL 파일 2개(`inventory_auto_deduct_rpc.sql`=재고O / `customer_order_submit_with_points_rpc.sql`=재고X). 6/3파일 재실행 시 재고차감 사라짐 → 단일기준 정리 필요. ⚠️주문서 자체취소 시 재고복구 여부 미확정(cancel RPC는 포인트만 복구). **신규 구매제한**: 상품별 N개·카톡계정(=전화번호 1:1) 기준·켜두면 방송무관 누적, OFF까지 적용. 구현 3파일(돈/재고/포인트 로직 무변경): ①QuickProductFastForm 상품수정에 "개인당 구매제한" 토글+수량(product_note JSON `purchase_limit_enabled`/`purchase_limit_qty`) ②**서버 강제** app/api/customer-orders/submit/route.ts에 `assertPurchaseLimit`(RPC 호출 전, 전화번호+product_id 누적 집계, 취소/테스트 제외, 초과 시 예외차단, 조회실패 시 주문은 진행=라이브 안전) ③고객 order page 담기 클램프에 구매제한 합산(effMax=min(재고,한도)+안내문구 분기). **라이브 RPC는 배포본을 직접 못봐서(로컬 키없음) 추정 수정 금지 → 돈/재고 RPC 안 건드리는 API층에서 차단**(재고초과는 RPC가 별도 방어). 검수 tsc 0에러 + BANKDA 가드 2개 통과 + **구매제한 시뮬 12/12 PASS**(정확히N통과/초과차단/누적/취소·테스트·직접입력 제외/OFF/여러줄합산/한도0무시) + 재고클램프 회귀0(제한OFF면 effMax=maxQty 동일). ⚠️미배포(맥에서 `npm run build` 후 push) + **DB변경 없음**(product_note 기존 JSON 사용). ※전체 12개 상품 실재고 점검용 `재고검수_읽기전용.sql`(읽기전용) 제공 — 사장님 Supabase 실행 후 결과 회신 시 판정.
- 2026-06-20 **다크 색감 보정: 입금내역 ledger 흰색 잔재 + 액센트 채도 낮춤**(색만·돈/입금/지급 로직 무변경): 사장님 스샷 — ①BANKDA 입금내역 모달 검색바·리스트가 아직 흰색 ②분홍/보라 액센트가 너무 채도 높아 눈아픔. **①흰색 원인 = 렌더 파일 누락**: 지난 0c2d5b4에서 엉뚱하게 `parts/DepositListTable`을 바꾸고 실제 렌더되는 `ledger/` 4개(`DepositLedgerFilters`검색바·`DepositLedgerTable`리스트·`DepositDetailModal`·`DepositLinkedOrderInfo`)를 빠뜨림 → 이번에 TW색클래스→토큰 162곳(흰 검색바/리스트 해결, 백드롭 bg-slate-950/35 보존). + 미션 '매출금액'토글·이벤트 탭칩·미션 지급버튼 흰 내부 등 **삼항 배경 false분기 ` : "#fff"`**(공백+콜론=배경만, `color:"#fff"` 글자는 콜론앞 공백없어 안 걸림) 19곳 → surface. **②채도(검색근거 Material/LogRocket: 다크 액센트 20~30%↓·순색 진동 회피)**: globals.css `.dark`만 — rose-deep `#e06a90`(캔디핑크)→`#ce7f99`(채도~40%↓·밝기유지=가독성), 카드결제 보라 `#7c3aed`→다크 `#9576b8`(신설 cardpay 토큰, 라이트는 그대로·LiveOrderDetailDrawer bg-violet-600→bg-cardpay), 상태색(ok/warn/danger/info-tx) 살짝 채도↓. **색은 globals.css `.dark` 한 곳에서 관리**(이후 톤조정 그 블록만). 검수 tsc 0에러 + 9파일 색값만 교체(돈/입금/지급/매칭 로직 줄 diff 0, DepositDetail모달 text-slate-950→ink·미션370 payoutUnlocked?#fff→surface도 색만 확인). 배포 4dea45f. DB변경 없음. ⚠️admin-v2 ledger도 평소 금지영역이나 입금패널 "사장님 명시 허용" 전례+색만.
- 2026-06-20 **다크 보완: 입금내역/입금매칭 흰 패널 다크화**(색만·입금/Bankda/매칭 로직 무변경): 사장님 스샷 — 다크 적용 후에도 ①입금 매칭 우측 드로어 입금 행 ②입금 내역 탭 BANKDA 모달이 흰색으로 남음. 렌더 경로 확인: ①=`LiveFloatingMatchPanel`(admin-live, 인라인 #fff 잔재 — 지난 23개 묶음에서 이 파일 인라인 hex 누락) ②=`AdminLivePaymentPanel`→`PaymentMatchPanel`(admin-v2) + 입금매칭 드로어 `ManualPaymentMatchDrawer`(admin-v2). 수정: LiveFloatingMatchPanel 인라인 hex(배경 `#fff`→surface[삼항 false분기 `rose:"#fff"`·단순 background 둘 다], 회색→ink토큰, **글자 `color:#fff` 8곳은 흰색 유지**); admin-v2 입금패널 4개(PaymentMatchPanel·ManualPaymentMatchDrawer·DepositListTable·DepositLedgerSummary)는 TW 색클래스(bg-white/slate/neutral·blue·orange·red)→시맨틱 토큰(경계 정규식, `bg-slate-950/30` 백드롭·emerald 확정버튼·rose 포커스 보존). **토큰은 라이트(구버전 /admin-v2 페이지)=흰색 그대로라 구버전 영향 0**, 다크(admin-live)만 다크. 검수 tsc 0에러 + 5파일 **삽입=삭제 60/60(순수 색값 교체)** + 입금/매칭/금액/Bankda 로직 줄 diff 0. 배포 0c2d5b4. DB변경 없음. ⚠️admin-v2는 평소 금지영역이나 입금매칭 패널은 작업기록상 "사장님 명시 허용" 전례 + 색만 변경. ※3번 주문상세·4번 회원상세는 이미 다크 정상.
- 2026-06-20 **관리자 다크모드 + 모바일/PC 반응형 전환 완료**(스타일·토큰만·돈/지급 로직 무변경): 사장님 — 어드민페이지를 Claude 앱 같은 따뜻한 뉴트럴 그레이 다크 + 모바일에서도 자동으로 보기 좋게. **정공법(시맨틱 토큰)** 채택(덧칠 오버레이 X): globals.css에 `@custom-variant dark` + 의미 토큰(surface/surface-2/3·ink/ink-soft/ink-mute·line·상태 ok/warn/danger/info·rose) 라이트/다크 2벌, 관리자 루트 div에만 `.dark` 토글(state+localStorage `ruru_admin_theme`, 다른 페이지 영향 0). **레이아웃**: 사이드바=모바일 오프캔버스 드로어(햄버거+백드롭)/PC 고정, 2열 그리드(좌 통계+탭+테이블 / 우 방송패널 column), 주문상세·입금매칭=우측 슬라이드 드로어(fixed inset-y-0 right-0), 테이블 가로스크롤. **방송패널 column**: 영상 9:16 크게(h-480)+👁동시시청자·👍좋아요(YouTube videos.list liveStreamingDetails/statistics, 봇토큰, 25초폴링·값 바뀔때만 setState=깜박임 0), 채팅 크게, 상품=작은 표시줄. **다크 전환 범위**: Dashboard·Sidebar·LiveOrderTable·LiveBroadcastPanels + 약 23개 팝업(설정/정산/카드결제/물건챙기기/포인트/고객·이슈/유튜브알림/입금매칭 등) 인라인hex·TW클래스 토큰화 + 마지막 3개(이벤트·룰렛/인형뽑기·상품관리·미션 게이지)는 `.ruru-event-sian/.ruru-product-sian` CSS변수 다크 오버라이드 + 인라인 hex 토큰화(`#fff`는 **배경만** surface로, `color:#fff` 버튼글자는 흰색 유지). 신규: lib/youtube.ts `fetchYoutubeLiveStats`, app/api/youtube/live-stats route. 검수 tsc 0에러 + 돈/지급 로직(grant·payout·spin_event·RPC·final_amount) diff 0 + 빌드 BANKDA 가드 통과(정적 38p). 배포 d7ac70b. DB변경 없음. ※톤 미세조정은 globals.css `.dark` 블록 한 곳만 수정.
- 2026-06-19 룰렛·미션 **'결제완료' 판정을 앱 정식기준(PAID_STATUS_VALUES)으로 통일**(돈=지급대상/참가자 영향 → 시뮬 검수): 사장님 — 룰렛 입금완료 47 vs 주문서 결제완료 62 안 맞음. 시뮬·코드 대조로 **진짜 불일치 발견**: 룰렛(route ROULETTE_PAID_STATUSES)·미션(lib/mission PAID_STATUSES)이 `자동입금확인·수동입금확인·카드결제완료` **3개만** 결제완료로 봤는데, 앱 정식 기준은 10개(`입금확인·자동입금확인·수동입금확인·카드결제완료·결제완료·출고대기·출고완료·킵·픽업·픽업예정`). → **출고완료·입금확인·결제완료 등인 결제완료 구매자가 룰렛 참가자·미션 카운트/지급대상에서 빠지던** 버그(특히 출고 시작 후 미션 지급에서 누락=돈 영향). 수정: 양쪽 다 `new Set(PAID_STATUS_VALUES)`로 교체(lib/admin-v2/statusDisplay 상수 import=단일출처·드리프트 방지, admin-v2는 읽기 import만). **47 vs 62 차이 분해**: ①상태셋 불일치(이번 수정으로 0) ②같은사람 중복주문→1명(룰렛 1인1표) ③당일 중복당첨 금지 제외 — ②③은 의도된 정상. 검수 tsc 0에러 + **시뮬**(정렬 후 상태셋 불일치 0, 남는 차이=중복+당첨제외) + 변경=미션lib·룰렛route 2파일(grant/금액/입금/정산 무변경, 대상 집합만 확대). ⚠️build 미실행→맥 build+push. DB변경 없음. ⚠️미션 지급대상이 넓어짐(이전 누락 구매자 포함)=의도된 보정. 이미 받은 46명은 ledger dedup으로 재지급 안 됨.
- 2026-06-19 룰렛/인형뽑기 **참가자 불러오기 버벅임 개선**(로딩 타이밍·추첨/지급 로직 무변경): 사장님 — 참가자 리스트 불러오는 게 자꾸 느리고 버벅임. 원인=loadParticipants가 참가자 GET 후 `await ensureRoulettePreviewEvent`를 호출 → 그 안에서 미리보기 이벤트 생성 POST(158명 명단 통째 전송) + loadEventsAndWinners(이벤트·당첨자 GET 2번) → **클릭 1번에 서버 왕복 4번이 순차**로 끝날 때까지 로딩(버튼 멈춤) 유지. 수정: 참가자 setParticipants 직후 **setLoading(false)로 목록·인원수 즉시 표시**하고, ensureRoulettePreviewEvent는 **await→void(백그라운드)**로 — 휠은 잠시 뒤 채워짐. create_event/spin_event/추첨/포인트 지급 로직 전부 무변경(로딩 타이밍·블로킹만 변경). 검수 tsc 0에러+변경=룰렛 패널 1파일(지급 API/입금/정산 무변경). ⚠️build 미실행→맥 build+push. DB변경 없음. ※참가자는 여전히 활성방송 기준 스냅샷(주문서 필터 무관)·버튼 다시 눌러야 갱신.
- 2026-06-19 미션 지급명단 **닉네임 번호로만 나오던 것 수정**(전화번호로 주문 닉네임 직접 조회·읽기전용·돈 무관): 사장님 — 명단이 전부 "당첨 …3499" 번호 뒤4자리로 나옴. 원인=오늘 46명은 닉네임저장 배포 전 지급이라 ledger에 닉 없음 + 매핑용 nameMap(현재 이벤트 buyers)이 이벤트구간 이동으로 그 사람들 미포함 → 전원 번호 fallback. 수정: payout_history에서 nameMap에 없는 전화번호를 모아 `orders.in(customer_phone, phones)`로 youtube_nickname/customer_name 직접 조회(최대 500개, 최신순 first) → nameMap 보강. 닉 우선순위=ledger→이방송buyers→**주문조회**→번호뒤4자리. **돈/grant/금액 0 변경**(읽기 조회만). 검수 tsc 0에러+변경=route 1파일(지급 API/입금/정산 무변경). ⚠️build 미실행→맥 build+push. DB변경 없음.
- 2026-06-19 미션 지급명단을 **룰렛/인형뽑기 "이벤트 목록"과 동일한 인라인 목록으로 통일**(UI·돈 무관): 사장님 — 당첨 리스트를 룰렛처럼 똑같이. 팝업·요약줄 방식 버리고, 미션 패널이 이미 `.ruru-event-sian` 안에서 렌더되는 점을 이용해 **룰렛과 똑같은 클래스(.seclabel/.row/.note/.badge .b-ok)**로 인라인 목록 재구성: "🎯 미션 지급 내역 지난기록" + 기간 칩(오늘/이번주/이번달/날짜선택, badge·rose 활성) + 새로고침 + 행=`시각(120px) · 🎯미션·당첨 <b>닉</b>·포인트 NP · [지급완료]`. 룰렛 행과 차이=삭제버튼 없음/배지 항상 지급완료(이미 지급된 기록). histAll(최근60일) 마운트·지급후 로드, 기간 칩 클라 필터(kstKey). 안 쓰게 된 팝업 모달·요약줄·loadHistory·history/histTotal/histTitle/histOpen state 제거(정리). **돈/grant/ledger 0 변경**(읽기 표시만). 검수 tsc 0에러+잔재 grep clean+변경=패널 1파일. ⚠️build 미실행→맥 build+push. DB변경 없음.
- 2026-06-19 미션 지급명단 **기간 필터(오늘/이번주/이번달/날짜) 팝업** + 일괄지급 닉네임 ledger 저장(룰렛 UX 참고·읽기전용·돈 무관): 사장님 — 지급내역을 기간별로 보고 싶음(룰렛 이벤트목록 UX 참고) + 참가자 로딩 기준 질문. ①**기간 필터**: payout_history에 `range:"recent"`(최근 60일) 추가, 팝업이 그걸 불러와 기간 칩(오늘/이번주=최근7일/이번달/날짜선택, kstKey로 KST 비교)으로 **클라 필터**(룰렛 filteredWinners와 동일 방식). 요약 줄(현재 방송)은 그대로. ②**닉네임 보존**: 과거 일괄지급은 ledger에 닉네임 미저장이라 명단에 번호뒤4자리로 보였음 → useBulkPointGrant가 `youtube_nickname:t.label` 전송(grant route가 이미 받던 필드, 금액/로직 무변경) + fetchMissionPayoutHistory가 ledger youtube_nickname/customer_name select → 닉네임 우선순위(ledger→이방송 주문매핑→번호). 이후 지급분부터 이름 저장됨(오늘분은 주문매핑으로 이미 이름 나옴). **지급 API·금액·입금·정산 0 변경**. 검수 tsc 0에러+변경=미션4파일(지급API 무변경). ⚠️build 미실행→맥 build+push. DB변경 없음. ※[질문답] 룰렛/인형뽑기 참가자=`activeBroadcastId`(현재 활성방송) 기준이지 실시간주문서 필터(기간/방송/상태)와 무관. 또 버튼 누른 순간 스냅샷이라 새 주문 자동반영 안 됨 → 👥버튼 다시 눌러야 갱신(자동새로고침 필요시 별도).
- 2026-06-19 미션 **지급내역 사라짐 수정(방송 전체 기준) + 중복지급 허용 토글 추가**(돈=지급대상 → 시뮬 검수): 사장님 — 새 이벤트 켜니 지급 내역 명단이 사라짐(직전 이벤트시작 기준 변경 부작용) + 한 방송에서 중복지급 가능 토글 원함. ①**지급 내역 = 방송 전체 기준**: fetchMissionBuyers가 `broadcastStartedAt`(방송 시작)도 반환 → payout_history가 이벤트구간(startedAt) 대신 broadcastStartedAt로 ledger 조회 → 새 이벤트 켜도 그 방송 지급분 전부 계속 보임(카운트/지급대상은 여전히 이벤트구간 기준=분리 유지). ②**중복 허용 토글**: 패널에 `allowDup` 체크박스(기본 OFF=같은사람1회/ON=이미받은사람도 다시), payout_preview/confirm body로 전달. route: allowDup면 remaining=buyers(제외 안 함), 끄면 기존 멱등 제외. 확인모달 문구 분기(신규/전원·⚠️중복 경고). **grant API·ledger insert·금액·useBulkPointGrant 0 변경**(대상 명단 계산만). 검수 tsc 0에러 + **시뮬 5/5 PASS**(OFF신규만·OFF전원받음409·ON전원재지급·ON아무도안받음·구매자0) + git 변경=미션 3파일(돈 파일 무변경). ⚠️build 미실행→맥 build+push. DB변경 없음. ⚠️중복허용 ON으로 지급하면 같은 사람 포인트 또 들어감(되돌리려면 회수). 기본 OFF.
- 2026-06-19 카톡 **프로필 사진 안 보임 = OAuth scope에 profile_image 누락** 수정(로그인 scope·돈 무관): 사장님 — 회원상세 카톡 프로필 사진+클릭확대 기능 꺼진 듯. 진단(SQL): customers 743명 중 사진 32명/없음 711명, **최근 로그인 전원 "없음"** = 특정 시점부터 수집 끊김. 코드 전수확인 결과 표시·확대·매칭·SELECT·login-sync 저장 전부 정상(우리 코드 안 꺼짐). 진짜 원인=카카오 로그인 authorize scope가 `profile_nickname,phone_number,shipping_address`로 **`profile_image` 빠짐** → 카카오가 profile_image_url 안 줘서 빈 값 저장. 사진 있는 32명은 예전 scope에 있던 시절 로그인분. 수정: app/order/page.tsx 2246 + components/home/KakaoStartPage.tsx 65 scope에 `profile_image` 추가. auth route(123)·callback·login-sync(212/255)·패널(457/431 표시+zoom)은 이미 정상이라 무변경. 검수 tsc 0에러+변경=2파일(scope만). ⚠️**배포 순서 중요**: ①카카오 개발자센터 동의항목 "프로필 사진"을 **선택 동의로 먼저 켜기**(안 켜고 scope만 배포하면 로그인 KOE 에러로 막힘=주문불가 사고) → ②build+push. ⚠️기존 711명은 소급 안 됨(각자 다음 로그인 때부터 사진 저장됨). DB변경 없음.
- 2026-06-19 룰렛/인형뽑기 **"자동지급 안 됨" 진단 + 금액 빈칸 가드**(UI 가드·돈 grant 무변경): 사장님 — 당첨자 지급대기, 자동지급 안 됨. 코드 확인 결과 **시스템 정상, 운영 실수 함정**: 자동지급 조건=`운영(live)모드 + 선물=포인트 + 금액>0`(AdminLiveEventRoulettePanel 428줄). 최근 당첨자(하얀 등)가 "포인트 1,000P" 아니라 "이벤트 당첨"으로 찍힘=`giftPointAmount` 빈칸(0)으로 돌려서 자동지급 스킵된 것. 떵구리/용신(01시대)은 배지 마킹 재시도 배포 전 거라 배지만 지급대기(돈은 지급됨, 사장님 확인). 당첨자행은 spin_event가 서버 동기 생성(event_roulette_winners insert)이라 마킹 경로 정상. 수정: `startSpin`을 래퍼로 — `mode==="live" && giftType==="point" && 금액<=0`이면 confirm으로 경고/차단(금액 안 넣고 돌려 당첨자 포인트 못 받는 사고 방지). **grantPointToWinner·customer-points API·중복가드·금액 로직 전부 무변경**(돌리기 전 가드만 추가). 검수 tsc 0에러+변경=룰렛 패널 1파일(지급 API/입금/정산 무변경). ⚠️build 미실행→맥 build+push. DB변경 없음. ※운영법: 돌리기 전 ①운영 모드 ②선물 "포인트" ③"당첨 내용(포인트)"에 금액 입력 필수. 지급대기 배지는 클릭하면 지급완료 토글.
- 2026-06-19 미션 패널 **종료 시 진행막대 숨김 + 지급내역 맨아래 요약줄→클릭 팝업**(UI 전용·돈 무관): 사장님 — 종료됐는데 "99개/목표50" 막대가 남아 헷갈림(초기화 원함) + 지급 명단은 룰렛 이벤트목록처럼 "방송제목·날짜시간" 한 줄, 클릭하면 명단 팝업, 위치는 아래로. ①진행막대를 `prog.active`일 때만 표시(종료/꺼짐이면 "미션 꺼짐 — 켜면 0부터" 안내만, 새 이벤트 켜면 0부터=직전 이벤트시작 기준 변경과 맞물림). ②상단에 펼쳐두던 지급 명단 제거 → **패널 맨아래(지급 버튼 섹션 아래)에 한 줄 요약**(🎁 방송제목 · YYYY.MM.DD(요일) HH:mm · 총 N명·총액 지급완료, `whenFull` 헬퍼) + 클릭 시 **명단 팝업**(번호·시각·닉네임·+N P·지급완료, 총N명·총액, 새로고침/닫기). histTitle·histOpen state 추가, loadHistory가 broadcastTitle도 저장. **돈/grant/ledger 0 변경**(읽기 전용 표시만). 검수 tsc 0에러+변경=패널 1파일(돈 파일 무변경). ⚠️build 미실행→맥 build+push. DB변경 없음.
- 2026-06-19 미션 카운트/지급 기준을 **"방송 시작"→"이벤트 시작(켜는 시점)"으로 변경**(돈=지급대상 영향 → 시뮬 검수): 사장님 — 미션 새로 켜는 순간부터 0으로 카운트, 종료 후 새 이벤트 만들면 그때부터 다시. 기존엔 broadcasts.started_at(방송 시작)부터라 켜자마자 97개가 차 있었음. **신규 settings 키 `mission_started_at`·`mission_ended_at`**(key/value 테이블이라 DB 스키마 변경 없음). ①mission route 저장 핸들러: active false→true(새 시작)면 started_at=now·ended_at="", true→false(종료)면 ended_at=now(시작 유지), true→true(편집)면 시각 유지. ②lib/mission.ts `readMissionTimes`+`resolveMissionWindow` 신규 — 카운트/지급/명단 구간=[이벤트시작, 종료(진행중이면 지금)], 기록 없으면 방송 시작 fallback. computeMissionProgress·fetchMissionBuyers 둘 다 이 구간 사용(fetchMissionBuyers가 반환하는 startedAt=이벤트시작이라 dedup·history도 이벤트 기준). **효과: 이벤트별 독립** — 새 이벤트는 카운트 0, 지급대상=그 이벤트 구매자, dedup도 그 이벤트 시작 이후 grant만 봄(이전 이벤트 받은 사람은 새 이벤트에서 다시 구매하면 재지급 가능=이벤트별). grant/금액/입금/정산 **0 변경**(구간 계산만). 검수 tsc 0에러 + **시뮬 9/9 PASS**(시작/편집/종료/재시작 전이·진행중/종료 구간고정/fallback·새이벤트 dedup 이전건제외) + git 변경=미션 2파일만(돈 파일 무변경). ⚠️build 미실행→맥 build+push. **DB 스키마 변경 없음**(settings 키 2개 추가뿐). ※현재 켜져있던 미션은 mission_started_at 없어 방송 시작 fallback 유지 → 종료 후 새로 켜면 그때부터 0.
- 2026-06-18 미션 패널 **"이 방송 지급 내역" 명단 추가**(룰렛 이벤트목록 스타일·읽기전용·돈 무관): 사장님 — 이벤트 끝났는데 진행막대(97/목표50)만 남아 헷갈림 + 누가 받았는지 결과 리스트가 안 남음(룰렛 "이벤트 목록"처럼 해달라). ①lib/mission.ts `fetchMissionPayoutHistory(startedAt)` 신규 — ledger(change_type='grant' && admin_memo=MISSION_PAYOUT_MEMO && created_at>=방송시작)에서 금액·시각 읽음(.range 페이지네이션). ②mission route `payout_history` 액션 — fetchMissionBuyers로 phone→닉네임 매핑(일괄지급은 ledger에 닉네임 미저장이라 주문에서 매핑, 못찾으면 …뒤4자리) + 금액·시각·총N명/총액 반환. ③패널 — `history` state + `loadHistory()`(마운트·지급후·새로고침 버튼에만 호출, 6초 폴링엔 미포함=부하↓) + 진행막대 위 헤더가 미션 OFF면 "이벤트 종료됨(위젯 숨김)"으로 바뀜 + 진행막대 아래 **"🎁 이 방송 지급 내역" 목록(시각·닉네임·+N P·지급완료 배지, 총 N명·총액)** 룰렛목록 스타일로 표시(지급기록 있을 때만). **돈/grant/금액/입금/정산 0 변경**(읽기 전용 — ledger 조회만). 검수 tsc 0에러 + git 변경=미션 3파일만(돈/grant 파일 미변경 확인). ⚠️build 미실행(샌드박스 SWC)→맥 build+push. DB변경 없음. ※오늘 이미 받은 46명은 ledger에 닉네임 없어 일부 "…뒤4자리"로 보일 수 있음(주문 매핑되면 닉네임 표시), 이후 지급분은 주문에서 매핑됨.
- 2026-06-18 미션 패널 **"이벤트 종료·지급했는데 체크박스가 미션 켜짐으로 남음" 수정**(UI 전용·돈 무관): 증상=종료+지급 끝났는데 "미션 켜짐(위젯 표시)" 체크 + "● 변경됨". 코드 확인 결과 **서버 저장값은 이미 OFF**(지급버튼 초록=payoutUnlocked=!prog.active, 종료버튼 숨김이 증거) — 실제로 위젯 꺼지고 지급 끝난 정상 상태였고, 체크박스는 **저장 안 된 화면값**(=저절로 재활성 아님). 원인=AdminLiveMissionPanel load()의 `initRef`(입력칸 첫 1회만 채움, 폴링이 편집값 덮어쓰는 것 방지)가 부작용으로 **첫 로드 때 ON이면 체크박스가 true로 굳어 서버가 OFF로 바뀌어도 폼이 스테일**. 수정: load()에 **clean-sync** — 폼이 저장값과 동일(편집 안 함)일 때만 서버 최신값을 폼에 반영(liveRef/savedSnapRef로 스테일 클로저 회피), 편집 중(dirty)이면 기존처럼 안 덮어씀(저장 전 체크 풀림 방지 유지). 저장/지급/돈 로직 무변경. 검수 tsc 0에러. ⚠️build 미실행→맥 build+push. DB변경 없음.
- 2026-06-18 미션 **한 방송에서 여러 번 지급 가능(같은 사람 중복지급 방지)**: 기존엔 `mission_paid_<방송>` settings 플래그로 **방송당 1회만**(2회차 409 차단). 업계표준(멱등성/idempotent ledger·Airbnb 중복결제방지 검색) 반영해 **진실원천을 settings 플래그→실제 지급기록(customer_point_ledger)**으로 변경. **멱등키=(방송+전화번호)**. ①lib/mission.ts: 상수 `MISSION_PAYOUT_MEMO="미션 게이지 공동목표 달성 일괄지급"` export + `fetchMissionPaidPhones(startedAt)` 신규(change_type='grant' && admin_memo=상수 && created_at>=방송시작 인 ledger에서 이미받은 전화번호 Set, .range 페이지네이션) + fetchMissionBuyers가 startedAt도 반환. ②mission route: payout_preview/confirm에서 `남은=buyers−이미받은phones` 계산, 방송당1회 409 차단 제거(남은0명일 때만 "전원 지급됨" 409). payout_reset은 호환용 no-op. ③패널: 안내문구("한번만"→"여러 번 가능·같은사람1회·이미받은사람 자동제외") + adminMemo를 상수로 단일화(드리프트 방지) + "이미 N명 지급됨(제외)" 표시. **돈 지급 로직(grant API·ledger insert·금액·useBulkPointGrant) 0 변경** — 바뀐 건 "대상 명단 계산"뿐(읽기/필터). 식별키=admin_memo(상수)인 이유=reason은 미션 title 따라 변해서 부적합. ⚠️중요=오늘 이미 받은 46명은 ledger에 있으니 **재실행해도 자동 제외(이중지급 0)** — settings 플래그 초기화 방식의 마이그레이션 위험 없음. 검수 tsc 0에러 + git 변경=미션 3파일만(돈/grant/입금/정산 파일 미변경 확인) + **멱등 시뮬 6/6 PASS**(1회차전원·2회차신규만·전원지급409·46명재실행이중0·실패자재포함·reward0가드). ⚠️build 미실행(샌드박스 SWC)→맥 build+push. DB변경 없음. ※여러 회차=이벤트 종료(미션OFF) 후 지급버튼 열림은 그대로, 새 구매자 들어오면 다시 지급 누르면 신규만 지급됨.
- 2026-06-18 룰렛 자동지급 **"지급대기" 배지 안 바뀜 수정**(돈 정상·배지만 문제): 룰렛 당첨 포인트 자동지급은 ①grant(지급) ②당첨레코드 is_reward_done=true 마킹(배지 지급완료) 2단계인데, ②의 당첨레코드 조회(grantPointToWinner, event_id로 재조회)가 **레코드 생성 직후라 비어서** 마킹이 스킵→배지가 "지급대기"로 남음(포인트는 1회 정상지급, 이중지급 아님). 수정: 그 조회를 **최대 3회·0.6s 간격 재시도**로 변경 → winnerId 잡아 mark_reward_done 확실히 실행. grant 호출·중복가드(grantedEventIdsRef·is_reward_done 게이트)·금액 로직 무변경(조회 재시도만 추가). ※수동: 당첨자 "지급대기" 배지 클릭 시 지급완료 토글(markRewardDone, winner.id 직접 사용이라 항상 됨). ⚠️배지가 지급대기면 중복지급 영구잠금이 안 걸린 상태라 재연출/새로고침 시 재지급 위험 → 이미 지급된 건 배지 눌러 잠그도록 안내함. 검수 tsc 0에러. ⚠️build 미실행→맥 build+push.
- 2026-06-18 물건챙기기 **검색 버그 수정**(닉네임으로 검색 안 됨, 표시·검색 전용·돈 무관): LiveOrderPickingModal 패널 nickname이 `recipientName(받는사람) || nickname || name` 우선이라, 검색도 그 값(보통 실명)만 비교 → 유튜브 닉네임(예 "임-d5t")으로 검색하면 안 잡히고 실명("임영순")으로만 잡힘. 수정: 패널에 `search` 필드 추가 = `[nickname, name, recipientName]` 합쳐 소문자화, visiblePanels 필터를 `p.search.includes(q) || p.nickname.includes(q) || items.text` 로 변경 → 닉네임·이름·받는사람·상품명 아무거나로 검색됨. picked_at/돈/주문 로직 무관. 검수 tsc 0에러. ⚠️build 미실행→맥 build+push.
- 2026-06-18 유튜브 라이브 채팅 **자동알림 완성**(엔드포인트 버그 해결 + 3단계 연결 + 테스트 미리보기): ①**[근본원인]발송 404 = 엔드포인트 오타** — lib/youtube postOnce가 `/youtube/v3/liveChatMessages`(잘못된 경로)로 POST → 존재안하는 주소라 **빈 본문 404 Not Found**(소유자·봇·chatId 무관 전부 실패했던 진짜 이유). 공식문서 기준 `/youtube/v3/liveChat/messages`로 수정(e3eac8a) → **봇 계정(@rur8249 루루쇼핑)으로 발송 성공 확인**. (그동안 의심한 소유자/비소유자·테스트모드·운영자권한 전부 무관했음. chatId 조회(videos/liveBroadcasts)는 주소가 맞아 정상이었음.) ②**3단계 자동게시** — app/api/customer-orders/submit/route.ts에 **`after()`(응답 전송 후 실행)**로 유튜브 채팅 게시 추가: 주문 RPC·받는사람 저장 다 끝난 뒤 곁다리로 buildOrderMessage+postLiveChatMessage. **주문 제출 속도·성공에 영향 0**(after는 응답 후 백그라운드), postLiveChatMessage는 throw 안 함+notify OFF면 내부 스킵, try/catch로 완전 격리. 주문/RPC/포인트 로직 무변경. items요약=상품명 외 N건, amount=final_amount 합. ③**테스트 발송이 입력 문구 반영** — YoutubeNotifyCard sendTest가 고정문구만 보내던 것을 **현재 입력칸 문구를 렌더(닉네임=테스트/items=샘플/amount=50,000원)해서 전송** → 이모지/문구 바꾸면 저장 안 해도 테스트에 바로 보임(실제 주문은 "저장"한 문구 사용). ④진단 정리: admin route 테스트 응답의 _dbg/연결채널·videos챗 문구 제거(getYoutubeDiag 정의는 남김·무해). 검수 tsc 0에러+after import 유효+주문 RPC 미변경. ⚠️build 미실행→맥 build+push. ⚠️자동알림은 설정에서 "알림 ON"이어야 주문 시 게시됨. lib/youtube resolveLiveChatId는 videos.list(실제 현재 chatId) 1순위로 정리됨(48a593e).
- 2026-06-18 미션 게이지 **"방송 OFF·0개" 진짜 원인 해결 = 컬럼명**(lib/mission.ts, 읽기전용·돈 무관): 임시 진단(_dbg를 overlay에 넣어 URL로 확인)으로 확정 — `_dbg.bcErr = "column broadcasts.title does not exist"`. 즉 broadcasts 테이블 방송이름 컬럼은 `title`이 아니라 **`public_title`**(liveBroadcastController가 `public_title`로 insert)인데, fetchActiveBroadcast가 없는 `title`을 select해서 **쿼리 전체가 에러→방송 0개→"방송 OFF·0개"**. 대소문자/RLS 아니었음(키는 service_role 정상). 수정: select `title`→**`public_title`**, broadcastTitle 읽기 2곳 `bc.public_title ?? bc.title`. 진단 _dbg는 overlay에서 제거(되돌림). 직전 대소문자무시 변경(3d1b553)은 그대로 둠(무해·맞는 방향). 검수 tsc 0에러+지급/PAID 로직 무변경. ⚠️build 미실행→맥 build+push. ※유튜브 채팅 여전히 보류(방송 후 로그 확인).
- 2026-06-18 미션 게이지 **"방송 OFF·0개" 버그 수정**(방송 중인데 진행률 0·OFF로 뜸, 읽기전용·돈 무관): 원인=lib/mission.ts `fetchActiveBroadcast`가 `.eq("status","ON")`(대문자 정확일치)로 방송을 찾는데, 메인 대시보드(liveBroadcastController.ts `getActiveBroadcast`)는 `status.toUpperCase()==="ON"`(대소문자 무시)라 DB값이 "on"/"On"이면 대시보드만 "방송중"이고 미션은 "방송 OFF"+집계 0. → fetchActiveBroadcast를 대시보드와 동일하게 **최근 20건 fetch 후 `status.toUpperCase()==="ON" && is_deleted!==true`로 JS 필터**로 변경. 진행률 집계(computeMissionProgress)·구매자추출(fetchMissionBuyers) 둘 다 이 함수 거쳐서 동시 해결. 지급/PAID_STATUSES/isPaidRow/중복가드 로직 무변경. 검수 tsc 0에러+지급함수 미변경. ⚠️build 미실행→맥 build+push. ※[보류] 유튜브 주문→채팅 자동멘트: 봇이 비소유자/테스트모드 등으로 liveChatMessages.insert가 빈 404("Not Found"). 소유자(루루동이) 계정 테스트사용자 추가까지 함. 미해결 — 방송 후 Vercel 로그(insert#2 fail의 chatId·wwwAuth) 보고 마무리 예정. lib/youtube.ts에 liveBroadcasts.list(소유자 우선)·상세진단 로그 이미 반영(09dd5fe·7f23627).
- 2026-06-18 알림음 **첫 주문 음성 안 나옴 → 음성 잠금 자동해제**(UI·돈 무관): 사장님 — 켜짐·볼륨100인데 주문 들어와도 "주문!" 무음. 원인=브라우저 TTS 정책(페이지 로드 후 사용자 제스처 전엔 speechSynthesis 차단). lib/adminVoice.ts `primeAdminVoice()`(제스처 안에서 1회 무음+resume) 추가, AdminLiveDashboard에 첫 pointerdown/keydown 1회 prime 리스너 → 운영자가 화면 아무 데나 처음 클릭하면 그 세션 음성 잠금 해제. realtime orders INSERT→speakAdmin 경로는 무변경. 검수 tsc 0에러. ⚠️build 미실행→맥 build+push. ※여전히 방송 시작 때 🔊 테스트 1번 눌러두면 확실. ※[참고] 유튜브 채팅 자동알림은 1·2단계(연결·테스트)만 됐고 **주문→채팅 3단계는 미연결**(submit route에 youtube 호출 없음 확인)이라 주문 시 채팅 안 올라가는 게 정상.
- 2026-06-18 알림음 컨트롤 **사이드바 이동 + 미션 위젯 가독성 강화**(둘 다 UI·돈 무관): 사장님 — 실제 방송(밝은 신발 배경) 위에서 위젯이 너무 얇고 흐려 안 보임 + 알림음 컨트롤은 사이드바가 나음. ①**알림음 컨트롤→사이드바**: AdminSoundControl을 팝오버→**인라인 블록**으로 변경(켜짐/꺼짐·볼륨 슬라이더·주문!/입금! 테스트), AdminLiveSidebar QUICK '빠른보기' 박스(방송 중 표시) 안에 마운트. 탭바(점검 옆)에서는 제거. 사이드바 미사용 import(LiveOpsStatusBox) 정리. ②**위젯 가독성**(app/event-mission/live/page.tsx): "얇게+완전반투명" vs 가독성 상충 해소 — 크기·대비 ↑. 투명도 0.34→**0.5**(달성 0.42→0.58), 내용높이 24→**32**, 패널패딩 5/14→**9/18**, 진행바 16→**22**·radius 7→10·트랙 0.24→0.3, 폰트 설명/달성 clamp13~17→**15~22**·보상 11~14→**14~18**·%오버레이 10~12→**12~15**, 이모지 17→21, 글자 그림자 강화(`0 2px 5px rgba(0,0,0,.8)` halo). 회전·문구·% 오버레이·달성 1회연출 로직 무변경(값만 ↑). ※OBS에서 소스 더 크게 두면 벡터라 또렷. 검수 tsc 0에러+돈/입금/정산 파일 미변경. ⚠️build 미실행→맥에서 build+push. DB변경 없음.
- 2026-06-18 알림음 **단독 컨트롤 + 볼륨 슬라이더 + 여성 음성**(알림 출력 전용·돈 무관): 사장님 — 기존 음소거 토글이 사이드바 LiveOpsStatusBox 안에 작게 있어 안 보임 + 볼륨 단독 + 여성 음성 원함. ①**신규 components/admin-live/AdminSoundControl.tsx** — 실시간 주문 탭바 우측(🛡️점검 옆)에 항상 보이는 `🔊 알림음` 버튼+팝오버(켜짐/꺼짐 토글, 볼륨 0~100% 슬라이더, "주문!"/"입금!" 테스트 듣기). localStorage `ruru_admin_sound_on`·`ruru_admin_voice_volume`(0~1) 사용. ②**lib/adminVoice.ts** — `speakAdmin`이 볼륨(getVolume) 반영 + **한국어 여성 음성 자동선택**(pickKoreanFemaleVoice: female/yuna/heami/sunhi/sora 등 힌트, 없으면 첫 ko 음성=대개 여성) + pitch 1.15로 여성 느낌. 비프 폴백도 볼륨 반영. 기존 사이드바 토글(LiveOpsStatusBox)은 그대로 둠(같은 localStorage 키라 동기화). 검수 tsc 0에러+돈/입금/정산 파일 미변경. ⚠️첫 재생은 사용자 클릭(테스트 버튼) 후 동작, OS에 ko 음성 있어야 또렷. ⚠️build 미실행→맥에서 build+push. DB변경 없음.
- 2026-06-18 관리자 알림음을 **한국어 음성("주문!"/"입금!")으로 교체**(알림 출력 전용·돈 무관): 사장님 — "띵동" 비프가 주문/입금 구분 안 돼 헷갈림. 신규 **lib/adminVoice.ts** `speakAdmin(text)`=`speechSynthesis`(ko-KR, rate1.05, ko 음성 자동선택) + 미지원/실패 시 비프 폴백. ①새 주문: AdminLiveDashboard realtime orders INSERT의 playNewOrderTone(720Hz 비프)→`speakAdmin("주문!")`(ruru_admin_sound_on off면 무음 유지). ②입금확인: LiveOpsStatusBox playTone("auto_paid")(980Hz)→`speakAdmin("입금!")`, 기존 playTone 함수 제거(미사용). soundOn(localStorage) on/off·toast·복사 로직 무변경. 검수 tsc 0에러+playTone 잔재0+돈/입금/정산 파일 미변경. ※TTS는 OS에 한국어 음성 있어야 또렷(맥/윈/안드 기본 보유), 첫 재생은 사운드 토글 클릭(사용자 제스처) 후 동작. ⚠️build 미실행→맥에서 build+push. DB변경 없음.
- 2026-06-18 포인트 이력 글씨 잘림 수정 + 미션 입력 쉼표 표시(표시 전용·돈 무관): ①**포인트 이력 잘림** — AdminLiveCustomerPointPanel "처리 후 9,…"·사유("주문취소 포인트…")가 `truncate`(말줄임)로 잘렸음 → `truncate` 제거+`break-words`로 전체 표시. ②**미션 패널 금액 쉼표** — 목표/1인당 포인트 입력칸을 입력 중 쉼표 자동(value=`won(Number(x))`, onChange는 숫자만 저장). 저장값은 숫자만이라 API `num()`(비숫자 제거)과 무관·안전. 검수 tsc 0에러+지급/취소복구/API/lib 미변경. ※[참고·돈] 사장님 "지급한 적 없는데 +160,000P?" → **자동복구가 맞음**: admin-live 주문서취소 시 `cancel_order_and_restore_points` RPC가 그 주문에 **썼던 포인트 자동 환원**(useLiveOrderCancelRestore.ts 85, 메모 "admin-live 주문서취소 자동 포인트 복구"). 이력 −160,000(사용)→+160,000(취소복구)=순증0. 새 지급 아님(정상). 테스트 계정(잔액 9,999,999). ⚠️build 미실행→맥에서 build+push. DB변경 없음.
- 2026-06-18 미션 패널 **"이벤트 종료" 게이트 추가**(components/admin-live/AdminLiveMissionPanel.tsx, 지급=돈 영역이지만 **지급 로직 자체는 무변경**·게이트 표시만): 사장님 — 이벤트 종료 버튼 누르면 지급 버튼 활성화 원함. 사장님 선택=**종료=지급버튼 활성화+미션 끄기(위젯 숨김)**. 구현: ①`endEvent()` — confirm 후 settings POST `{active:false,...현재 폼값}`(기존 save와 동일 경로, 새 돈 로직 0)→ setActive(false)+savedSnap 갱신+load. 미션 진행중(`prog.active`)일 때만 "🛑 이벤트 종료(위젯 끄고 지급 열기)" 빨강 버튼 노출. ②**지급 버튼 게이트** `payoutUnlocked = !!prog && !prog.active`(서버 저장값 기준=종료/미션OFF 후에만 열림, 새로고침해도 유지). 잠김 시 회색·not-allowed+"※ 이벤트 종료 누르면 열림" 안내. openPayout/doPayout/grant/payout_confirm·중복가드(mission_paid_*)·API route·lib/mission **전부 무변경**(검증: API route·lib/mission·useBulkPointGrant git 미변경). ③**미션 끄기는 지급 대상에 영향 없음 확인**: fetchMissionBuyers=방송(broadcast ON)+결제완료 기준, reward=mission_reward_amount 설정값 → active=false여도 대상·금액 유지(단 지급은 방송 ON 동안). 검수 **tsc 0에러+시뮬 15/15 PASS**(0건 위젯표시·pct·게이트 상태전이·재잠금). ※위젯: 0건이어도 미션ON+목표면 0%로 보임(liveOk=ok&&active&&goal>0, current 무관). ⚠️build 미실행(샌드박스 SWC)→맥에서 build+push. DB변경 없음.
- 2026-06-18 미션 게이지 위젯 **100% 달성 연출(1회 반짝)+달성 후 문구만**(app/event-mission/live/page.tsx, UI 전용·돈 무관): 사장님 — 무한 반짝임은 산만, 달성 "순간"만 1회 반짝 후 정적 + 달성 후엔 바 빼고 문구만. 반영: ①`@keyframes ruruCelebrate`(금빛 box-shadow+scale 1.015) 추가, 패널 animation=`celebrate ? "ruruCelebrate 1.2s ease-in-out 2"(2회만) : near ? pulse : none`. ②**달성 1회 감지**: `useRef wasDone`+`useState celebrate`, data 기준 effect(early-return 앞, deps[data])로 pct가 100 넘는 "순간"에만 celebrate=true→2.6s 후 false(미만으로 내려가면 재발동 가능). ③**달성 후 렌더**: `done`이면 회전·바 없이 `🏆 목표 달성! 구매자 전원 N P 선물!` 중앙 1줄 고정, 아니면 기존 회전(장면A/B). useRef import 추가. ⚠️위젯은 표시 전용 — 100% 돼도 **자동지급 아님**(지급=관리자 패널 버튼, 돈 로직 별개·무변경). 검수 tsc 0에러+돈로직 미등장. ⚠️build 미실행(샌드박스 SWC)→맥에서 build+push. DB변경 없음.
- 2026-06-18 미션 게이지 위젯 **회전 배너 + 바 안 % 오버레이로 확정**(app/event-mission/live/page.tsx, UI 전용·돈 무관): 사장님 — 한 줄에 정보 다 넣으면 "뭔 소린지" 모름 + 생숫자(개수/매출) 노출은 욕심·작아보임 우려. 소비자/판매자 심리(goal-gradient·게이미피케이션·선물 프레이밍) 반영해 **최종 UI 기준(이후 이 값 유지)**: ①**4초마다 글씨 회전**(phase state+setInterval 4000): 장면A `🛒 다 같이 구매할수록 바가 가득 차요!`(바 없음, 가운데) ↔ 장면B `🎁 [진행바] 100% 되면 전원 N P 선물!`(바 등장). 둘 다 `position:absolute;inset:0` opacity 전환(.55s), 컨테이너 `position:relative;height:24`라 **세로 안 잘림**(고정 작은 행+패널 overflow 안 막음→확대해도 클립 없음). ②**진행 % 는 바 안에 오버레이**(중앙, `Math.round(pct)%`, text-shadow). 개수/금액 절대수치는 **표시 안 함**(63%가 63/100인지 6300/10000인지 외부서 못 가늠→욕심·작아보임 차단). ③**가독성**: 모든 글씨 `text-shadow 0 1px 3px rgba(0,0,0,.55)`(밝은 영상 위에서도 흰글씨 보임), 폰트 clamp 상향(설명 12~16, 보상 11~14). ④달성 시(done) 회전 멈추고 장면B 고정 `🏆 목표 달성! 전원 N P 선물 🎉`. **미사용 변수 정리**(goalText/currentText/remainText/goal/current/goalType/title/remaining 제거, reward/pct/pctRounded만 사용). 투명/슬림/blur(0.34) 유지. 검수 **tsc 0에러**+돈로직 미등장+변경 이 파일 1개. ⚠️build 미실행(샌드박스 SWC)→**맥에서 build+push**. DB변경 없음. ※OBS는 소스 크기 조절로 가독성 확보(벡터라 확대해도 또렷). ※직전 "한 줄 정적 숫자" 버전은 이걸로 대체됨.
- 2026-06-18 미션 게이지 위젯 **한 줄 배너로 재구성**(app/event-mission/live/page.tsx, UI 전용·돈 무관): 사장님 — 여전히 두껍고 "글씨가 다음 줄로 내려가는 것" 싫음, 더 얇은 가로 배너(검색배너 시안 두께) 원함. **변경된 UI 기준(이후 이 값 유지)**: ①레이아웃 세로 3블록(태그라인+제목 2줄 / 진행바 / 잔여문구)을 **단일 가로 1줄**로 통합 — `🎁 + 제목·목표·현재(flex-shrink:1, overflow ellipsis) + 진행바(flex:1) + 잔여 + 전원 N P 뱃지`, 부모 `white-space:nowrap` → **절대 줄바꿈 안 함**(좁으면 제목이 …처리, 진행바가 줄어듦). ②문구 단축: goalText "오늘 누적 판매 100개 목표"→**"100개 목표"**, currentText "현재 63개"→**"63개"**, remainText "37개 남았어요!"→**"37개 남음"**. 감성 태그라인 "다 같이 채우면 구매자 전원 포인트"는 한 줄화 위해 제거(뱃지 "전원 N P"가 보상 의미 전달). ③더 슬림: 패널패딩 7/14→**5/14**, borderRadius 13→12, 진행바 높이 9→**8**·radius 6→5, 이모지 18→16, 뱃지 "구매자 전원"→"전원"·padding 4→3. 투명도/blur는 직전(0.34/blur3) 유지. 검수 tsc 0에러+돈로직 미등장+git 이 파일 1개. ⚠️build 미실행(샌드박스 SWC)→맥에서 build+push. DB변경 없음.
- 2026-06-18 미션 게이지 위젯 슬림+반투명 디자인 기준 변경(app/event-mission/live/page.tsx, UI 전용·돈 무관): 사장님 — 모바일 미리보기에서 위젯이 세로로 두껍고 너무 진함. "껌 두께처럼 얇게 + 뒤 방송영상 비치게 더 반투명" 요청. **변경된 UI 기준(이후 이 값 유지)**: ①투명도 패널 bg 0.55→**0.34**(달성 0.72→0.42) + `backdrop-filter:blur(3px)`(영상 위 가독성). ②슬림: 바깥패딩 10/12→6/10, 패널패딩 10/16→**7/14**, borderRadius 16→13, 진행바 높이 16→**9**·radius 8→6·marginTop 9→6, 이모지 22→18, 잔여문구 marginTop 5→4. ③폰트 제목 clamp 13~17→**12~15**·잔여 11~13→10~12·뱃지 11~14→11~13(padding 6/10→4/10). 테두리 2px→1~1.5px로 가볍게. ※모바일 줄바꿈(word-break:keep-all/clamp/flex-shrink)은 병행 세션에서 이미 적용됨 — 중복수정 안 함. 검수: **tsc 0에러**(이 파일), git 변경=이 파일 1개(22+/19-), 돈/입금/정산/포인트 로직 미등장 확인. ⚠️npm run build는 리눅스 샌드박스 SWC 미설치로 미실행 → **사장님 맥/Claude Code에서 build+push 필요**. DB변경 없음.
- 2026-06-18 미션 게이지 안정화(방송 직투입 대비 전수검수): 사장님 — 테스트 불가 환경(방송 직행)이라 시뮬레이션으로 완벽 검수 요청. 코드 재감사로 실버그 3건 수정: ①**[치명]1000건 초과 누락** — Supabase 요청당 1000행 한계로 `.limit(2000)`도 1000에서 잘려 큰 방송(이미 1072건)에서 집계·지급대상 일부 누락 → lib/mission.ts fetchAllOrders(.range 페이지네이션, 최대 30p=30000)로 전건 수집(computeMissionProgress·fetchMissionBuyers 둘 다). ②**더블클릭 중복지급** — doPayout에 executing 가드+버튼 disabled(paying||executing) → 가드 설정 전 2회 실행 차단. ③**num() 빈값 버그** — Number("")=0 함정으로 qty 없는 주문이 개수 0으로 잡힘 → 빈 문자면 기본값 반환(qty 없으면 1, qty 0은 0 유지). +**지급 후 "지급 명단" 결과 팝업**(성공 명단 닉네임+지급액 +1,000P / 실패 명단 닉네임+사유, 총 지급액, 실패자 수동지급 안내). 검수 tsc 0에러+BANKDA 가드+**전수 시뮬 15/15 PASS**(개수/매출 진행률·pct 상한/0분모·구매자 추출[미결제·테스트·번호없음 제외]·금액합산·최신시각·1500건 페이지네이션 누락0·qty기본값·더블클릭차단·전부실패 가드해제·일부성공 가드유지). ⚠️미배포(로컬 build+push). DB변경 없음. ※게이지=결제완료 전체 매출/개수(번호없는 주문도 매출이라 포함), 지급=번호있는 결제완료만(의도된 차이).
- 2026-06-18 미션 게이지 "변경됨·저장하세요" 표시: 사장님 지적 — "저장 안 해서 안 켜지는 건 정상 아니냐"(맞음). 직전 수정은 "입력 중 폴링이 값 덮어쓰는 것"만 막은 거고 저장규칙은 그대로. 부작용=저장 안 해도 화면엔 체크 남아 헷갈림 → AdminLiveMissionPanel에 **저장값 스냅샷(savedSnap)과 현재 폼 비교(snapOf)** 추가, 다르면 저장버튼 위 **"● 변경됨 — 저장해야 적용돼요"**+버튼 빨간테두리+"변경사항 저장하기" 라벨. 저장/초기로드 시 스냅샷 갱신. 표시 전용(돈/저장 로직 무관). tsc 0에러+가드+clean+dirty 시뮬 3/3 PASS. ⚠️미배포(로컬 build+push). DB변경 없음.
- 2026-06-18 미션 게이지 보완(켜짐 안풀림+지급 2중확인 목록+투명도): 사장님 피드백 3건. ①**[버그]"미션 켜짐"이 저절로 풀림** — 패널이 6초 폴링 load()에서 입력칸(active 등)을 서버 옛값으로 매번 덮어써서, 저장 전 체크가 풀렸음. initRef(useRef)로 **입력칸은 첫 1회만 채우고 이후 폴링은 진행률(prog)만 갱신** → 편집 중 안 덮어씀. ②**지급 2중확인 목록** — "구매자 전원 지급" 확인모달에 **닉네임·결제금액·주문시각(MM/DD HH:mm KST) 목록(스크롤) + 총 N명·총 지급 P** 표시, 버튼도 "○○P 지급 실행". lib/mission.ts MissionBuyer에 amount(같은 사람 여러 주문 금액합산)·when(최신 주문시각) 추가, payout_preview가 buyers 배열 반환. ③**위젯 투명도** 0.62→0.55(달성 0.82→0.72)로 더 투명(흰 배경 미리보기라 진해 보임). 검수 tsc 0에러+가드+clean+집계 시뮬 4/4 PASS(중복합산·최신시각·총액). ⚠️미배포(로컬 build+push). DB변경 없음. ※preview=1 미리보기 정상 확인됨.
- 2026-06-18 미션 게이지 2단계(구매자 전원 지급) + 위젯 미리보기: ①**전원 지급(돈)** — 미션 탭에 "🎁 구매자 전원에게 지급하기" 버튼+확인모달. **새 돈 로직 0**: 기존 검증된 일괄지급 훅(useBulkPointGrant→/api/admin-live/customer-points action:grant)을 재사용. 흐름: payout_preview(대상 N명·1인당·총액·이미지급여부 조회) → 확인 → payout_confirm(중복가드 mission_paid_<broadcastId> 선설정 후 대상 반환) → 클라가 일괄지급 → 전부 실패 시 payout_reset로 가드 해제(재시도 가능). 대상=현재 방송 결제완료·비테스트 주문을 **전화번호(숫자만) 기준 1인 1회 중복제거**. lib/mission.ts에 fetchMissionBuyers/readMissionPaid/setMissionPaid 추가, API에 payout_* 액션 분기(설정저장 기본동작 유지). ②**위젯 미리보기** — /event-mission/live?preview=1이면 미션 OFF여도 샘플 막대(100개중 63개) 표시(켜기 전 디자인 확인용). 검수 tsc 0에러+BANKDA 가드+clean+**지급로직 시뮬 4/4 PASS**(중복제거·미결제/테스트/번호없음 제외·총액·중복가드). ⚠️미배포(로컬 build+push). **DB변경 없음**(포인트는 기존 customer_point_ledger/balances, 가드는 settings 키). ※테스트모드 구분 없이 실제 지급되니 확인모달의 총액 확인이 안전장치(롤렛처럼 운영모드 게이트는 미적용 — 필요시 추가).
- 2026-06-18 미션 게이지 위치 수정(사이드바 메뉴→이벤트 탭): 사장님 — "이벤트 메뉴 안에 탭으로 넣기로 했잖아". 직전 커밋(21d8b93)에서 룰렛 지급로직 파일 안 건드리려고 임의로 별도 사이드바 메뉴로 뺀 것을 합의대로 되돌림. **AdminLiveEventRoulettePanel**에 eventTab "mission" 추가(룰렛/인형뽑기 옆 "🎯 미션 게이지" 탭), 그 탭일 때 룰렛+참가자 본문 대신 `<AdminLiveMissionPanel/>` 렌더(룰렛 본문 전체를 eventTab!=="mission" 분기로 감쌈). 룰렛/인형뽑기 추첨·지급·spin 로직은 무변경(탭+렌더 분기만). 별도 사이드바 메뉴는 원상복구(adminLiveMenu mission 항목·union·AdminLiveDashboard import/MENU_KEYS/모달렌더/placeholder 제거). AdminLiveMissionPanel/lib/mission/API/위젯페이지는 그대로 재사용. tsc 0에러+BANKDA 가드+사이드바 잔재0. ⚠️미배포(로컬 build+push). DB변경 없음.
- 2026-06-18 미션 게이지(공동목표) 1단계 — 설정+게이지 위젯(돈 위험0): 사장님 신규 기능. "오늘 누적판매 N개/매출 N원 목표 → 달성 시 구매자 전원 포인트" 라이브 공동목표 막대. **1단계는 읽기·설정 전용(전원 지급=돈은 2단계로 분리).** 신규: ①**lib/mission.ts** — 설정(settings 테이블 mission_* 키)·진행률 계산. 진행률=현재 방송(broadcasts.status='ON') 기간의 **결제완료(자동·수동입금확인/카드결제완료)·비테스트** 주문을 세서 qty합(개수)/final_amount합(매출). 주문 읽기만, 돈로직 무관. ②**app/api/event-mission/overlay** — OBS 위젯용 공개 토큰(mission_luludongi_live) 진행률 API(읽기). ③**app/api/admin-live/mission** — 관리자 GET(진행률)·POST(설정 upsert, settings mission_* 키만. 설정패널 숫자전용 시스템과 분리). ④**app/event-mission/live/page.tsx** — OBS 슬림 게이지 위젯(투명배경, 4초 폴링, 평소/임박90%+/달성100% 연출, "구매자 전원 N P" 문구, 미션 OFF·목표0이면 완전 숨김). ⑤**components/admin-live/AdminLiveMissionPanel.tsx** — 진행률+목표종류(개수/매출)·목표값·1인당 포인트·제목 설정+위젯주소 복사. 수정: adminLiveMenu(🎯 미션 게이지 메뉴 추가), AdminLiveDashboard(import+MENU_KEYS_FOR_URL+모달 렌더+placeholder 체인에 mission 제외). 검수 tsc 0에러+BANKDA 가드+console.log clean. ⚠️미배포(로컬 build+push). **DB변경 없음**(settings 키 사용, 새 테이블 X). ※2단계=전원 포인트 지급(요약 확인 후 일괄지급 재사용+중복가드), 3단계 다단계목표, 4단계 달성 축하연출, 5단계 유튜브 채팅 멘트.
- 2026-06-18 물건챙기기 시각표시 개선(닉네임 옆·전체날짜): 사장님 — 제출시간이 닉네임 아래 별도 줄로 칸 차지해 보기 안좋음 + 년월일(요일) 오전/오후로 보기 편하게. 반영: ①헤더 시각을 **닉네임 옆 같은 줄**(items-baseline, 닉네임 truncate·시각 shrink-0 회색 11px)로 이동(세로칸 절약). ②표시 형식 **"YYYY.MM.DD(요일) 오전/오후 h:mm"**(whenText 헬퍼). 직전엔 submittedAt(이미 "HH:mm" 가공문자열)을 표시하려다 못 읽었는데, 이번엔 **raw createdAt**으로 포맷. 오전/오후는 환경(ICU) 안 타게 24시값에서 직접 계산(자정=오전12:00·정오=오후12:00 검증). ③정렬도 raw 타임스탬프(ts 헬퍼 ms 비교)로 변경 → 시간순이 날짜 넘어가도 정확(기존 "HH:mm" 문자열비교는 일자경계서 틀림). 표시·정렬만, picked_at/돈/주문 로직 무관. tsc 0에러+가드. ⚠️미배포(로컬 build+push). DB변경 없음.
- 2026-06-18 물건챙기기 헤더에 제출시간 표시: 사장님 — "시간순" 정렬이 무슨 시각 기준인지 안 보여 막연. 패널 헤더 닉네임 아래에 **"제출 HH:mm"**(KST, 회색 11px) 추가 = 정렬 기준(submittedAt 없으면 createdAt)을 그대로 노출. timeText 헬퍼(Intl Asia/Seoul, 파싱실패 시 미표시). 금액·입금시간은 의도적으로 제외(피킹에 불필요·노이즈). 표시만, picked_at/돈/주문 로직 무관. tsc 0에러+가드+clean. ⚠️미배포(로컬 build+push). DB변경 없음.
- 2026-06-18 물건챙기기 헤더/상품 구분 + 검색 + 높이고정: 사장님 — ①닉네임과 상품 둘 다 앞에 같은 체크박스라 헷갈림(말 그대로 이모지 말고 best 방법 찾기) ②닉네임·상품명 검색칸 ③세로px 검색량 무관 무조건 88vh 고정. 반영(그룹리스트 시각계층): **패널 헤더(주문서)엔 체크박스 제거 → 사람 아바타(닉네임 첫글자·앱 고객목록 패턴)+이름(15px 검정굵게)+결제완료/미결제(미결제=빨강 강조)+진행 N/M·완료**, 헤더 클릭=전체 토글(완료 시 아바타=✓·헤더 emerald-50). **체크박스는 상품 줄에만(네모)+pl-6 들여쓰기** → "동그란 아바타=주문서 / 네모 체크=상품" 즉시 구분(이모지 제거). 검색=닉네임 또는 상품명 부분일치(고정영역 input), **검색은 표시만·진행률/초기화 범위(scopedPanels=결제완료만+정렬)엔 영향 없음**(totals·resetAll은 scoped 기준). 높이=흰 박스 h-[88vh] 고정+헤더/검색/하단버튼 shrink-0+목록만 flex-1 스크롤(검색량 무관 불변). picked_at 한 칸만 update·돈/주문 로직 무관. 검수 tsc 0에러+BANKDA 가드+clean. ⚠️미배포(로컬 build+push). DB변경 없음.
- 2026-06-18 물건챙기기 패널형 재설계(피킹 UI 표준): 사장님 피드백 — 평면 리스트 헷갈림. 검색 근거(warehouse order picking: pick list per order·항목 verify·progress·status 명확) 반영. LiveOrderPickingModal을 **주문서(같은 order_group_id=LiveOrder 1개) 단위 패널**로 재작성. 패널 헤더=닉네임(크게)+결제완료/미결제 배지(미결제=빨강 강조)+진행 N/M·완료. 패널 헤더 클릭=그 주문 전체 챙김/해제(일괄), 상품줄 클릭=개별. **패널 안 상품 전부 체크되면 자동 "✓완료"**(초록 테두리/배경). 상단 "챙김 N개/전체 M개"=수량 합계(체크 시 카운트). 결제완료만 토글·ㄱㄴㄷ/시간 정렬·전체 초기화·엑셀 유지. picked_at 한 칸만 update(.in 청크), 돈/주문 로직 무관. 검수 tsc 0에러+가드+clean. ⚠️미배포(로컬 build+push). DB변경 없음(picked_at 이미 적용).
- 2026-06-18 물건챙기기 체크리스트 팝업(picked_at 서버저장): 사장님 — 기존 물건챙기기가 누를 때마다 엑셀 새로 뽑아 "내가 뭘 챙겼나" 기록이 안 남아 헷갈림. **신규 LiveOrderPickingModal.tsx** — 주문 상품별 한 줄(닉네임·주문내역·수량)+상품별 "챙김" 체크. 체크 시 **orders.picked_at**(서버) 토글 → 다른 기기·새로고침에도 동일 유지(여러 기기 동기화), 개별 재클릭 해제+전체 초기화(확인 후). "결제완료만" 토글(기본 ON, 끄면 미결제 포함·취소건 항상 제외), ㄱㄴㄷ/시간 정렬, 엑셀 내보내기(기존 exportLiveOrdersForPicking 재사용). 모달 높이 88vh 고정(흰 박스 flex-col 내부 스크롤). **LiveOrderTable** 물건챙기기 버튼이 기존 export-confirm 대신 모달 열게(orders=exportableOrders, 송장출력 rozen 플로우는 무변경). **돈/입금/정산/주문상태 무관** — picked_at 한 칸만 update(.eq id / .in 청크). 검수 tsc 0에러+BANKDA 가드+grep clean. ⚠️미배포(로컬 build+push) + ⚠️Supabase에 orders_picked_at.sql(ADD COLUMN picked_at) 실행해야 체크 저장됨(미실행 시 체크 토글이 에러, 주문엔 영향 없음).
- 2026-06-18 이벤트 패널 높이 고정 + 당첨카드 돌리기버튼 안가림: 사장님 — 룰렛 모달 늘었다줄었다·잘림 + 당첨 오버레이가 ▶돌리기 가림. ①**[버그]모달 높이 무효** — 박스 maxHeight가 `calc(100vh-24px)`(띄어쓰기 없어 CSS 무효) → 내용따라 늘었다줄었다·잘림. `height: "88vh"` 고정+overflowY auto로 변경(AdminLiveEventRoulettePanel 1069). ②**당첨 발표카드가 돌리기 버튼 덮음** — 카드가 박스 정중앙(top:50% translate -50%)이라 하단 ▶돌리기 가림. 카드를 룰렛 상단(top:8px, translateX만)으로 올리고 pointerEvents:none(시각+클릭 통과) → 돌리기 항상 보이고 눌림. 표시·레이아웃만. ③**[버그]자동지급 전화번호 조회 보강** — grantPointToWinner가 전화번호를 `orders.youtube_nickname`에서만 찾아서, 주문 이력 매칭 실패 시 "전화번호 못찾음"으로 지급대기 유지됐음. 카카오 간편로그인은 customers에 전화번호 항상 저장되므로 → 조회를 ①orders 닉네임 → ②customers youtube_nickname → ③customers kakao_nickname 순으로 보강(nick trim). 지급 로직(/api/admin-live/customer-points 호출·is_reward_done 게이트·중복가드)·금액·테스트모드 스킵은 무변경, 전화번호 찾는 범위만 넓힘. 검수 tsc 0에러+가드. ⚠️미배포(로컬 build+push). ※테스트모드는 여전히 자동지급 스킵(운영모드만 지급)=의도.
- 2026-06-18 고객 목록 정리(필터 기본 OFF + 일괄지급 칩 ✕): 사장님 — "0건이라고 다 숨기면 아직 주문 안 한 진짜 가망고객까지 가려진다"(맞는 지적). ①**"주문 있는 고객만" 필터 기본 ON→OFF** (buyersOnly useState(false)): 평소엔 모든 진짜 고객 표시, 구매자만 보고 싶을 때만 켜는 선택 토글로. ②일괄지급 모달 선택 칩에 ✕(개별 제거) 추가. ③테스트 찌꺼기 두두 3개(id 690·887·888, 가짜번호 9999/1222/3333·주문0)만 콕 집어 삭제(Supabase 수동) — 나머지 0건 행은 실제 가망고객이라 보존. ※고객 식별 구조는 customer_phone 기준(카카오=번호 1:1), 받는사람은 shipping_addresses(별도 고객 아님) — 갈아엎기는 돈매칭 위험으로 비추천(memory: customer-identity-model 기록). 표시·레이아웃만, 돈/주문 로직 무변경. tsc 0에러+가드. ⚠️미배포(로컬 build+push).
- 2026-06-18 고객·이슈 일괄지급 UX보완 4건: 사장님 피드백. ①**선택 회원 이름 표시** — 일괄지급 모달에 selectedCustomersList(전화번호 기준) 칩 목록(max-h-24 스크롤)으로 누구 받는지 표시(기존 'N명'만 → 이름 칩). ②**필터 검색에도 적용** — buyersOnly 조건에서 `!searchText` 제거 → '주문 있는 고객만' ON이면 검색해도 0건/테스트 계정 숨김(끄면 보임). ③**모달 높이 고정** — AdminLiveCustomersPanel 모달 박스 max-h-[88vh]→h-[88vh](내용 따라 늘었다 줄었다 방지, 내부 flex-1 overflow-y-auto 스크롤). ④**전화번호 차단 버튼 삐져나감** — AdminLivePhoneBlockPanel grid-cols-[180px_1fr_110px_110px]→flex flex-wrap(입력 flex-1 min-w, 버튼 shrink-0 px-4) → 차단해제 버튼 넘침 해결. 돈/포인트/차단 로직 무변경, 표시·레이아웃만. 검수 tsc 0에러+가드. ⚠️미배포(로컬 build+push). ※받는사람(배송지) 검색→주문자 노출 아이디어는 별도 검토(현재 두두·백아연 등 0건 행은 shipping_addresses 아니라 별도 테스트 customers 행이라, buyersOnly 필터로 숨김 처리됨).
- 2026-06-18 고객·이슈: 주문있는고객만 필터 + 일괄 포인트지급 + 포인트 메뉴 통합: 사장님 요청. ①**주문 있는 고객만 필터(기본 ON)** — AdminLiveCustomersPanel filteredCustomers에 `buyersOnly && !검색어 && orderCount<=0 → 제외`(검색 중엔 미적용=찾던 사람 안 가림). 테스트·0건 계정 숨김(삭제 아님·데이터 유지). 고객목록 헤더에 토글칩. ②**일괄 포인트지급** — 고객 행 체크박스 다중선택(전화번호 있는 고객만)+페이지 전체선택+선택 N명 바→모달(1인당 금액·사유 드롭다운[방송이벤트당첨/단골감사/리뷰감사/오지급보정/직접입력]+직접입력칸·내부메모·고객노출체크·합계 표시·성공/실패 집계). **신규 useBulkPointGrant.ts** = 기존 단건 API(/api/admin-live/customer-points, action:grant)를 선택 고객마다 **순차 호출**(새 돈 로직 0, 회수는 일괄 제외). 돈/포인트 로직·API·lib/customerPoints 무변경. ③**포인트 사이드바 메뉴 제거** — adminLiveMenu.ts에서 point 항목 삭제(빈 placeholder였음). 포인트 관리는 고객·이슈로 통합(단건 지급은 기존대로 회원상세에). 검수 tsc 0에러+BANKDA 가드+grep clean. ⚠️미배포(로컬 build+push). DB변경 없음. ※업계표준 근거: bulk action UX(체크박스+선택수+성공/실패 피드백), CRM 목록 필터.
- 2026-06-18 설정 모달 UI/UX 재설계(업계표준 2-pane): 사장님 "설정 메뉴 별로다, 업계표준 검색해 제대로". 기존=카드 7개 세로 1줄 나열(긴 스크롤)+상단 전역 저장버튼인데 유튜브/보안은 자체 저장이라 **저장 모델 혼란**+미리보기 카드는 입력값 중복+개발자 잔재 문구. 재설계(검색 근거: 카테고리 네비/progressive disclosure/sticky save): AdminLiveSettingsPanel을 **좌측 카테고리 네비(결제·배송/포인트 적립/주문서 표시/유튜브 알림/관리자 보안) + 우측 스크롤 내용 + 하단 고정 저장바**로. SETTINGS_TABS/GLOBAL_SAVE_TABS 추가, activeTab state. 하단 저장바는 운영값 탭(결제·배송/포인트/주문서)에서만 saveSettings(기존 그대로) 노출, 유튜브/보안은 자체 저장이라 숨김 → 저장 혼란 해소. **제거**: 현재 설정 미리보기 카드(입력 중복), "합배송 시간...다음 단계" 개발자 잔재, 상단 큰 헤더(모달헤더 ⚙설정과 중복). preview useMemo·useMemo import 정리. **돈/저장 로직 전부 무변경** — saveSettings/SettingInput/상태/upsert 키 그대로, JSX 배치·네비만. 모달 껍데기(AdminLiveDashboard)=max-w 700→880, py-8 페이지스크롤→h-[88vh] flex 고정+내부스크롤(헤더 고정). 검수 tsc 0에러+BANKDA 가드+grep clean. ⚠️미배포(로컬 build+push). DB변경 없음.
- 2026-06-18 유튜브 알림 2단계 보완(인증버그 수정+URL통일+문구 placeholder): ①**[버그]설정저장 시 "관리자 로그인 불일치"** — /api/youtube/* 라우트가 옛 인증(assertAdminRequest=ADMIN_SESSION_TOKEN, lib/adminAuth.ts)을 썼는데 admin-live 실제 인증은 lib/admin-auth.ts(서명쿠키 ruru_admin_session, env RURU_ADMIN_ID/ADMIN_SESSION_SECRET)+middleware라 쿠키값 불일치로 401. → 3개 라우트(oauth-start·oauth-callback·admin)를 verifyAdminSessionFromRequest로 교체(미들웨어 matcher엔 /api/youtube 없어 라우트 자체 인증 유지). ②**라이브URL 통일** — 설정에 중복 입력칸 제거, 메인 컨트롤타워가 저장한 현재 방송(broadcasts status=ON)의 youtube_live_url을 알림이 자동 사용(resolveLiveChatId가 readActiveBroadcastLiveUrl로 읽고 videoId 바뀌면 캐시 자동갱신). 카드엔 "현재 라이브 주소(자동)" 읽기전용 표시. ③**문구 확장** — buildOrderMessage({nickname,itemsSummary,amount})로 {{nickname}}·{{items}}·{{amount}}(택배비 포함 총액) 치환, 기본문구 DEFAULT_MESSAGE_TEMPLATE="🛒 {{nickname}}님 주문 감사합니다! ({{items}} · {{amount}})", 빈 placeholder 잔여기호 정리. 실제 items요약("xxx 외 N건")·금액은 3단계(주문 제출 연결)에서 주문데이터로 채움. 검수 tsc 0에러+가드 통과+grep clean. ⚠️미배포(로컬 build+push). DB변경 없음(youtube_integration 이미 적용). ※설정 메뉴 모달 UI/UX 개선은 사장님 요청으로 별도 진행 예정.
- 2026-06-18 유튜브 라이브 채팅 자동알림 2단계(연결+발송기능, 곁다리): 주문 제출 시 유튜브 라이브 채팅에 "○○님 주문 감사합니다" 자동 게시 기능. 봇 계정(별도 구글 계정)으로 게시. **신규**: supabase/sql/youtube_integration.sql(refresh_token 서버 전용 테이블, RLS로 anon 차단·service_role만 접근), lib/youtube.ts(refresh→access token, 라이브URL→videoId→videos.list activeLiveChatId 조회·캐시, liveChatMessages.insert, 실패 시 throw 안 함 {ok:false}만), app/api/youtube/oauth-start·oauth-callback(관리자 전용+state CSRF, code→refresh_token 저장), app/api/youtube/admin(GET 상태/POST save-url·save-settings·test), components/admin-live/YoutubeNotifyCard.tsx(설정 패널 카드: 연결버튼/라이브URL저장/ON·OFF/문구/테스트발송). 비밀 아닌 설정(youtube_live_url·live_chat_id·notify_enabled·message_template)은 settings 테이블, **refresh_token만 서버전용 테이블**. 봇 계정이라 라이브 자동탐지 불가→방송마다 라이브URL 붙여넣기. 할당량: insert 50점/일10,000=하루 200건, 실측 최대 147건이라 여유(증설 불필요). 검수 tsc 0에러+BANKDA 가드 2개+grep clean. ⚠️미배포(로컬 build+push) + ⚠️Vercel 환경변수 YOUTUBE_CLIENT_ID/YOUTUBE_CLIENT_SECRET 추가 + ⚠️Supabase에 youtube_integration.sql 실행 필요. ※3단계(주문 제출 route에 곁다리 호출)는 테스트 발송 성공 확인 후 진행(주문 로직 보호).
- 2026-06-18 방송 위젯(product-widget) 자유 위치 드래그: 사장님 — 위젯을 화면 왼쪽에 바짝 붙이려는데 안 됨(카드가 left:24px 고정, OBS 풀스크린 소스라 자유 이동 불가). ProductWidgetClient 카드에 드래그 이동 추가 — pos state{x,y}+dragRef, onMouseDown=startDragWidget(window mousemove/up로 이동, 종료 시 localStorage 'ruru_product_widget_pos' 저장), 카드 style left/top=pos(없으면 기본 left:24/bottom:24 유지)+cursor:move+pointerEvents:auto(페이지 전체는 기존 pointerEvents:none 유지=OBS 캡처 방해 안 함). 토스트/컨페티는 기존 좌하단 유지(전환·주문 토스트·로테이션 로직 전부 무변경). ⚠️OBS에선 소스 우클릭→상호작용(Interact)에서 드래그해야 OBS localStorage에 저장됨(브라우저서 옮긴 위치는 OBS와 별개). 표시 전용·돈/주문 로직 무관. 검수 tsc 0에러+BANKDA 가드 통과. ⚠️미배포(로컬 build+push). DB변경 없음.
- 2026-06-18 상품관리 썸네일 클릭 확대(방송상품·쇼핑몰진열 탭): 사장님 피드백 — 방송 상품/쇼핑몰 진열 탭에서 썸네일 클릭해도 안 커져 상품 보기 어려움. AdminLiveProductManagePopup의 전체상품 탭(1335 setLightbox)·판매기록 탭은 이미 확대되는데 이 두 탭만 썸네일 span에 onClick 없었음. 두 탭 썸네일 span에 onClick={()=>img&&setLightbox(img)}+cursor zoom-in+title 추가(드래그 방해 안 되게 e.stopPropagation). 기존 lightbox 오버레이(fixed z-60) 그대로 재사용. 표시 전용, 돈/재고/주문 로직 무관. 검수 tsc 0에러+BANKDA 가드 통과. ⚠️미배포(로컬 build+push). DB변경 없음.
- 2026-06-18 입금패널/사이드패널/새로고침 UX 4건(돈·매칭 로직 무변경): 사장님 실사용 피드백. ①**입금매칭 패널 버튼 상단 이동+고정** — LiveFloatingMatchPanel 하단의 [선택 후 입금확인]/[금액 무시하고 수동확인]+선택합계 줄을, 매칭배너 바로 아래(헤더 flexShrink:0 영역)로 이동. 사이드패널 컨테이너가 max-h-[1000px] 고정이라 모니터보다 길어 하단 버튼이 화면 밖으로 밀리던 문제 해결(이제 스크롤해도 상단 고정). handleConfirmWithDeposit/handleConfirmWithoutDeposit onClick·매칭 API(/api/admin-v2/manual-payment-match, manual-payment-confirm-without-deposit) **그대로**. ②**오른쪽 사이드 패널 단일 슬롯 자동전환** — 주문상세 열 때 매칭패널 닫기(setMatchPanelOpen(false)+setSelectedOrderForMatch(null), onSelectOrder 2곳=테이블/빠른보기), 매칭 열 때 주문상세 닫기(onSelectForMatch에 setOrderDetailOpen(false)). 매칭 열린 상태에서 주문 클릭해도 안 바뀌던 문제 해결. openManualMatchForOrder는 원래 주문상세 닫음(유지). ③④**새로고침 시 필터·페이지 보존** — 앱 ↻/실시간 폴링(loadOrders)은 원래 filters/page 안 건드림 확인 → 증상은 **브라우저 F5**로 React 상태 초기화되는 경우. filters(Dashboard)·page(LiveOrderTable)를 **sessionStorage**에 보존(LIVE_ORDERS_FILTERS_KEY/PAGE_KEY, 기본값/1페이지면 removeItem=자동정리). 복원 시 didInitBroadcastFilter는 filtersRestoredRef로 기본값 덮어쓰기 스킵, setPage(1) 효과 2개는 mountedRef로 첫 마운트 스킵(복원 페이지 보존). 초기화 버튼(resetFilters)=기본값→저장삭제+page1로 자연 리셋. sessionStorage=세션 한정(새 탭/새날은 현재방송 기본값). 운영/돈 데이터 아닌 보기상태(기존 ruru_admin_sound_on localStorage·activeMenu URL 보존과 동일 관행). 검수 tsc 0에러+BANKDA 가드 2개+grep clean. ⚠️미배포(로컬 build+push). DB변경 없음.
- 2026-06-18 #3 3단계 UX보완(피커 모달화+옵션 정확화+삭제버튼 위치): 사장님 실사용 피드백 3건 반영. ①**사이즈 옵션 누락 수정** — 피커가 product_note.stock_variants만 읽어 size_options/sizes로 저장된 상품(예 뉴발 신발)의 사이즈가 안 떴음. 고객 주문페이지(app/order/page.tsx)의 옵션 읽기 로직을 그대로 이식: optionSuggestions(stock_variants + colors/color_options/product_colors/option_color/color, size 동일)+normEmpty(없음계열 제거)+optionMode(select/input/none, size_option_enabled·color_option_enabled 플래그). 색상/사이즈를 **field별 모드**로 렌더(select=칩 선택필수 / input=직접입력 / none=숨김). RPC엔 등록상품에서 읽은 옵션값 그대로 전달(재고 키 일치 유지). ②**피커를 큰 팝업(모달)로** — 좁은 380px 사이드바 인라인 → fixed inset-0 z-[120] 중앙모달(560px, 헤더+✕닫기+검색+상품목록 스크롤+옵션/수량/단가+하단 추가버튼). LiveOrderDetailDrawer가 onClose 전달. (드로어 ruruSidePanelIn 애니메이션 fill-mode 없음→모달 fixed 정상, 입금팝업 z-40과 배타분기라 충돌X). ③**삭제버튼 위치 이동** — 주문내역 카드 아래 별도 줄(세로폭 증가) 제거 → LiveOrderItemEditCard '수정' 버튼 옆으로(canDelete/deleting/onDelete props 추가, 기존 handleDeleteItem 연결 그대로). **돈/입금/정산/포인트/재고 RPC 로직 전부 무변경**(표시·입력수집·삭제호출 위치만). 검수 tsc 0에러+BANKDA 가드 2개 통과+grep clean. ⚠️미배포(로컬 build+push). DB변경 없음(SQL 2개는 이미 적용됨).
- 2026-06-18 #3 3단계: 등록상품 추가 피커 UI 신설(재고차감 RPC 연결): 다른 방에서 만든 admin_add_order_item RPC(등록상품 재고차감)가 SQL만 있고 프론트 호출이 없던 死코드였음 → 실제 UI 연결. **신규 LiveOrderRegisteredProductPicker.tsx** — products select(*)+status!=='deleted' 필터+상품명 검색(상위30)+썸네일(resolveProductImageUrl)+상품 클릭 선택. 선택 시 product_note.stock_variants 파싱: variant 있으면 옵션칩(색상/사이즈+재고수, 재고0=disabled) 선택 필수 / 없으면 '옵션없음'(총재고 또는 재고관리안함 표시). 수량+단가(상품 price 기본값, 수정가능). **핵심 안전장치**: 색상/사이즈는 자유입력 아니라 선택한 variant 객체에서 그대로 RPC로 전달 → 재고 키 불일치 원천차단. **useLiveOrderItemAdd.ts**에 addRegisteredItem 추가(LiveOrderRegisteredAddInput, admin_add_order_item RPC 호출, confirm·검증·res.ok 체크, 성공시 결과반환) — 기존 addDirectItem(직접입력 직접INSERT 경로)은 무변경. **LiveOrderDetailDrawer**: 주문내역 헤더에 [+ 등록상품 추가] 버튼 추가(직접입력 버튼과 상호배타 토글), showPicker 시 피커 렌더, handleAddRegisteredItem이 RPC 호출+성공시 localOrder.items 낙관적 append+onAfterStatusChange(직접입력과 동일 패턴). **돈/입금/정산/포인트 무관** — 재고차감·카드vat·그룹필드복사·재고부족검증·INSERT는 전부 RPC가 처리(택배비0·point_used0·입금확인된 주문은 금액↑시 입금확인취소 재매칭=기존 합의정책 동일). 검수 tsc 0에러+BANKDA 가드 2개 통과(next build는 샌드박스 swc 미설치로 로컬 실행 필요)+grep/import/연결 검수 통과. ⚠️미배포(로컬 build+push) + ⚠️Supabase에 admin_add_order_item_rpc.sql·admin_delete_order_item_rpc.sql 2개 실행해야 추가/삭제 동작(미실행시 'RPC 없음' 에러).
- 2026-06-17 #3 2단계: 주문상품 삭제 + 등록상품추가 RPC 작성: Supabase 재고함수(admin_update_inventory_linked_order_item) 전문·트리거·FK 확인 후 정석 구현. **신규 SQL 2개(Supabase 직접 실행 필요)**: ①admin_add_order_item_rpc.sql — 기준 행 복사로 그룹에 상품 추가(p_product_id null=직접입력 재고무관 / 값=등록상품 재고차감, 옵션=product_inventory_variants·총량=products.stock, inventory_ledger 기록, 재고부족시 raise, 카드 vat=그 주문 수수료율 동일공식). ②admin_delete_order_item_rpc.sql — 주문상품 1줄 삭제(등록상품=재고복구 후 하드삭제 / 직접입력=그냥 삭제). 가드: 그룹 마지막1개·포인트사용 항목은 삭제금지(주문취소 유도). FK 확인됨(order_items CASCADE/money_logs CASCADE/status_logs SET NULL, inventory_ledger는 orders FK 없음)→하드삭제 안전. **UI**: LiveOrderDetailDrawer 주문내역 각 항목에 [🗑 이 상품 삭제] 버튼(취소아님+2개이상일때, useLiveOrderItemDelete→admin_delete_order_item RPC, 성공시 localOrder.items에서 제거+onAfterStatusChange). 입금내역/정산/포인트 무변경(금액↓시 입금확인취소로 재매칭=합의). DELETE는 orders UPDATE 트리거(포인트)와 무관. 검수 tsc 0에러+가드 통과. ⚠️미배포(로컬 build+push) + ⚠️Supabase에 SQL 2개 실행해야 함(삭제RPC 미실행시 삭제버튼 동작 안함). ※등록상품 선택추가 UI(피커)는 add RPC 위에 다음 단계로 붙임(현재 직접입력 추가는 기존 JS 경로 유지).
- 2026-06-17 주문상세 주문내역 내부스크롤 제거(정정): 사장님 본래 요청=주문내역 영역 자체 내부스크롤 없애고 주문상세 패널 본문(min-h-0 flex-1 overflow-y-auto)이 통째로 스크롤. 이전에 max-h-[240px]→[480px]로 잘못 반영했던 것을 LiveOrderDetailDrawer 주문내역 리스트 div `max-h-[480px] overflow-y-auto pr-1` → `space-y-2`로 변경(내부 스크롤 제거). 로직 무관, UI만. tsc 0에러+가드 통과. ⚠️미배포(로컬 build+push).
- 2026-06-17 #3 1단계: 주문상세 직접입력 상품 추가(재고 무관) 신설: 신규 useLiveOrderItemAdd.ts — 기존 주문(LiveOrder)의 첫 행(order.rowIds[0])을 select * 로 읽어 그룹 공유필드(SHARED_KEYS: created_at·order_group_id·order_lookup_code·broadcast*·youtube_nickname·customer_name/phone·recipient*·주소·payment_method·카드수수료율·order_status/admin_status/order_manage_status/admin_order_status_v2/shipping_status·exclude*·customer_id) 복사 + 직접입력 항목값(product_id=null→재고 무차감, product_name/color/size/qty, product_price=단가, adjusted_product_price=상품금액, shipping_fee/adjusted_shipping_fee=0[택배비 중복방지], vat_amount=카드면 round(상품금액×customer_card_extra_rate_applied/100) else 0[submit route 130~137 동일 공식·그 주문 수수료율], total_price/adjusted_total_price/final_amount=상품금액+vat, memo, 선택컬럼 actual_card_fee_amount/point_used_amount=0/point_original_amount는 첫행에 있을 때만)으로 orders INSERT. LiveOrderDetailDrawer 주문내역 헤더에 [+ 직접입력 추가] 토글 버튼+인라인 폼(상품명/색상/사이즈/수량/단가)+추가 시 localOrder.items 낙관적 append+onAfterStatusChange. created_at 복사로 주문일 안 튐. **입금내역(deposit_confirmed_at)/포인트차감/정산/자동매칭/재고 RPC 무변경** — 추가행은 그룹 상태 그대로 복사라 입금판정 일관, 금액↑시 운영자가 입금확인취소로 재매칭(합의 정책). 고객 주문조회는 같은 order_group_id/lookup_code라 자동 반영(#4 추가분 충족). 검수 tsc 0에러+BANKDA 가드 통과+금액시뮬 10/10(무통장/카드 vat·반올림·총액). ⚠️미배포(로컬 build+push). DB변경 없음. ※2단계=상품 삭제(재고 복구 RPC 정의 확인 후), 3단계=등록상품 추가(재고 차감 RPC) 남음.
- 2026-06-17 [긴급] 상품관리 팝업 삭제(숨김) 실패 수정: 증상="삭제"→"상품 숨김 처리 실패 [object Object]" 토스트, 숨김 안 됨. 원인=AdminLiveProductManagePopup.deleteProduct가 `update({status:"deleted", is_visible:false})`로 직접 update했는데 is_visible 컬럼이 없으면 update 전체 실패(앱 다른 곳은 insert/updateProductSchemaSafe로 없는 컬럼 자동 제거 후 재시도하는데 이 핸들러만 미경유), 게다가 throw한 supabase 에러객체를 `String(e)`로 찍어 [object Object]로 표시됨. 해결=숨김 마커는 status="deleted" 하나로 충분(관리자 목록 645/679/1498 status==="deleted" 제외 + 고객 app/order 1823/2695 status!=="deleted" 제외 양쪽 확인) → `update({status:"deleted"})`만 실행(is_visible 제거로 실패원인 차단), 에러도 error.message 표시. 돈/입금/정산/배송/재고/RPC 무관(상품 표시 전용). 검수 tsc 0에러+BANKDA 가드 통과. ⚠️미배포(로컬 build+push). DB변경 없음.
- 2026-06-17 [긴급] 상품등록 막힘 + 고객 카테고리 "음식" 누락 수정: ①증상=방송모드/쇼핑몰모드 선택 UI 삭제 후 방송 OFF 상태에서 새 상품 등록 시 "방송상품은 방송 시작 후 등록할 수 있습니다" 경고로 막힘. 원인=QuickProductFastForm.saveProduct의 resolvedProductType이 신규=항상 "broadcast" 고정(773~776)→789 차단. 해결=신규는 activeBroadcastId 있으면 "broadcast"(방송상품), 없으면 "group_buy"(상시판매)로 등록(수정모드는 기존 타입 보존 그대로, 기존 group_buy 17개 보호). 방송 ON이면 종전대로 broadcast_products 연결. ②증상=등록폼은 커스텀 카테고리(음식) 추가 가능한데 고객 주문페이지(app/order/page.tsx) 카테고리 탭이 ["전체","의류","신발","잡화"] 하드코딩이라 "음식" 탭 없음. 해결=categoryFilter state 타입 string으로 완화 + 탭을 quickGroupBuyProducts의 실제 product_note.category에서 동적 생성(PRESET 의류/신발/잡화 유지 + 커스텀은 정렬해 뒤에 자동 추가), 탭 컨테이너 flexWrap. 필터 판정 로직(cat===categoryFilter) 무변경. **돈/입금/정산/배송/포인트/RPC 전부 무관(상품등록·표시 전용)**. 검수 tsc 0에러+BANKDA 가드 통과+시뮬 9/9(타입결정·차단·카테고리탭). ⚠️미배포(로컬 build+push). DB변경 없음.
- 2026-06-17 라이브 출고완료 처리/해제 기능 신설(송장출력→출고완료→고객 주문조회 반영 버그 해결): 증상=라이브(/admin-live)에서 송장 출력해도 고객 주문조회에 '출고완료'가 안 뜸. 원인(근거확인 완료)=라이브 "🚚 송장 출력"(LiveOrderTable runExport→exportLiveOrdersForRosen)은 **엑셀 내보내기만** 하고 상태를 안 바꿈. 출고완료 반영은 admin-v2의 로젠 재업로드("1차 출고완료 반영")에만 있어, admin-v2를 안 쓰면 출고완료가 영영 안 찍힘. 추가로 라이브 "출고완료" 칩·'출고'칼럼·counts.shipped가 order.shippingStatus를 읽는데 어댑터가 안 채워 **항상 0/"-"인 미완성 표시**였음. 해결(신규 useLiveOrderShipped.ts, 취소/복구 훅과 동일 안전패턴): 선택 주문 일괄 **출고완료 처리**(결제완료+미취소만 대상, admin_order_status_v2/order_manage_status="출고완료"+shipped_at 최초1회만 기록=기존값 보존 .is shipped_at null) / **출고완료 해제**(현재 출고완료 행만 .eq 가드로 "출고대기" 환원+shipped_at=null). LiveOrderTable에 선택>0일 때 [📦 출고완료 처리][↩ 출고완료 해제] 버튼(송장출력 옆), 성공 시 선택해제. liveOrderAdapter에 shippingStatus=배송단계값만(출고대기/출고완료/킵/픽업) 노출→'출고'칼럼·칩·counts 정상화(표시 전용). types.ts LiveOrder.shippingStatus 추가. **돈/입금/정산/포인트/Bankda/RPC/자동매칭 전부 무변경** — "출고완료"·"출고대기"는 PAID_STATUS_VALUES 포함이라 결제완료 판정 안 깨짐(어댑터 getPaymentStatus 확인). 고객반영 경로 확인: order_manage_status="출고완료"→getCustomerOrderStatusLabel="배송출발"→MyOrderResultCard deliveryStatus="출고완료". 검수 tsc 0에러 + BANKDA 가드2개 통과 + 순수로직 시뮬 14/14 통과. 1차 배포완료(3b0d3cd). admin-v2 무변경(보존).
- 2026-06-17 출고완료 2차 보완(입금배지 보존+직전상태 복원+출고칸 줄바꿈): 1차 배포 후 사장님 피드백 — ①출고완료/해제하면 입금배지가 "자동입금확인→입금확인"으로 바뀜(요청 안 함) ②출고칸 "출고완료/출고대기" 글씨 줄바꿈 ③출고완료33 기준?. 원인=orders에 배송상태 전용칸 없음→admin_order_status_v2/order_manage_status 한 칸이 입금확인→출고대기→출고완료로 단계 진행(admin-v2도 동일 구조). 출고완료로 올리면 자동/수동입금확인 구분이 그 칸에서 사라져 일반 입금확인으로 표시되고, 1차 해제는 "출고대기"로 고정복귀라 원래 입금상태 미복원. 출고완료33=예전 admin-v2 로젠재업로드로 이미 DB가 "출고완료"인 기존주문(신규생성 아님), 1차에서 shippingStatus 살리며 비로소 표시됨. 해결(추천안=직전상태 보존): **DB** orders ADD COLUMN shipped_prev_status(orders_shipped_prev_status.sql, ADD only). **useLiveOrderShipped** 출고완료 처리 시 현재 상태 조회→직전 입금상태를 shipped_prev_status에 보관(이미 출고완료/보관값 있으면 보존), 해제는 현재 출고완료 행만 직전상태로 복원(없으면 출고대기 fallback)+shipped_prev_status=null+shipped_at=null. **liveOrderAdapter.getPaymentStatus** 현재상태가 배송단계(SHIPPING_STAGE_STATUSES)고 shipped_prev_status 있으면 basisRow(상태=직전값)로 입금 배지 판정→자동/수동입금확인·카드결제완료 보존(직전값 없는 기존데이터는 기존동작 그대로=회귀0). **LiveOrderTable** 출고칸 badge whitespace-nowrap+px-1+출고완료 파랑(줄바꿈 해결). **돈/입금내역(deposit_confirmed_at)/정산/포인트/자동매칭 무변경** — shipped_prev_status는 표시·복원 전용. 검수 tsc 0에러+BANKDA 가드2개+시뮬 10/10(자동/수동/카드 보존·복원, 기존33 회귀0, 재처리 보관값보존) 통과. ⚠️미배포(로컬 `npm run build` 후 push) + ⚠️Supabase에 orders_shipped_prev_status.sql 실행해야 보존 동작(미실행시: 출고완료는 되나 입금배지 일반화·해제는 출고대기 fallback). ※1차에서 이미 해제했던 테스트주문은 직전상태 미기록이라 출고대기로 남음(기능상 결제완료 유지, 필요시 수동조정).
- 2026-06-16 받는사람(배송) ↔ 주문자(입금/포인트) 분리: 증상=배송지 시트에 받는 분·연락처를 입력받는데 주문엔 안 쓰이고 배송지 바꿔도 이름·전화 안 따라감(주소만 바뀜). 원인=orders에 받는사람 칸이 없어 customer_name/phone(=주문자)이 배송 받는분까지 겸함+onSelectShippingAddress가 customerName/phone 세팅했다가 closeCustomerInfoEditBottomSheet가 되돌림. 해결(표준 쇼핑몰식 주문자/받는사람 분리): **DB** orders ADD COLUMN recipient_name/recipient_phone(orders_recipient_columns.sql, ADD only). **order/page** recipientName/recipientPhone state 신설→applyCustomerFromRow·loadCustomerByNamePhone가 기본배송지 받는분/연락처로 세팅(없으면 주문자 fallback), onSelectShippingAddress는 이제 받는사람만 갱신(주문자 customerName/phone 불변), 주문서확인 카드 받는분/연락처=받는사람 우선, 제출 body에 recipient_name/recipient_phone 추가. **submit API**(app/api/customer-orders/submit/route.ts) RPC 무변경 유지+제출 직후 order_group_id로 recipient만 best-effort UPDATE(실패해도 주문 성공). **송장/엑셀**(adminLiveOrderExcelExport recipientName/phoneText, liveOrderAdapter recipientName/Phone 매핑, types.ts LiveOrder 필드) 받는분=recipient 우선·없으면 닉네임/주문자 fallback(옛주문 호환, 닉네임은 주소칸 /닉네임으로 유지). **입금자동매칭(닉네임 or customer_name)·포인트(customer_phone)·RPC 전부 무변경** — 주문자 기준 보호. 검수 tsc 0에러+시뮬3(선물배송 받는사람반영&주문자불변&입금매칭정상&송장정우성/본인배송/옛주문닉네임fallback) 통과. ⚠️미배포(빌드+push 필요) + ⚠️Supabase에 orders_recipient_columns.sql 실행해야 recipient 실제 저장됨(미실행시 송장은 기존 닉네임 fallback로 안전).
- 2026-06-16 직접입력 미완성항목 주문막힘 버그 수정(app/order/page.tsx): 증상=주문서 확인에 "아디"(직접입력·0원·수량0) 같은 유령 항목이 남아 제출 시 "수량을 입력해주세요"로 막힘. 원인=①직접입력창은 글자치는 즉시 items에 저장(상품명 onChange 4779)되는데 X로 닫아도(closeDirectInputSheet) 미완성 항목을 안 지움 ②카트/확인 화면에 삭제버튼이 아예 없음(removeItem 정의만 있고 미연결) ③`수량 {toNumber(qty)||1}개`라 빈 수량이 "1"로 보여 사용자 혼동(실제 검증 3141은 qty 없으면 차단). 수정: ①confirm 카드 각 상품에 ✕삭제버튼 추가(removeItem 연결, 비면 emptyItem 1개 유지) ②closeDirectInputSheet에서 미완성(상품명/수량/금액 누락) 직접입력 항목 자동 정리(완성·등록상품 product_id는 보존, 확정은 setDirectInputOpen(false) 직접호출이라 정리와 무충돌) ③수량 표시를 실제값으로(0이면 빨강), itemAmount도 실수량 사용. 돈/입금/정산/배송/포인트/RPC 무변경, UI만. 검수 tsc 0에러. 미배포(빌드+push 필요). ※배송지 "변경" 시 주소는 바뀌지만 받는분 이름·연락처가 안 따라가는 건 의도(customer_name/phone=주문자=입금/포인트 매칭 기준 보호). 받는사람 분리 반영은 입금매칭/송장 영향이라 별도 설계 필요(미진행).
- 2026-06-16 주소 일원화 Phase4(flat↔JSON 단일화, DB 트리거): 감사결과 남은 유일한 주소 리스크=주소 저장소 2원화(customers.address flat vs shipping_addresses JSON)로 배송지 시트 수정 시 JSON만 갱신·flat 잔존 → 관리자 회원상세 "📍주소" 한 줄만 옛값(※실제 송장/엑셀은 orders 스냅샷 사용이라 배송사고 무관, 손님화면은 JSON우선이라 정상). 해결=shipping_addresses(JSON)를 진실원천으로 단일화 + flat은 DB 트리거가 항상 기본배송지로 자동 미러. supabase/sql/customers_address_sync_trigger.sql — 함수 ruru_sync_customer_address()+트리거 trg_sync_customer_address(BEFORE INS/UPD): JSON 비어있지않으면 flat=기본배송지(isDefault>첫항목, 빈주소면 flat 유지), JSON없고 flat있으면 flat→JSON seed(phone 하이픈 포맷). 본문 전체 exception 삼킴→customers 쓰기(주문제출 등) 절대 안 막음=제출 위험0. 앱 코드 무변경(순수 DB). 기존 678행 `update customers set address=address`로 일괄 동기화. 운영 적용완료, 검증 주소있는데JSON없음0/flat↔JSON불일치0(id=742 빈JSON기본 1건은 flat주소로 JSON 채워 해결). customers_backup_20260616_p4 백업존재. ⚠️트리거 .sql은 기록용 커밋만(DB 이미 적용, 배포 불필요).
- 2026-06-16 주소 버그 근본해결 Phase1-2(카카오 로그인 후 배송지 뒤바뀜/누락): 진짜 원인=전화번호 포맷 불일치로 customers row 중복(주문 RPC는 숫자만 저장, 클라/login-sync는 하이픈 저장) + flat/JSON 분산 + 쓰기키 split-brain. 실측: customers 883건중 중복 205그룹·410row, flat주소만 789건, orders.customer_id 226·deposits.match_customer_id 1(FK 둘다 NO ACTION). **Phase1(DB, Supabase 적용완료)**: supabase/sql/customers_dedup_normalize_phone.sql — 원자적 DO블록으로 같은사람(숫자기준) 중복을 canonical 1개로 병합(canonical=kakao>주소>JSON>최근주문>먼저생성 순, 빈칸만 보충·주소는 한 row 통째·non-null 보존·is_blocked는 text'true'면 차단유지), orders/deposits 참조 canonical로 repoint 후 중복삭제, 전 row customer_phone 숫자만 통일. 결과 883→678(205제거), 남은중복 0. customers_backup_20260616 백업존재. **Phase2(코드)**: app/order/page.tsx의 loadExistingCustomerByKakaoPhone·loadCustomerByNamePhone·saveCustomer·saveShippingAddresses 4곳 + app/api/customer-login-sync/route.ts insert 1곳을 customers 조회/쓰기 키를 onlyNumber(숫자만)로 통일(표시·localStorage는 normalizePhone 하이픈 유지). 돈/입금/정산/배송/포인트/RPC 무변경. 검수 tsc 0에러 + 코드로직 시뮬 4시나리오 통과(A기존고객매칭/B신규중복0/C회귀증명/D혼합입력단일키). **배포완료(7ae26de)**. 운영DB 재확인 총678/숫자만678/비숫자0/중복0/주소있는row659. **STAGE3 unique 제약(customers_customer_phone_key) 적용완료** → 같은 전화번호 중복 물리적 차단(재발방지 완료). **Phase3 적용완료**: 타이밍 땜질 2개 제거 — callback 1800ms→800ms 환원, loadExistingCustomerByKakaoPhone의 빈배열 700ms retry 제거(+retryCount 파라미터 제거, 호출부 3곳 1-arg 정상). localStorage 주소 직접전달은 진실원천 이중화라 채택 안 함(DB 단일 row 유지). 검수 tsc 0에러. ※진짜 원인은 login-sync 라우트의 별도 normalizePhone만 하이픈이었음(order페이지 normalizeOrderPhone은 원래도 숫자만). **Phase3 배포완료(f6a6a20)**.
- 2026-06-16 방송 선택을 달력(캘린더) 방식으로 교체: 기존 방송 드롭다운(쌓이면 2년치 쭉 나옴)을 사장님 요청대로 달력 팝업으로. 신규 컴포넌트 BroadcastCalendarPicker.tsx — 방송 있는 날에 딥로즈 점·하루 2개+면 우상단 개수배지, 날짜 클릭 시 onPick(broadcastId)→scope=broadcast+broadcast=그방송(여러개면 아래 리스트로 고르기), 헤더 ‹달›› 이동+"전체 방송 보기" 버튼, 처음 보일 달=선택방송>최근방송>이번달. 날짜키는 부모에서 getAlwaysOrderDateKey(KST)로 변환해 전달(TZ버그 회피). LiveOrderTable: scope===broadcast일 때 BroadcastSearchSelect 대신 BroadcastCalendarPicker 렌더(import 교체, broadcastCalendar prop 추가). AdminLiveDashboard: broadcastCalendar useMemo(broadcasts→{id,dateKey,label}) 신규+prop 전달. ※중간에 시도했던 "기간 내 방송 칩 줄"은 사장님이 '2년치 쌓임 싫다' 해서 폐기(미배포). 돈/입금/정산/matchScope·matchBroadcast 무변경. 검수: tsc 0에러, 돈판정 diff 무변경. ⚠️next build 샌드박스 swc 미설치로 미실행(로컬/CC 빌드 필요). 미배포(커밋대기)
- 2026-06-16 주문서 필터 5칸→3칸 재설계(기간/범위/상태): ①날짜축 — LiveOrderDateFilter에서 `yearmonth` 제거·`lastmonth`(지난 달) 추가, `custom` 라벨 "직접 선택"→"기간 선택", filterYear/filterMonth 필드 완전 제거(타입·DEFAULT_FILTERS·resetFilters·라벨맵·useMemo deps). matchesDate에 lastmonth(전월 1일~말일, 1월=작년12월 연경계 처리) 구현. ②범위축 신규 — LiveOrderFilters에 `scope: "all"|"broadcast"|"shop"` 추가. filteredOrders에 matchScope **레이어만 추가**(all=제한없음/broadcast=상시제외/shop=상시만, isAlwaysOrderLike 기준), 기존 matchBroadcast·isPaid·matchesStatus 돈/방송 판정 **무변경**. ③UI(LiveOrderTable) — 방송 단일 드롭다운(방송+상시날짜 뒤섞임)을 [기간][범위][상태] 3칸으로. 범위=방송일 때만 BroadcastSearchSelect 노출(상시:always 옵션 제외 + hideShopOption prop으로 하단 공구·상시 버튼 숨김). 범위 비-방송 전환 시 broadcast="all" 리셋(빈화면 방지). ④needsFullLoad에 lastmonth·custom 추가(과거 범위 500건 캡 누락 방지). ⑤첫로드 방송중 기본=scope:broadcast+broadcast:current. 검수: tsc 0에러, BANKDA 가드 통과, 돈판정 diff 무변경 확인, lastmonth/scope 시뮬 전수통과. 로컬 next build 통과(BANKDA 가드+Compiled OK, 정적 34페이지 생성). 배포 완료
- 고객 하단바: "담은상품 N개·확인하기" 1버튼 통합, 제출은 주문서 확인 시트 내부에서만 (a70d8d5). 0개시 비활성.
- 2026-06-09 세션18(재고 key 불일치 버그 수정): QuickProductFastForm.tsx buildVariantRows(203~220) key 생성을 로드(636줄)와 동일 형식으로 통일 — 210줄 `${color}__${size}` → `${color || "__EMPTY_COLOR__"}__${size || "__EMPTY_SIZE__"}`. 기존엔 색상·사이즈가 둘 다 빈문자열인 변형(variant)의 경우 로드 key(__EMPTY_COLOR____EMPTY_SIZE__)와 재생성 key(__)가 불일치 → previousMap 미스 → 저장/재고편집(updateVariantStock 738) 시 해당 재고 0으로 유실. 이제 로드(636)·재생성(210)·편집핸들러(738) 3곳 key 일관. 없음 sentinel은 truthy라 영향 없고, 진짜 빈옵션만 해당. 돈/입금 로직 무관(657e20c)
- 2026-06-09 세션18(상품목록 SOLD OUT 업계표준): app/order/page.tsx 상품목록 카드 — ①SOLD OUT 오버레이 표시조건 isSoldOutOrderProduct(product)→sold(버튼 disabled/품절라벨과 동일기준 통일) ②오버레이 스타일 업계표준화(bg rgba(0,0,0,0.55)+flexDirection column, SOLD OUT 폰트 11px/letterSpacing 0.1em) ③카드 전체 opacity(sold?0.5:1) 제거→항상1(오버레이로 충분). 돈/입금/정산/배송 로직 무변경, UI만(47d31cd)
- 2026-06-08 세션17(공지배너+직접입력 토글+주문취소 조회): ①주문조회에 "주문취소" 상태 추가(CustomerOrderLookupBottomSheet 타입+빨강배지#C0392B+필터그리드 repeat(filters.length), order/page ORDER_LOOKUP_FILTERS+ruruOrderLookupStatusLabel 취소 최우선판정 payment_status canceled 포함 관리자 정합)(4a4668e). ②TopCustomerNav 헤더 목업교체 — 루루동이+👋닉네임님!(좌)+☰메뉴보기(우)만, 담은상품버튼 헤더서 제거(하단바 유지), 딥로즈#7A1E47. ③손님 주문페이지 settings 연동: noticeText/directInputEnabled state+loadOrderSettings 조회. TopCustomerNav 아래 공지배너(비면 숨김, bg#F9EEF3 좌보더3px#7A1E47), 직접입력버튼 문구"+상품을 못 찾으셨나요? 직접 입력하기"+directInputEnabled=false면 숨김. ④AdminLiveSettingsPanel "주문서 공지/직접입력" 카드(공지 textarea + 직접입력 ON/OFF 토글, settings notice_text/direct_input_enabled upsert, NumericSettingKey 분리). 돈/입금/주문제출 로직 무변경(9246da8). ⚠️settings에 notice_text/direct_input_enabled 행 없으면 기본값(빈공지/직접입력ON)
- 2026-06-08 세션17(카카오ID저장+변경이력): customer-login-sync route — insert에 kakao_id/kakao_nickname/first_login_at/last_login_at, update에 kakao_id보완·nickname갱신·last_login_at항상갱신, customer_history 변경기록(name/address/detail 채워질 때 {field,old,new,changed_at} append)(b3eb1d3). 회원상세(AdminLiveCustomersPanel) 카카오정보+로그인시각+변경이력 표시, selectedProfile 전화매칭(5e95c0e). ⚠️add_customers_kakao_columns.sql + customer_history(jsonb) 컬럼 Supabase 적용 필요
- 2026-06-06 세션16(주문서 배지·입금매칭 리스킨 + 가드): ①닉네임 클릭→주문상세 드로어는 이미 연결돼 있음(onSelectOrder→setOrderDetailOpen→LiveOrderDetailDrawer), 변경 불필요. ②LiveOrderTable 상태배지 시안① 팔레트 인라인(입금확인green/매칭필요amber/대기·미결제red/취소muted/카드완료blue), 상태칩(전체/입금대기/입금확인/주문서취소)은 이미 딥로즈+로직 그대로(8cd4dec). ③입금매칭(admin-v2) 시안⑧ 추가 리스킨: 헤더 '⇄ 입금매칭' 딥로즈+확정버튼 slate→녹색, 매칭 로직 무변경(9a5b82d). ④금지사항에 'LiveOrderTable 임의 전체교체 금지·최소수정만' 추가. ⑤룰렛 자동지급 중복가드(5a235d5)
- 2026-06-06 세션16(시안 화면 교체 다건 + 주문 realtime): ①주문서 실시간 새로고침 버그수정(907d1de) — useAutoBankdaPaymentSync가 매 폴링마다(successCount 0이어도) onSynced+window이벤트로 loadOrders 2번 호출해 주문표 계속 리셋되던 것을, onSynced는 detail.successCount>0일 때만 loadOrders + window핸들러는 입금내역만 + supabase realtime orders INSERT/UPDATE 구독→디바운스 loadOrders 추가(새주문 즉시반영). BANKDA setInterval·가드 무변경. ②회원상세(CustomerDetailDrawer) 시안⑥ 중앙모달 딥로즈(59d1189). ③정산(admin-v2 SettlementMoneyFlowDashboard) 리스킨 파랑→딥로즈, 계산·방송별테이블·연월필터 유지(0e77d6b). ④설정 입력 포커스링 딥로즈(f408179). ⑤입금매칭(admin-v2 ManualPaymentMatchDrawer) 검색/새로고침 파랑→딥로즈, 매칭로직 무변경(b2fc232). ※admin-v2는 사장님 명시 허용. ⚠️포인트 일괄지급(시안⑩)은 빈 placeholder=신규기능이라 이번 건너뜀(별도 spec 필요)
- 2026-06-06 세션15(이벤트 입금완료 필터 서버연동·P2): app/api/admin-live/event-roulette/route.ts handleParticipants가 paidOnly 파라미터 읽어 buildParticipantsForRequest(…,paidOnly) 전달 → paidOnly면 주문 rows를 admin_order_status_v2/order_manage_status가 자동입금확인·수동입금확인·카드결제완료인 것만 필터 후 참가자 구성(isPaidOrderRowForRoulette). 기존엔 서버가 paidOnly 무시해 전체와 동일했음. buildRouletteparticipants(import)·추첨/포인트 로직 무변경. PUSH 0b30e29
- 2026-06-06 세션15(당첨자 목록 종류뱃지): AdminLiveEventRoulettePanel 당첨자행에 🎡룰렛/🪆인형뽑기 표시 — winners엔 종류정보 없어 w.event_id로 events 매칭→overlay_token 접두사(roulette/claw) 판별, 매칭실패 시 "이벤트". 프론트 표시만. PUSH 7d1731a ※events 응답이 과거 당첨자 이벤트 다 포함 안하면 "이벤트"로만 뜸 → 그땐 winners API에 종류 내려주기 보강 필요
- 2026-06-06 세션15(고객정보 중복row 재발방지): app/order/page.tsx saveCustomer가 customer_phone으로만 조회/갱신해 번호변경 시 새 row INSERT(중복 원인 — 471/475 같은). saveCustomer(previousPhone?) 추가 → lookupPhone=옛번호≠새번호면 옛번호로 조회/update(customer_phone을 새번호로 갱신). 정보수정 호출(2374)만 customerInfoEditSnapshot?.customerPhone 전달, 주문제출(3037)·돈로직 무변경. sed 다중매칭(cleanPhone 7곳) 위험 회피 위해 Edit으로 saveCustomer에만 적용
- 2026-06-06 세션15(적립률 소수점·소급 1.5%): 설정 적립률칸 소수점 허용(공유 SettingInput에 선택 props type=number step0.1 min0 max100 inputMode=decimal만 추가, 기존 칸 무변경 / decimalInput 핸들러로 onlyDigits 대체 → 1.5 입력가능). 소급적립 SQL(order_point_backfill_today.sql) 적립률 고정 1.5%(=15/1000 bigint 정수연산, 설정값 의존·v_rate 제거). PUSH d5e2ceb
- 2026-06-06 세션15(포인트 자동적립): 설정에 "포인트 적립 규칙" 카드(자동적립 ON/OFF 토글 + 적립률 입력, settings.point_auto_earn_enabled/point_earn_rate, 적립률 소수점 허용 type=number step0.1). 결제완료(자동·수동 입금확인/카드결제완료) 시 자동적립 = DB 트리거 SQL(supabase/sql/order_point_auto_earn_rpc.sql, 별도 적용 — 입금 JS 무수정, 상품금액(택배비제외)×률, 1주문1회 point_earned_at, 포인트사용/테스트/정산제외 주문 제외, 선물팝업 미표시 customer_seen_at=now). 주문서 멘트 "구매금액 N% 적립"/OFF숨김(OrderPriceSummaryBox pointEarnRate prop). 오늘건 소급적립 SQL(order_point_backfill_today.sql, 1.5% 고정=15/1000, 1단계 미리보기+2단계 DO). orders ADD COLUMN point_earned_at/point_earned_amount. 돈/입금/매칭 로직 무변경. ※SQL 2개는 Supabase SQL Editor 직접 실행 필요
- 2026-06-06 세션15(위젯 실시간 토스트·버그2건): components/product-widget/ProductWidgetClient.tsx — 주문(orders INSERT)/입금확인(자동·수동, admin_order_status_v2·order_manage_status가 ~'입금확인')/카드결제완료 UPDATE 4종 실시간 토스트, **큐 방식**(order_group_id 중복방지, 상단 딥로즈 반투명 3초 순차). 버그수정2: ①금액 0원 → amount()가 total_amount만 읽던 것을 **final_amount(=total_price/adjusted_total_price) 우선**으로(submit route 실제 컬럼) ②토스트 안사라짐 → 큐 useEffect가 [currentToast,toastQueue] 의존이라 표시직후 재실행으로 setTimeout 즉시clear됨 → **표시 effect와 자동숨김 effect([currentToast]만) 분리**. 옵션 색상만 표시(colorsOf, 사이즈 제거). 카드 크기 280px로 적당히 축소(이미지72/상품명14/가격16). 투명배경 유지
- 2026-06-06 세션15(상품카드 이미지 버그): app/order/page.tsx loadBroadcastProducts 매핑이 image_url/main_image_url/thumbnail_url을 누락 → quickGroupBuyProducts dedup([...catalog,...broadcast]→Map)에서 broadcast가 catalog를 덮어써 이미지 빈칸. **broadcast 매핑에 image_url=pickOrderProductImageUrl(product) 추가**로 수정(이미지 표시만, 주문/돈 로직 무변경). ※여전히 안뜨면 image_url이 전체URL 아닌 스토리지 경로인 경우 — 경로→URL 변환 추가 필요
- 2026-06-06 세션15(카드결제 팝업 최종): components/admin-live/AdminLiveCardPayPopup.tsx — 최종형 = 좌(복사4칸 패널)+우(페이스터 iframe) 좌우 960px. **window.open 별도창 완전 제거**(openPaysterRightHalf no-op로, LiveOrderTable import 호환 유지). handleComplete(카드결제완료 상태변경) 로직 무변경. ⚠️페이스터(user.service.payster.co.kr)가 iframe 임베드 차단(X-Frame-Options/CSP)하면 우측 빈화면 → 그 경우 window.open 방식 복귀 필요(검증 대기)
- 2026-06-06 세션14(고객 order page P1): app/order/page.tsx(4664줄) 상단바를 손님페이지 시안(/Users/ruru/Downloads/files/루루동이_손님페이지_위젯_통합본.txt)대로 딥로즈 1:1 인라인 교체 — 루루동이+LIVE배지(방송ON시)+주문서아이콘(담은개수 cartCount 배지)+☰. ☰→메뉴 바텀시트(주문조회/정보수정/내포인트/유튜브·카톡·밴드·인스타 링크 placeholder, 기존 openOrderLookup/openCustomerInfoEdit 핸들러 재사용). MENU_ITEM_STYLE 모듈상수. 주문제출/돈/포인트 로직 무변경. ※order page는 섹션별 증분(P2 주문방법~P10 위젯)
- 2026-06-06 세션13-2(상품 시안 P2): 새 상품 등록 폼(QuickProductFastForm) 시안③ 인라인 1:1 교체(560px 중앙모달, .ruru-product-sian). 드로어(AdminLiveQuickProductDrawer)는 우측 aside/헤더 제거→폼만 렌더(모달 본체=폼). 배송구분 select normal/vendor/vendor2(=업체배송1/2), 상품종류 broadcast/group_buy(방송상품/상시판매), 방송화면 캡처=UI버튼+토스트(기능없음), 옵션4줄(색상/사이즈+프리셋/수량/금액)+재고관리·고객노출 토글. 저장로직(saveProduct/insertProductSchemaSafe/broadcast_products/combine_shipping 등) 100% 보존, 렌더만 교체. ※combine_shipping은 vendor만 N(미변경) — vendor2 합배송 처리 필요시 별도
- 2026-06-06 세션13(상품 시안 P1·P3): 상품관리 팝업(AdminLiveProductManagePopup) 시안② 인라인 1:1 교체(.ruru-product-sian 스코프 globals.css, 560px 고정) + 순환/고정 모드 토글(순환=broadcast_products insert 기존, 고정=products.is_pinned update 기존고정해제후 선택고정). 지금띄운상품 패널(LiveBroadcastPanels): 고정상품 있으면 1개, 없고 순환 있으면 broadcast_products 목록 나열, 둘다없으면 안내(activeBroadcastId prop 추가, 상품변경 이벤트 갱신). 돈/배송 로직 무변경. ※P2 잔여=QuickProductFastForm(③ 새상품등록, 1244줄) 시안 인라인 이식 — 저장로직 보존 위해 별도 정밀 패스 필요
- 2026-06-06 세션12-2(중복당첨 토글 연동): event-roulette API의 중복당첨 방지(applyNoDuplicateWinnerRule, 666 "이미 모든 참여자 당첨" 에러)를 excludeDailyDup으로 게이트 — false면 중복체크 건너뜀(handleParticipants searchParams + createEvent body 둘 다, 기본 true). 패널이 "당일 중복당첨 금지" 토글값을 create_event 3곳 + 참가자GET에 전송. 돈/포인트/추첨 로직 무변경
- 2026-06-06 세션12(이벤트 4건): 당첨자 카드=overlay result-card 디자인 정밀화 / 룰렛 회전 30바퀴 / 인형뽑기 탭=canvas 숨기고 🕹️인형UI 표시(버그수정) / 당첨 확정 시 giftType=point+금액 있으면 **포인트 자동지급**(운영모드만, orders 최신주문 customer_phone로 닉네임→전화 매핑, 기존 /api/admin-live/customer-points API 재사용, 테스트모드는 미지급). 모달 680px 고정 유지. 추첨 로직 보존
- 2026-06-06 세션11-3(이벤트 시안 인라인 그대로 이식): screens.event의 HTML/CSS를 Tailwind 변환 없이 인라인 style + 시안 클래스(.btn/.note/.nick/.tog/.row/.badge/.wheel 등)로 이식. globals.css에 시안 :root 변수(--rose/--rose-bg/--rose-bd/--green/--blue 등) + 클래스를 `.ruru-event-sian` 스코프로 추가(전역충돌 방지). 룰렛은 canvas 실제스핀 유지(화살표 휠 안으로 삽입). 추첨/포인트/API/상태 로직 보존, 핸들러만 연결. + UI작업 필수원칙/룰렛기준 섹션 CLAUDE.md 추가
- 2026-06-06 세션11-2(이벤트 시안⑨ 1:1 + 실제 canvas 룰렛): 패널 본문을 시안⑨ 단일컬럼으로 재구성(상단 룰렛+돌리기 | 참가자3버튼[👥주문서전체/💵입금완료/✎수동]+당일중복금지·가중치 토글 / 당첨고정 rose박스 칩 / 제목+당첨선물50:50 / 위젯주소 / 이벤트목록 기간필터+달력). 시안색상(rose-deep/rose-soft/rose-line/#f7f5f1). conic div → **실제 canvas 룰렛**(requestAnimationFrame, 당첨확정 시 8~14바퀴·4~6초·easeOutCubic로 당첨칸 정지, 화살표 상단삽입, 가운데 당첨자 오버레이). 추첨/포인트/API/상태 로직 100% 보존. ※가중치·당일중복 토글·입금완료필터는 여전히 서버연동 P2/P3
- 2026-06-06 세션11(이벤트 패널 UI 시안 완전교체): AdminLiveEventRoulettePanel 렌더 JSX 전체를 시안 2단 레이아웃으로 교체(헤더 ◆이벤트+룰렛/인형뽑기탭+모드+초기화+✕ / 좌: conic-gradient 룰렛휠+화살표+가운데 당첨자오버레이+돌리기+위젯주소복사+이벤트목록[기간필터 오늘·주·월·날짜+달력, dateTimeFull 년월일(요일)시간] / 우: 참가자3버튼[활성방송기준·OFF비활성]+목록토글+채팅@파싱 / 당첨고정칩 / 당일중복금지·가중치(누적금액40%+당일60%) 토글 / 당첨선물 50:50). 룰렛=conic-gradient div(canvas 대신, 동일 모양·안정적). 기존 추첨/포인트/API/상태 로직 100% 보존, 렌더만 교체. 미사용 함수 일부 잔존(로직 보존 원칙). ※가중치·당일중복 토글은 UI 상태만(서버 추첨공식 연동은 P3), 입금완료 서버필터는 P2
- 2026-06-06 세션10(이벤트 P1-B): 참가자 3버튼(주문서전체/입금완료/수동) · 불러오기 기준=현재 활성방송(activeBroadcastId prop, Dashboard 전달) · 방송OFF시 전체/입금완료 버튼 비활성+안내 · 목록 토글(기본숨김) · 채팅붙여넣기 @파싱(중복제거) · 당첨고정 칩클릭(select 이중목록 제거)+"👑 당첨고정:닉" 한줄 · 당첨선물 드롭다운(포인트/직접)50:50+포인트 쉼표포맷 · 돌리기 시 currentEvent=null로 이전당첨자 제거. 추첨/포인트/돈 로직 무변경. ※입금완료 서버필터(paidOnly)는 P2 연동 대기
- 2026-06-06 세션10(이벤트 P1-A): 사이드바 "◆ 이벤트" 메뉴 추가(adminLiveMenu event 키) + AdminLiveEventRoulettePanel을 Dashboard에 항상마운트(controlled props renderTrigger/controlledOpen/onRequestClose, activeMenu=event일때 표시, 닫아도 상태유지) + 초기화 버튼(참가자+당첨고정+currentEvent 리셋). 추첨/포인트/주문 로직 무변경. ※기존 LiveHeader/LiveOrderTable의 "🎁 이벤트" 버튼 인스턴스는 잔존(별도 state) — 후속 정리 검토. ※P1 잔여=참가자3버튼/채팅@파싱/당첨고정칩/당첨선물 드롭다운(P1-B)
- 2026-06-06 세션9-2: 카드결제 복사창 좌우분할 개선 — 💳카드결제 버튼 클릭 시 복사창+페이스터 동시열림(openPaysterRightHalf를 LiveOrderTable 클릭 제스처에서 호출→팝업차단 회피), 페이스터=화면 오른쪽절반(left=½/width=½/height=전체), 복사창 모달=화면 왼쪽절반 고정(fixed inset-y-0 left-0 md:w-1/2)
- 2026-06-06 세션9: 카드결제 복사창 신규(AdminLiveCardPayPopup, 시안⑫). LiveOrderTable card_unpaid 입금셀에 "💳 카드결제" 버튼→팝업. 복사4칸(상품명 외N건요약/결제금액 cardPaymentTotalAmount우선 없으면 totalAmount 숫자만/닉네임 매칭기준/전화 하이픈제거) 각 클립보드복사. 페이스터 window.open(PC≥1024 우측반분할, 복사창 유지). "카드결제완료 처리"=DetailDrawer handleCardPaymentStatusChange와 동일 update(orders admin_order_status_v2/order_manage_status="카드결제완료", order.items id 기준)→loadOrders, 신규 결제write 없음
- 2026-06-06 세션8-2: 팝업 제목 시안 통일(Dashboard 정산통계→정산·입금확인→입금내역, admin-v2 ManualPaymentMatchDrawer 수동입금매칭→입금매칭). 제목 텍스트만, 돈/입금/정산 로직 무변경. ※admin-v2는 사장님 명시 허용으로 제목만 변경(구버전 AdminV2Client도 같이 바뀜, 미사용)
- 2026-06-06 세션8: 상품관리 팝업 신규(AdminLiveProductManagePopup, 사이드바 "상품" 메뉴→fixed모달, 시안②기준 탭[방송상품/공구·상시판매/전체창고]+상품명검색+올림날짜(created_at)+2열카드 다중선택+페이지네이션, "+새상품등록"→기존 ruru-open-quick-product-panel 이벤트로 ③폼, "순환담기"→broadcast_products insert(중복제외, 활성방송필요)). 방송/돈 로직 무변경. 가정: 공구·상시탭=product_type group_buy(상시판매 별도타입 없음), 올림날짜=created_at. 기존 AdminLiveProductListPanel은 미사용으로 잔존
- 2026-06-06 세션7-3: 시안 라벨 통일(사이드바 메뉴명 방송→주문·입금/정산통계→정산/고객관리→고객·이슈/카톡상담→카톡채널, 메인 송장출력·물건챙기기·검색placeholder 시안문구, 팝업제목 주문상세→✎주문서수정/포인트관리→🪙포인트/CUSTOMER DETAIL→👤회원상세) + 방송모드 토글 추가(AdminLiveSidebar, LiveHeader title/url을 Dashboard로 끌어올려 공유, URL있을때만 ON가능, ON→startBroadcast OFF→endBroadcast, start/end 로직 무변경)
- 2026-06-06 세션7-2: 용어 표시라벨 2차 통일(AdminLiveDashboard/AdminLiveOrdersPanel/adminLiveOrderExcelExport에서 "입금매칭 필요"→"매칭필요", "카드 미결제"→"카드미결제", 표시만·영어 status코드 무변경, 엑셀 상태칸 텍스트도 반영). CustomersPanel/SettlementPanel은 정규식(분류 로직)이라 표시통일 대상 아님
- 2026-06-06 세션7: 용어 표준 확정(입금대기/입금확인/매칭필요/카드미결제/카드결제완료, 입금내역/입금매칭) · 표시 라벨 통일 1차(LiveOrderTable/LiveOrderDetailDrawer/LiveStatsCards에서 "입금매칭 필요"→"매칭필요", "카드 미결제"→"카드미결제", 영어 status 코드·분류 정규식은 무변경)
- 2026-06-06 세션6: 방송패널 3분할(영상+채팅+지금띄운상품 flex 1:3:1.2, 높이360, 영상9:16) / 지금띄운상품=고정1개(사진+상품명+금액+옵션+다른상품띄우기) / 등록상품리스트패널 제거 / 주문서 10칸(주문일·닉네임·이름·주문내용·수량·상품금액·택배비·총금액·입금·출고, 주문일=createdAt YYYY.MM.DD/(요일)HH:mm 두줄, 입금=배지+paidAt, 주문내용 font-black) / 헤더+셀 전부 가운데정렬(text-center) 통일
- 2026-06-05 세션4~5: 주문서 그리드 개선, 실시간 필터, 매출요약 6항목, 방송패널 3분할 진입
- 2026-06-05 세션3: 고객관리 모달 완성(회원목록/고객이슈), 메인화면 재설계 진입, 매출요약6항목 배포, DB 확인(배송처·출고 데이터 비어있음)
- 2026-06-04 세션1~2: 딥로즈 색상 전면적용(파랑제거), 주문테이블 카드형, 사이드바→팝업모달 구조 전환

## 남은 작업 (우선순위 순 · 끝나면 지우고 진행상황에 기록)
[admin-live 관리자]
1. 화면 전체 용어·버튼·라벨·문구 시안 기준 전수 통일 (입금상태뿐 아니라 전 화면 일괄, 일부만 고치지 말 것) — 입금상태·메뉴·주요 팝업제목(정산·입금내역·입금매칭 포함) 통일 완료. 잔여 세부 라벨은 화면별 점검 시 추가
2. 이벤트 — P1 완료. ✅완료: 시안⑨ 인라인, canvas 실제스핀, 인형뽑기 탭, 당첨 포인트 자동지급(운영모드만), 중복당첨 토글(excludeDailyDup), **입금완료 서버필터(paidOnly)**, 당첨자목록 종류뱃지. 잔여: P2-나머지(winners 기간필터 서버연동), P3(가중치 추첨공식 서버 — 토글UI만). ✅당첨 자동지급 중복가드(5a235d5): grantedEventIdsRef 세션가드 + is_reward_done 영구게이트(지급성공 시 mark_reward_done true) — 재실행/재로드 재지급 차단, 지급↔지급완료 배지도 자동 연동됨
3. 수동매칭 설정 팝업 메뉴
4. ⚠️ 포인트 일괄지급(시안⑩) 신규 구축 — 현재 빈 placeholder. 명단추가→한번에 지급(grant API 반복)+기간별 기록. 새 지급로직이라 spec 받고 진행
5. (사장님 추가 예정 — 그 외 작업 생각나는 대로 여기 누적)
   ※ 후속: 상품관리 팝업 — 상시판매 별도 type 도입 / 올림날짜 전용 필드 / 기존 AdminLiveProductListPanel 정리 여부

[포인트 자동적립] — 코드(설정 UI·주문서 멘트·트리거 SQL·소급 SQL)는 완료. 운영 적용/검증 남음:
- ⚠️ Supabase에서 order_point_auto_earn_rpc.sql 실행(미적용이면 자동적립 안 됨) + 설정에서 자동적립 ON + 적립률 저장
- 소급적립(order_point_backfill_today.sql) 오늘건 1.5% 실행 여부는 사장님 판단(1단계 미리보기 후)
- 시안 ⑪ 적립규칙 잔여: "90일 미사용 소멸" / "환불·취소 시 자동 회수" 토글 — 이번엔 ON/OFF+적립률만 구현. 필요 시 추가 설계
- 포인트 사용 주문은 현재 적립 제외(정책). 포함 원하면 트리거/소급 SQL의 point_used_amount 조건 제거

[카드결제 팝업] — ⚠️ 페이스터 iframe 임베드 차단(X-Frame-Options) 시 우측 빈화면 → window.open 방식 복귀 필요(실기기 검증 후 결정)

[고객 주문 페이지] — 시안: /Users/ruru/Downloads/files/루루동이_손님페이지_위젯_통합본.txt (텍스트 사양, 딥로즈 #7B2D43 인라인). app/order/page.tsx 섹션별 증분:
- ✅ P1 상단바+☰메뉴 (세션14)
- ✅ P2 주문방법 접기/펼치기 (추가형 howToOpen)
- ✅ P3 영상 — 방송 ON+유튜브URL일 때만 16:9 임베드+접기 (videoOpen/videoEmbedSrc)
- ✅ P10 방송 위젯 신규 라우트 /product-widget (OBS, 고정/순환/주문토스트/투명배경, 읽기전용)
- ✅ P4 상품목록 — 딥로즈 검색+2열격자+페이지네이션, 카드(방송중배지=is_pinned/품절 SOLD OUT), 직접입력 fallback (quickGroupBuyProducts/selectQuickGroupBuyProduct 재사용)
- ✅ P5 옵션 선택 시트 — 딥로즈, 색상/사이즈(선택·입력형)+수량+선택금액+필수 빨강경고 (registeredOption* 재사용)
- ✅ P6 담기 confetti 토스트 — addRegisteredProductToOrderItems 후 '🎉 주문서에 담았어요!'+주문서보기/계속담기
- ✅ P7 주문서 결제부 딥로즈 — OrderPriceSummaryBox 인라인 재작성(금액내역+보유포인트/사용입력/전액사용/사용차감/🪙적립예정), 결제방법(무통장·카드+率)+카드안내+제출바+장바구니 코랄→딥로즈. 금액/포인트/제출 로직 100% 무변경(렌더만). ※적립률 코드에 없어 '적립 예정' 숫자 미표기(일반안내) — 률 정하면 숫자 반영
- ✅ P8 제출 후 계좌안내 — CustomerPaymentGuideBottomSheet coral→rose-deep/soft/line 딥로즈(입금자명·계좌복사·닉네임복사·금액 무변경)
- ✅ P9 주문조회 배지 — 입금확인 초록/택배출고 파랑/입금대기 노랑/그외 회색, 활성탭 등 딥로즈 통일
- ★ 고객 order page P1~P10 전체 시안 1:1 완료(딥로즈 #7B2D43). 돈/입금/주문제출/포인트 로직 전부 무변경
- 진입(카톡로그인/유튜브닉네임 유니크) 화면도 시안 반영 대상

※ 위 목록은 완전하지 않을 수 있음. 사장님이 새 작업 말하면 즉시 여기 추가할 것.

## 필수 확인 절차
코드 수정 전 반드시:
1. 관련 파일 nl -ba로 실제 라인 확인
2. 돈/입금/정산/배송 로직 포함 여부 확인
3. 변경 범위 최소화

## 금지 사항
- 추정으로 돈/입금/정산/배송/주문상태/포인트/Bankda 로직 수정 금지
- grep 결과만 보고 수정 금지
- order_items 테이블 신규 write 금지 (deprecated)
- localStorage에 운영 데이터 저장 금지
- admin / admin-v2 폴더, DB schema·RLS 건드리지 말 것
- **LiveOrderTable.tsx는 임의로 전체 교체 금지. 기존 필터/로직(상태칩·정렬·페이지·검색·onSelectOrder 등) 유지하며 최소 수정만.** (닉네임 클릭→onSelectOrder→주문상세 드로어 연결은 이미 동작 중)

## 빌드 및 검수
npm run build (반드시 "✅ BANKDA 안전가드 통과" 메시지 확인)
grep -r "TODO\|FIXME\|console.log" components/ app/
git status

## 기술 함정
- 맥 sed s///1은 줄마다 첫 매칭이라 여러 줄 다 바뀜 → 여러 줄 치환은 python 스크립트 사용
- 긴 체인(sed+build+commit+push 한 줄)은 중간에 멎음 → 단계 분리

## ★★ 고객 식별 원칙 (2026-07-09 사장님 확정 — 절대 어기지 말 것)
- **고객의 정체성 = 카카오 계정이다.** (`customers.id` uuid PK / `customers.kakao_id`)
- **전화번호·주소·이름·닉네임은 전부 "바뀔 수 있는 정보"다. 절대 식별 키로 쓰지 말 것.**
- 고객이 번호를 바꾸든 주소를 바꾸든 **같은 사람**이다. 포인트·주문·이력이 전부 따라와야 한다.
- ❌ 앞으로 어떤 세션에서도 "고객 식별은 customer_phone 기준" 이라고 말하거나 그렇게 설계하지 말 것.
  (과거 진행상황 기록에 그런 문장이 남아 있으나 **폐기된 기준**이다.)
- ⚠️ 코드 실태: `customer_point_balances` / `customer_point_ledger` 의 **읽기·쓰기가 아직 `customer_phone` 기준**이다.
  (RPC·트리거·API 전부) → 번호를 바꾸면 여전히 새 고아 행이 생긴다. **STEP 3에서 전환 예정.**
- ✅ 2026-07-09 완료: 고아 포인트 복구(문수 1,395P `01058794497`→`01089904497` / 김정임 295P `01034200786`→`01034300786`,
  `supabase/sql/point_phone_migration_20260709.sql`) + **`customer_id` 컬럼 추가·백필 완료**
  (`supabase/sql/point_customer_id_step1.sql` 실행됨 — 잔액 441/441, 이력 930/930 연결, 미연결 0).
  단 **코드는 아직 customer_id를 읽지 않는다**(동작 변화 0). 기반만 마련된 상태.
- 신규 코드에서 포인트/회원 관련 조회·쓰기는 **customer_id 우선**, phone은 폴백/연락처로만.
- ✅ 2026-07-09 **근본수정 완료**: ①`customer-login-sync`가 **kakao_id로 고객을 먼저 찾음**(전화번호는 폴백)
  → 번호 바꿔도 같은 사람, 중복 고객 row 생성 안 함. 번호가 바뀌면 `customers.customer_phone`을 갱신(unique 충돌 시 스킵).
  ②**DB 트리거 `trg_sync_identity_on_phone_change`**(`supabase/sql/point_identity_sync_trigger.sql`, 2026-07-09 실행 완료)
  — 번호 변경 시 `customer_point_balances`(있으면 합산)·`customer_point_ledger`·`customer_phone_blocks`가 자동으로 따라감.
  트리거는 exception을 삼키고 warning만 남김 → customers 저장(주문 제출) 절대 안 막음(주소 트리거와 동일 방침).
  ③`trg_fill_point_customer_id_*` — 신규 포인트 행에 customer_id 자동 채움. **돈 계산·지급 RPC/API 무변경. 시뮬 17/17 PASS.**
  → **차단 우회도 함께 해결**(차단 행이 새 번호로 따라감).
- ✅ 2026-07-09 **남은 2건도 해결**:
  ①**1인당 구매제한 우회 차단** — `assertPurchaseLimit`(submit route)이 `kakao_id` 기준 누적으로 전환
  (`kakao_id.eq.X` OR `and(kakao_id.is.null, customer_phone.eq.현재번호)`). kakao_id 없으면 기존 전화번호 폴백(회귀 0).
  ②**주문취소 실패 위험 제거** — `cancel_order_and_restore_points` 3단 폴백으로 교체
  (`supabase/sql/order_point_cancel_restore_fix_20260709.sql`): ①주문 번호로 잔액 찾기 → ②없으면 주문의 kakao_id로
  고객 **현재 번호**를 찾아 재시도 → ③그래도 없으면 잔액 row를 생성해 복구. **`raise exception` 제거**(번호 자체가 없는 주문만 예외).
  ledger/balance 모두 "실제 잔액이 있는 번호"에 기록 → 고아 재발 0. **복구 금액식·중복복구 차단·정산/입금 계산 무변경.**
  ③트리거 `trg_sync_identity_on_phone_change`에 (d) 추가 — 번호 변경 시 **옛 주문에 kakao_id 스탬프**(주문의 번호·금액·상태는 불변).
  검수 tsc 0 + **시뮬 16/16 PASS**(우회 차단·회귀 0·취소 항상 성공·중복복구 방지).
  ⚠️ Supabase에 `order_point_cancel_restore_fix_20260709.sql` + 갱신된 `point_identity_sync_trigger.sql` 실행 필요.
- ※ 입금 자동매칭은 `orders.youtube_nickname`(주문 시점 스냅샷)+금액 기준이라 **고객이 닉네임을 바꿔도 과거 주문 매칭은 안 깨진다**(2026-07-09 확인).
- ✅ 2026-07-09 **로그인 안전망 + 중복 kakao_id 정리**: ①login-sync 번호 갱신이 unique 충돌하면 **번호만 빼고 재저장**
  (로그인 500 실패 방지 — kakao-first 전환으로 새로 생긴 실패 경로를 막음). 빈 번호 row도 이때 채움.
  ②같은 kakao_id 중복 고객 2쌍(750/752 최송아, 721/1129 송기영) 정리 — 포인트0·주문0·참조0 확인 후 삭제,
  백업 `customers_backup_20260709_kakao_dedup`. 중복 kakao_id 현재 **0건**. (login은 created_at 최신 row를 선택 = 진짜 row)

## DB 규칙
- 스키마 변경: ADD COLUMN only
- canonical 테이블: orders (flat)
- 입금 추적: deposits 테이블
- 포인트 주인: `customers.id` (카카오 계정). `customer_phone`은 연락처일 뿐 식별자가 아님.

## DB 변경사항 — Supabase SQL Editor에서 직접 실행해야 적용됨 (커밋만으로는 미적용)
포인트 관련 테이블: customer_point_balances(잔액), customer_point_ledger(이력, change_type grant/use/cancel/adjust/expire), customer_point_gifts는 ledger의 customer_visible+seen으로 표시.
적립 금액 컬럼은 orders.final_amount(=total_price=adjusted_total_price, 택배비 제외 상품금액은 adjusted_product_price?? product_price × qty).
- **supabase/sql/order_point_auto_earn_rpc.sql** — 자동적립 트리거. orders ADD COLUMN point_earned_at/point_earned_amount + 트리거 trg_accrue_order_points(입금확인·카드결제완료 전환 시 settings.point_earn_rate%로 적립, 1주문1회, 포인트사용/테스트/정산제외 제외, customer_seen_at=now라 선물팝업 미표시). **아직 미적용이면 자동적립 안 됨.**
- **supabase/sql/order_point_backfill_today.sql** — 오늘(KST) 이미 결제완료된 건 소급적립(고정 1.5%=15/1000). 1단계 미리보기 SELECT → 확인 → 2단계 DO 실행. point_earned_at 가드로 트리거와 중복 안 됨.
- 기존: customer_order_submit_with_points_rpc.sql(주문저장+포인트차감 RPC, 적립 없음), customer_points.sql, order_point_usage_columns.sql 등
- ⚠️ 자동적립을 쓰려면: ① 위 트리거 SQL 적용 + ② 관리자 설정에서 "자동적립 ON" + 적립률(예 1.5) 저장 (둘 다 필요)


## UI 작업 필수 원칙 (완전 리뉴얼 프로젝트)

## 핵심 원칙 (절대 준수)
- 이 프로젝트는 기존 UI/UX를 전부 무시하고 시안 기준으로 완전히 새로 만드는 리뉴얼 작업
- 기존 코드 위에 얹거나 수정하는 방식 금지. 반드시 시안 기준으로 새로 설계
- UI 작업 시 시안 파일 /Users/ruru/Downloads/시안모음.html 에서 해당 화면 screens.XXX 코드 직접 읽고 HTML 구조/인라인스타일/클래스 1:1 JSX 변환
- Tailwind 임의 변환 금지. 시안 인라인 style 그대로 사용
- 시안에 없는 구조/색상/레이아웃 임의 추가 금지
- 시안과 다르게 만들면 작업 실패. 다시 만들 것
- 시안 작업 시 용어/단어는 시안 기준이 아닌 현재 시스템에서 사용 중인 통일된 용어를 따른다. 시안의 단어가 현재 시스템 용어와 다를 경우 현재 시스템 용어 우선.

- 이 프로젝트는 기존 코드를 수정하는 게 아니라 시안 기준으로 완전히 새로 만드는 리뉴얼 작업
- UI 작업 시 기존 코드 위에 얹지 말 것. 반드시 시안 파일을 직접 읽고 1:1로 구현
- 시안 파일 경로: /Users/ruru/Downloads/시안모음.html
- UI 작업 전 반드시: cat /Users/ruru/Downloads/시안모음.html 에서 해당 화면 screens.XXX 코드 읽고 구조/색상/비율 파악 후 구현
- 색상 변수: --rose:#7B2D43 --rose-bg:#F5E6EB --rose-bd:#D9C5CC --green:#0F6E56 --blue:#185FA5
- 시안과 다르게 만들면 작업 실패. 추상적 명령어로 임의 해석 금지

## 룰렛/이벤트 작업 기준
- 룰렛은 반드시 canvas + 실제 스핀 애니메이션 (conic-gradient div는 회전 안 됨)
- 화살표(▼)는 룰렛 안으로 50% 삽입 (너무 위에 떠있으면 안 됨)
- 당첨자 표시는 룰렛 가운데 오버레이
- 회전: 30바퀴, 4~6초, ease-out (당첨칸 정지)
- 방송용 위젯 배경은 반드시 transparent (OBS 크로마키용)

## 확정된 설계 기준
- 관리자 화면: 사이드바 클릭 = 페이지 전환 아님. 방송화면+채팅+주문서 항상 뒤에 유지, 각 기능은 fixed 팝업 모달로 오버레이
- 입금상태 용어: 입금대기/입금확인/매칭필요/카드미결제/카드결제완료
- 출고 용어: 출고대기/택배출고
- 결제수단: 무통장/카드
- 화면명: 입금내역/입금매칭
- 시안 파일: /Users/ruru/Downloads/시안모음.html

## 스택
Next.js / Supabase / Vercel
관리자: /admin-live 경로 (/admin 은 구버전, 미사용)
프로젝트 경로: /Users/ruru/Desktop/ruru-order-app

## 2026-06-10 관리자 UI 정리 (실시간주문 중심 통합)
- 테스트 주문 배지: 화면에서 숨김(testOrderBadge 항상 null). is_test_order 데이터/필터/정산제외 로직은 무변경. (8c436e3)
- 주문 날짜 연도/월 필터 추가: 날짜 드롭다운에 "연·월 선택" + 연도(올해~3년전)/월(1~12) 드롭다운. LiveOrderFilters에 filterYear/filterMonth 추가, matchesDate에 yearmonth 판정. 기존 필터(all/today/yesterday/7days/month/custom) 무변경. (커밋)
- 중복 새로고침 버튼 제거: 탭바 ↻ 삭제, 주문테이블 툴바 ↻(onRefresh)만 유지. 둘 다 loadOrders 호출하던 중복. (커밋)
- 주문 관리 탭/팝업 제거: 실시간 주문서와 기능 완전 중복(둘 다 LiveOrderTable). 실시간 주문서에서 "방송: 전체보기"로 전체 주문 조회 가능(filteredOrders의 broadcast==="all"이면 true). 탭 3개로 축소(실시간주문/입금내역/입금매칭). AdminLiveOrdersPanel.tsx 파일은 보존(삭제 안 함). (f358d28)

### 관리자 화면 원칙 (업계 표준 reconciliation/UX 검색 반영)
- 메뉴/패널은 적을수록 좋고 중복 제거가 표준. 주문은 상태탭+날짜필터 하나로 관리(별도 주문관리 페이지 불필요).
- 실시간 주문서 = 전체 주문 관리 겸용. "방송: 전체보기"가 전체 조회.

### 남은 작업 후보
- 실시간 주문서 "전체보기"에서 과거 전체 주문(1000건 초과)이 다 보이는지 검증 필요 — loadOrders도 행수 캡 영향 가능성. 안 보이면 페이지네이션 적용.
- 포인트 일괄지급 UI, 관리자 고객정보 직접수정, 정산 메뉴.

## 2026-06-10 주문 조회 범위 + 첫화면 기본값 (해결 완료)
- [해결됨] loadOrders의 .limit(500) 제약 해소: "전체보기" 또는 "연·월 선택" 필터일 때만 .range 페이지네이션으로 전체 주문 로드(needsFullLoad 분기). 평소(현재방송/오늘/7일/특정방송)는 500건 유지(방송 중 성능). filters.broadcast/date 변경 시 재조회 useEffect 추가(마운트 중복은 ref로 스킵). (933347d)
- 첫 접속 기본 필터: 방송 중이면 현재 방송 주문(broadcast="current"), 아니면 전체(라이브 표준). 1회성 초기화(didInitBroadcastFilter ref), 사용자가 바꾼 필터는 안 덮어씀. (커밋)

### 다음에 할 후보
- 알림 소리(새 주문 들어오면 소리) — 라이브 표준, 미구현. realtime orders INSERT 감지해서 소리 재생.
- 포인트 일괄지급 UI, 관리자 고객정보 직접수정, 정산 메뉴.
